# 修缮线的协作文件从第 2 轮起搬到自己的 worktree

第 1 轮（2026-08-30）已把 worktree 建成：

```text
D:\code\MC-fix          分支 feat/fix，基线 9b459e1f，独立 node_modules
D:\code\MC-server-fix   自己的服务器，端口 25566（RCON 25576），全新世界
MC_WORLD_ID = fixing-A:修缮世界
```

按本线协议「worktree 建成后迁入，此处只留指路条」，从**第 2 轮**起：

```text
D:\code\MC-fix\docs\协作\修缮\
├── 说明.md        本线协议（与本目录同名文件保持同步）
├── 给CC.md        开发 → 施工，本轮任务书
├── CC反馈.md      施工 → 开发，本轮结果
└── 主协议快照.md  主协议的离线副本，以建造线那份为准
```

本目录（`D:\code\MC-blueprint-ir-v1\docs\协作\修缮\`）保留第 1 轮的
`给CC.md` 与 `CC反馈.md` 作为过渡期记录，不再更新。

**注意**：`docs\协作\历史\` 仍然是三条线共用主仓这一个，只增不删。

放子目录而不是直接放 `D:\code\MC-fix\docs\协作\` 下，是因为那一层的
`说明.md` 和 `建造\` 是 git 跟踪的建造线文件，覆盖会造成分支间协议文件分叉。
放在 `修缮\` 子目录里全是未跟踪文件，零合并风险。
