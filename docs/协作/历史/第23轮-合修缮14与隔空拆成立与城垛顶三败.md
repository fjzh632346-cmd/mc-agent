# 建造 第 23 轮反馈

- 线: 建造
- 轮次: 23
- 日期: 2026-09-05
- 状态: 已完成（第 1、2、5 步全做完；第 3 步真机按 #48 同砖三败自停于 `605,82,-18`，真机只跑了 10 分钟；有 1 个问题等老板拍板）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / `d64c7777`（合修缮 14）；本反馈与文档随后一笔
- 实际运行模型 / 强度 / ultracode: Fable 5.1（`claude-fable-5-1`，已证实）/ 思考强度本端读不到档位，UNKNOWN / **未开 ultracode**（全程未用子代理）；全程未切模型

---

## 一句话结论

修缮线的"隔空拆根治"在门楼真身上成立了（够不着的柱子她如实认账、不再假报拆掉，收工按账扫描账外零残留），传送规则两次触发、两块高处砖传上去后各四秒盖上；但她随后站在一格宽的城垛顶上，对下一块砖算不出任何能走到的站位，三十秒一次连败三回，脚本按规矩停手——本轮真机只跑了十分钟、盖了十二块。

## 白话摘要

先把修缮线交来的两批代码收进主线，没有任何冲突，六十三项测试全部通过，主仓跟着更新。开工前按老板决定把那只被推错位的活塞拆掉了：先查了它和邻格的现状，删掉那一格，再复查邻格都没动。

真机上她认出原工地，开场三个数与预估一致。上一轮连撞三次的那只高处活塞，这次她从地面自己搭了六格土柱、四十六秒就放上了，随后四只活塞、两根铁栏杆、一块墙砖一气呵成，十二块全部用服务器命令核实过。修缮线这轮的修复得到了验证：她够不着的五格土柱，她老老实实记成"够不着"、没有发挖掘指令、账上留了五格；收工按账扫描，账外一格残留土都没有；两处她走过去拆的都是真拆掉了。

老板批准的"传送回屋顶"用上了两次：两次都是她在地面、目标高十五格、连续两次站不上去，传上去以后两块砖各四秒就放好了。锁占死这轮没有出现，自动重启的兜底没用上。

卡点出在最后：她站在一格宽的城垛顶上，对东边五格外的下一块砖算不出任何能走到的落脚点，三十秒失败一次、连败三次，脚本按"同砖三败"停手，我六秒内把她收了。这三次她一步都没挪、高度跟目标一样高，所以传送规则不适用；我的脚本在这种"钉在原地、零候选"的情形下也没有把她挪开——这是脚本的漏洞，我的责任。收工按规矩清了五格残留土，服务器优雅停机，进程清零。

下一步：问题 1 请老板拍板（这三败要不要算数、下轮怎么续）；"隔空拆"可以定论交修缮；"站在墙顶零候选"和"每次开工先去箱子补料、从屋顶算不到箱子"两条交修缮。

## 需要老板拍板的问题

### 问题 1：`605,82,-18` 这三败要不要算数？下一轮怎么续？
- 背景：这三次失败全是同一个原因——她钉在一格宽的墙顶上没挪过窝，系统对四十一个候选站位全部算不出路，三十秒失败一次，三次之间没有任何补救机会。上一轮定的"钉在原地就救援传送到地面"的兜底本来能救这一回，但我的脚本在"传送规则因为高度不够被跳过"的分支里漏掉了这个检查（已定位，改法只有几行，在保姆脚本里）。存档里这块砖现在是"待放、已重试三次"，下轮续建会先放它。
- 选项 A：三败照算、不改规则；下轮脚本补上"钉在原地 → 先救援传送到地面 → 再按传送规则回屋顶"的兜底，续建时正常先放这块砖。代价：无；风险：同类墙顶零候选还会出现，但每次都能被兜底救回，同时给修缮线攒数据。
- 选项 B：把"零候选且原地未动"的连续失败改成不计三败（先救援一次再开始计）。代价：改老板的规则 #48；风险：规则变复杂，判读也复杂。
- 我的建议：A，因为现有的救援规则已经覆盖这种情形，缺的只是脚本没执行到，不需要改规则。

## 技术细节

### 一、第 1 步：合修缮 14 `feat/fix@621b2d91`（完成，`d64c7777`）

- 预核（**已证实**）：`git merge-base HEAD 621b2d91` = `edfbb642`；`HEAD..621b2d91` 恰 2 笔 `621b2d91` / `9d871333`；`git diff --stat edfbb642 621b2d91` = `actions/build.js` +145 / `actions/move.js` +22 / `tests/actions.test.js` +131 / `tests/building-command-policy.test.js` +156（4 文件 +450/−4）。主干侧自 `edfbb642` 起动过 `actions/build.js`（`b058f9bb` 后勤 14 KEEP_YAW/LOOK_SYNCED + 导出）；`actions/move.js` 主干侧没动
- 合并（**已证实**）：`git merge 621b2d91` → `Merge made by the 'ort' strategy`，**零冲突**（任务书预判的导出表撞车没发生：两侧追加在导出表不同位置，git 自动并了）。合后 `module.exports` 核对：修缮 14 的 `temporaryReferenceClearReach` / `temporaryReferenceResidueLedger`（`actions/build.js:8277-8278`）、后勤 14 的 `placementLookAngles` / `sendPlacementLookPacket`、修缮 13 的 `hasClearScaffoldColumnBelow` / `selectTruncatedVerticalAccessPreviews` 都在；4 个文件 `grep '^<<<<<<<\|^>>>>>>>'` 0 行
- 测试（**已证实**）：`npm test` 63 组（`package.json` test 脚本 63 个 `node tests/…`）退出码 0；单跑 `building-command-policy` / `actions` / `oriented-placement-state` 各 `tests passed` 退出码 0
- grep 五处（**已证实**）：`temporaryReferenceClearReach` `actions/build.js:3853`（定义）/ `:3939` `:3957`（调用）；`[BUILD_TEMP_REFERENCE_CLEAR_UNREACHABLE]` `:3966`；`[BUILD_TEMP_REFERENCE_RESIDUE]` `:3804`；`[BUILD_TEMP_REFERENCE_CLEAR_APPROACH]` `:3959`；`temporaryReferenceResidueLedger` `:3882`；`isWithinPlacementReach` `actions/move.js:599`（定义）/ `:577`（在 `trackMovementScaffoldPlacements` `:562` 内调用）
- 主仓（**已证实**）：`git -C D:\code\MC merge --ff-only` `bbc56ecf → d64c7777`；`npm --prefix D:\code\MC test` 退出码 0（63 组）

### 二、第 2 步：#73 拆 `604,79,-11` 朝北活塞（完成）

服务器 pid 36712（`start-detached.ps1` + JDK21，`Done (2.718s)`），`forceload add 588 -26 611 5`（9 chunks）。**已证实**，全部 `execute if block` Test passed：

| 格 | 改前 | 动作 | 改后 |
|---|---|---|---|
| `604,79,-12` | air | — | air |
| `604,79,-11` | `piston[facing=north,extended=false]` | `setblock 604 79 -11 minecraft:air` → `Changed the block` | **air** |
| `604,79,-10` | `piston[facing=down,extended=false]` | — | `piston[facing=down,extended=false]`（未动） |
| `604,78,-11` / `604,78,-10` | air / air | — | air / air |
| `603,80,-10` | `redstone_wire[power=0]` | — | `redstone_wire[power=0]` |
| `603,80,-11` | — | — | air |
| `604,79,-9..-6` | 全部 `piston[facing=down,extended=false]` | — | `-9` 复查仍 `facing=down,extended=false` |

同时快照：`603,80,-5` air、`604,81,-10` air、`600,81,-2` air、`605,81,-10` stone_bricks、`605,82..83,-10` air。**只动了这一格。**

### 三、第 3 步 0.：回答修缮 14 的矛盾（`601,69..73,-10` / `608,71,-8`）

（**已证实**，引第 21 轮反馈"执行"表）第 21 轮收工清残留时，`601,69..73,-10` 5 格与 `608,69..71,-8` 3 格（含 `608,71,-8`）**逐格 `execute if block … minecraft:dirt` 全部 Test passed 之后才 `setblock … air`**，改后逐格 `execute if block … air` 复核 50/50 通过——即第 21 轮开工时这 6 格**确实是 dirt**，扫描的时点（第 21 轮开场、bot 未起）与坐标（逐格）都没有错。修缮 14 看到的第 20 轮 `[pickup-detect] itemPos=601,73,-10 / 602,71,-9 / 601,69,-10` 掉落物与"这 6 格被真拆"不等价：掉落物坐标只是物品实体的位置（会滚动、会从邻格落下），第 20 轮她在同一根柱子上反复"拆—重新垫—再拆"（第 21 轮反馈已指出 `600,67..74,-8` 是脱困柱重新垫的），掉落物多半来自邻格或同一柱子更早一次的拆除，之后又被垫回。**合理推断**：这 6 格是"拆过又垫回"，不是"没拆过"，也不是扫描错。转修缮：如需钉死，可对第 20 轮日志按这 6 格逐一排"最后一次 CLEAR 之后有没有 SCAFFOLD_PLACED/放置"的时间线（修缮 14 对 17 格用过这个筛法，这 6 格没过筛正说明最后一次 CLEAR 之后有重新放置或掉落物归属不明）。

### 四、第 3 步：续建真机（12:36:38Z–12:46:50Z 施工 10 分 12 秒，bot 未重启）

**ONLINE-GATE**（**已证实**）：bot pid 12308（父 18992 包装，`Get-NetTCPConnection` 只连 25565）；`[端口] 使用 .env 指定端口: 25565` 12:35:39.931Z、`[TickLoop] started` 12:35:41.877Z、`[Bot] 上线成功` 12:35:41.881Z、`[DEBUG_STATUS] {"botUsername":"LinXia",…}` 12:36:32.131Z（保姆脚本 `debug_status` 触发）。日志 `logs/bot-2026-09-05-05-35-39.log`（= `bot-current.log`，另存 `.tmp/cc-r23-bot-log-2026-09-05T1235Z.log`）。

**保姆脚本** `.tmp/cc-r23-fort-driver.js` = r22 + ①回声加 `TEMP_REFERENCE_CLEAR_UNREACHABLE|CLEAR_APPROACH|RESIDUE|DRIVER_ROOF_TP|DRIVER_BOT_RESTART|pickup-detect|ESCAPE_RUNG` 等；②#74：按 `BUILD_PLACE_REPOSITION_REJECT … reason=stand_move_timeout_blacklisted` 与任务失败原因 `move_timeout|placement_still_unreachable_after_xz_move|no_reachable_build_stance_after_*` 逐目标计数，≥2 且 `目标 y − 她 y ≥ 10` → tp 到该目标最近一次 `BUILD_VERTICAL_ACCESS_PLAN stand=`（先 RCON 核脚下实心、脚位/头位空气；不行换 `alternatives`；再不行按存档 verified 实心格离目标最近者的上方），任务进行中则先 `停止任务`；打 `[DRIVER_ROOF_TP] target= to= reason= source= heightDiff= herBefore=`；③#75：连续 2 次 `lock_already_held` → `停止任务` → 按「连 25565 + `bot.js`、排除自身 pid」杀 bot → `start-detached.ps1` 重起 → 等四信号 → `[DRIVER_BOT_RESTART]`；④三败/预算立刻 SHUTDOWN（`停止任务` → tp `599 68 -19` → 退出）；位置采样 5 s，y 骤降 ≥6 打 `ROOF_FALL`。中途 12:42 打过一处补丁并以 `--attach` 重起脚本（见"新现象 1"）。

**续建命中原 run + 开场三个数**（**已证实**）：
```
[12:36:41.178Z] [BUILD_CONSTRUCTION_RUN_RESUME_EVIDENCE] runId=construction_run_d2f3cbb572ceb6b7 sequence=52 before=1159/1864 after=1159/1864 processRestarted=true lanPort=25565 …
[12:36:41.230Z] [BUILD_CONSTRUCTION_RUN] mode=resume runId=construction_run_d2f3cbb572ceb6b7 verified=1159 pending=678 repair=0 stateRepair=27
```
verified 1159 / stateRepair 27 与预估一致；pending 678 = 预估 677 + 1（`604,81,-10` 的 terminal_failed 在续建对账时被复位成 pending 优先重放，存档 `terminal_failed` 0）——偏差 0。

**首撞 `604,81,-10 piston`——没用传送、46 秒放上**（**已证实**）：她在地面 `600.5,67,-16.5`（脚本开场 tp 到 `597,68,-22` 后自行走到 `598.5,70,-17.5`），候选 8 个 legal 6，`PLAN stand=604,80,-8 scaffoldCount=8 scaffolds=598,69..74,-18|598,75,-16|598,76,-16`：
```
[12:36:41.320Z] [BUILD_STEP] index=576 kind=place phase=frame pos=604,81,-10 block=piston
[12:36:49.214Z]…[12:37:20.714Z] [BUILD_VERTICAL_ACCESS_SCAFFOLD_PLACED] target=604,81,-10 position=598,69..73,-18 / 598,75,-16 block=dirt   （6 条，她随柱子从 y70 升到 y76）
[12:37:26.116Z] [BUILD_PLACE_ORIENT] target=604,81,-10 block=piston facing=down source=piston_facing look=0,0 pitch=1
[12:37:26.121Z] [BUILD_ORIENTED_PLACE_KEEP_YAW] target=604,81,-10 reference=605,81,-10 block=piston facing=down source=piston_facing sneak=false
[12:37:26.125Z] [BUILD_PLACE_LOOK_SYNCED] target=604,81,-10 block=piston facing=down sent=true pitch=1.571 yaw=-3.142
```
12:37:4x RCON `604 81 -10` = `piston[facing=down,extended=false]` Test passed。与第 22 轮同一块砖三败（每次 2.5 分钟、四行 partial ×100 片）的对照：**本轮 `PREVIEW_EXTENDED` 0 次、`preview=partial previewSlices=100` 0 次、`clearColumn` 0 次**——她起步点不同（第 22 轮从 `600.5,67,-5` 掉落点起、本轮从工地西侧 `598,70,-17` 起），候选算出 legal 6 直接走脚手架方案。修缮 13 的补算边界本轮没有被触发。

**修缮 14 判据**（**已证实**，整轮）：

| 行 | 条数 | 明细 |
|---|---|---|
| `SCAFFOLD_PLACED`（≤6 格内语义） | 8 | `598,69..73,-18`（5）、`598,75,-16`、`601,82,-10`（dirt）、`600,82,-10`（dark_oak_planks） |
| `BUILD_STAIR_TEMP_REFERENCE_CLEAR`（真拆） | 3 | `598,75,-16`（attempts=1 stableMs=365，前有 `[pickup-detect] itemPos=598,76,-15`）、`600,82,-10`、`601,82,-10` |
| `CLEAR_APPROACH result=moved` | 2 | `598,75,-16` distance=4.59 inReach=true（耗时 1.4 s）；`600,82,-10` distance=3.95 inReach=true（4.2 s） |
| `CLEAR_APPROACH result=move_timeout` | 5 | `598,73/72/71/70/69,-18` distance=5.91/6.80/7.72/8.65/9.60 inReach=false，**每条恰 8.0 s**（moveTo 8 s 预算） |
| `CLEAR_UNREACHABLE reason=out_of_reach` | 5 | 同上 5 格，`reach=5.1 clientSays=dirt` |
| `CLEAR_UNREACHABLE reason=unverified_client_air` | **0** | 没有出现"被骗过的格子再被看成空气" |
| `TEMP_REFERENCE_RESIDUE` | 1 | `left=5/6 positions=598,73,-18|598,72,-18|598,71,-18|598,70,-18|598,69,-18 maxDistance=9.60 reason=out_of_reach` |
| `BUILD_VERTICAL_ACCESS_CLEANUP` | 2 | — |

走位层代价：5 条 `move_timeout` 共 40 s（她站在 `600.5,77,-15.4` 的屋檐上，柱子在 `598,y,-18`，路径 `noPath allowList=0 excl=0`——从屋檐下不去）；**没有因清理走动掉下屋顶**（位置采样 12:37:29–12:38:14 全程 `600.49,77.00,-15.39`，`ROOF_FALL` 由脚本打的 2 条都是脚本自己的 tp，见下）。

**收工判「治好了」**（**已证实**）：按 8 个 `SCAFFOLD_PLACED` 格 + 5 个 `RESIDUE` 格（重合）RCON `execute if block … dirt` 扫描 8 格：dirt 5 格 = **全部在 RESIDUE 名单内**（`598,69..73,-18`）；名单外 3 格（`598,75,-16` / `600,82,-10` / `601,82,-10`）全部非 dirt（air）。**dirt 只出现在名单里 → 修缮 14 在门楼真身成立**（样本小：8 格）。清残留按 #65：5 格逐格 `execute if block … dirt` Test passed → `setblock … air` → 复核 5/5 air（另核 `598,74,-18` / `598,76,-16` air）。**三个数：查 8 / 删 5 / 跳过 0**。5 格全在 bounds 外、非图纸格。

**#74 传送记账**（**已证实**，2 次，另 1 次按高度跳过）：

| # | 目标 | 之前站位超时 | 她在 | 传到 | 来源 | 传送后 |
|---|---|---|---|---|---|---|
| 1 | `603,82,-18 chiseled_deepslate` | 2（`REJECT stand=602,83,-18 stand_move_timeout_blacklisted` 12:40:51 + 任务失败 `placement_still_unreachable_after_xz_move` 12:40:53；此前 12:38:58 还有一次 `no_reachable_build_stance` 也算在内，共 3） | `601.6,67.0,-18.4`（地面，heightDiff 15） | `602.5,83,-17.5`（站 `602,82,-18` dark_oak_log 上，verified） | verified_roof（PLAN 无 stand 记录：候选 total=1 noPath） | 12:40:59 续建 → 12:41:36 `TARGET_ADVANCED` → 存档 verified、RCON chiseled_deepslate ✔（放置耗时含开工 37 s） |
| 2 | `600,82,-14 chiseled_deepslate` | 2（`REJECT stand=600,83,-12` ×2，12:42:51 / 12:43:43） | `600.3,67.0,-11.5`（地面，heightDiff 15） | `600.5,83,-14.5`（站 `600,82,-15` 上，她本轮刚放的 chiseled_deepslate） | verified_roof | `停止任务`→tp→12:43:49 续建 → 12:45:26 `TARGET_ADVANCED prevDurationS=4` → verified、RCON ✔ |
| — | `605,82,-18` | 2（`no_reachable_build_stance` ×2） | `600.5,83.0,-15.4`（**已在屋顶**，heightDiff −1） | `ROOF_TP_SKIPPED heightDiff=-1.0 (<10)` | — | 第三次同因失败 → 三败停手 |

**传送后盖的砖**：`603,82,-18`、`600,82,-14`（2 步）。传送 #2 之后她**未再离开屋顶**，其后 `604,82,-18`、`600,82,-13` 也是在屋顶盖的（不算"传送后"那一步，但站位来自传送）。传送后放置行（**已证实**）：`600,82,-14`：`BUILD_STANCE_CANDIDATES total=3 attempts=600,83,-15|600,83,-14|600,83,-12` → 在 `600,83,-15.3` 原地放上，无 `REJECT`；`604,82,-18`：`REJECT stand=602,83,-18 stand_move_timeout_blacklisted` → `[BUILD_MOVE_TIMEOUT_REACH_RECOVERED] target=604,82,-18 reason=partial_move_reference_reachable` → 放上（站 `600.5,83,-15.36`，眼到目标中心 ≈5.0 格）。三行 `PLACE_ORIENT/KEEP_YAW/LOOK_SYNCED` 只有活塞打（6 组，见下），深板岩无朝向不打。

**#75 锁占死兜底**（**已证实**）：`[task-manager] fail … reason=lock_already_held` **0 次**；`DRIVER_BOT_RESTART` 0 次；`result=lock_already_held` 整轮 1 行且是 `owner=task_manager_status_inventory`（`debug_status` 查询与在跑任务 #3 抢 inventory 锁，非病因）；`enqueue task=return_to_base` **0 次**（整轮 `WORKSITE_ANCHOR_HOLD` 184 行 `priority=TOO_FAR_FROM_BASE|NIGHT_UNSAFE worksiteDistance≤17.8 ageMs≤64623`，锚点一直在刷新）。修缮 15 要的"锁占死前失败→回家间隔"本轮无样本。

**三败停手 `605,82,-18`——新现象 2（钉在墙顶零候选）**（**已证实**）：她从传送 #2 起一直站在 `600.5,83.0,-15.36`（`600,82,-15` 顶上，一格宽的城垛顶），对 `605,82,-18`（东 5 格、同高）三次任务（#4 / #5 / #6，12:45:41 / 12:46:14 / 12:46:47，间隔 33 s）日志同型：
```
[12:45:41.301Z] [BUILD_STEP] index=611 kind=place phase=frame pos=605,82,-18 block=chiseled_deepslate
[12:45:41.319Z] [BUILD_VERTICAL_ACCESS_CANDIDATE] target=605,82,-18 id=vertical_0 stand=603,83,-18 endpoint=none preview=noPath … pathNodes=0 rejection=preview_noPath   （vertical_1 604,83,-18 / vertical_2 606,83,-18 同）
[12:45:41.344Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=605,82,-18 total=3 previewed=3 extended=0 legal=0 noPath=3 …
[12:45:41.347Z] [BUILD_VERTICAL_ACCESS_CANDIDATE_FALLBACK] target=605,82,-18 restricted=3 reason=restricted_candidates_exhausted
[12:45:41.578Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=605,82,-18 total=41 previewed=41 extended=0 legal=0 noPath=41 …
[12:45:41.867Z] [Task:build_blueprint#4] fail: no_reachable_build_stance_after_vertical_access_candidates
```
同一站点上 12:38:58 对 `603,82,-18` 第一次也是 `total=1/41 noPath` 全灭（她当时在 `600.48,83,-11.58`，也在城垛顶）。即：**从城垛顶出发，预演 A* 对任何站位都 noPath**（`MOVE_PATH_STATUS status=noPath goal=602,83,-18 allowList=0 excl=1233` 同型 3 行），但同一站点靠"够得着就原地放"仍能放 `600,82,-14` / `604,82,-18` / `600,82,-13`。原因 UNKNOWN（合理推断：城垛顶 1 格宽、两侧是 1 格落差 + 空气，A* 起点/落脚判定把它当不可走；`allowList=0` 说明这些步的走位不带脚手架方案）。存档：`605,82,-18 pending retry.count=3 lastError=no_reachable_build_stance_after_vertical_access_candidates`（三败后我 `停止任务` 前任务 #7 已开始对账，把 terminal_failed 复位成 pending，与 `604,81,-10` 本轮开场同型）。

**为什么脚本没救**：三次失败间隔 33 s，脚本在"#74 因高度跳过"的分支里没有走 r22 的 `pinned → 停止任务 → rescue_tp_site` 检查（代码顺序问题：`else if (pendingTp)` 先于 `pinned` 分支），第三次失败到达即 `GIVE_UP`。我在 12:46:5x 手动 tp 她到 `597.5,68,-22.5`（记 `.tmp/cc-r23-manual-actions.log`），但脚本已在 12:46:50.9 停手并 tp 她回 `599,68,-19`，手动 tp 对施工无影响。**另一个脚本副作用**：传送 #2 时我先 `停止任务` 再 tp 再续建（任务 #4），脚本随后又把 12:43:45 已发生的任务 #3 失败当新终态多发了一次续建，排队生成任务 #5——所以 #4 失败 0.4 s 后 #5 立即开工、#5 失败后 #6 由脚本再续，**三次尝试之间没有任何脚本介入窗口**。这两处都是脚本问题，已定位，下轮改（问题 1 选 A 则只改脚本）。

**新现象 1（每次开工先去材料箱、从屋顶算不到箱子）**（**已证实**）：每个续建任务开工都先做补料（`CHEST_SCAN_START` → `CHEST_PATH_ESCAPE_ATTEMPT waypoint=600.5,82,-13.5 reason=direct_move_timeout` → `CHEST_PATH_ESCAPE_REACHED` → `MOVE_PATH_STATUS status=noPath goal=596,67,-23 allowList=0 excl=0` → `CHEST_PATH_ESCAPE_RETRY_FAILED … timeoutMs=29080` → `CHEST_PATH_UNREACHABLE`），两只箱子各 ~30 s，整轮 `CHEST_PATH_UNREACHABLE` 10 次 / `CHEST_PATH_ESCAPE_*` 21 行 / `noPath allowList=0 excl=0` 20 行几乎全是它；补料失败后任务照常开工（本轮 `BLOCKED_MATERIAL_SHORTAGE` 0，`give` 0）。r22 脚本的"90 s 内 2 次箱子不可达 → tp 到箱子旁"规则在她**在屋顶时**把她传下地面（12:40:37、12:41:36 两次，`ROOF_FALL` 2 条都是这个 tp），下一步又要靠 #74 传回来（传送 #1 就是这样触发的）。12:42:10 我给脚本加了"她 y≥78 时跳过箱子救援"（打 `CHEST_RESCUE_SKIPPED_ON_ROOF`，之后 2 次）并 `--attach` 重起脚本（状态清零：站位计数从头数，传送账在两段日志里）。**交修缮/开发**：补料是不是可以只在真缺料时去、或者从屋顶算箱子路时带脚手架方案。

**其它记数**（**已证实**）：`LOOK_SYNCED sent=true` 6 / `sent=false` 0（`604,81,-10/-9/-8/-7/-7/-6`，`-7` 第一次 `BUILD_PLACE_RETRY attempt=2 reason=place_failed:unstable_air` 后重放成功）；`state_mismatch` 0（含 facing）；`REPEATER_DELAY_TUNE` / `COMPARATOR_MODE_TUNE` / `POWER_SOURCE` 0；供电源步 0（18 个 `BUILD_STEP` 里无火把/按钮/拉杆）；`BUILD_STATE_REPAIR` 0，state_repair 27 → 27（名单未动）；`stand_move_timeout_blacklisted` 5；`MOVE_PATH_STATUS` 25（`noPath allowList=0 excl=0` 20、`noPath allowList=0 excl=1233` 3、`timeout allowList=0 excl=1233` 2）；`EVENT_LOOP_STALL` 0；`destination full` / `chest_item_not_found` / `protected_building_dig_blocked` / `lost connection` 0；`clear_obstruction` 只出现在 `BUILD_PIPELINE` stages 列表里（非事件）；血量整轮 20.0，无摔落。

**三个数改前 / 改后**（**已证实**，存档 `reconciliation.at=12:47:08Z`）：verified 1159 → **1171**（+12：`604,81,-10/-9/-8/-7/-6` piston ×5、`600,81,-2` / `608,81,-2` iron_bars、`600,82,-15` / `603,82,-18`* / `600,82,-14`* / `604,82,-18` / `600,82,-13` chiseled_deepslate ×5；*= 传送后盖的 2 步）；pending 678 → 666；state_repair 27 → 27；terminal_failed 0 → 0（`605,82,-18` pending retry 3）。run `ACTIVE`。**下次首撞**：`605,82,-18 chiseled_deepslate`（retry 3 优先重放）；两盏 y82 灯笼 `604,82,-1` / `604,82,-15` 仍 pending 未到。12 块全部 RCON Test passed（活塞 `facing=down,extended=false`）。

**收工**（**已证实**）：12:46:50 脚本 `GIVE_UP` → `停止任务` → tp `599 68 -19`（`FINAL_POS 599.5,67,-18.5`）→ 12:47:1x `Stop-Process` 本线 bot（pid 12308，`Get-NetTCPConnection RemotePort=25565` + `CommandLine =~ bot.js`，排除自身；同时段无 25566 连接、修缮线无进程在线）→ `list` 0 人 → 残留清理 5 格 → `forceload remove all`（复查 0 chunks）→ `doDaylightCycle` true（本轮未改任何 gamerule / time）→ `save-all flush` → `mc-server.ps1 stop` 经 RCON → `All dimensions are saved` / `RCON Listener stopped` → 25565/25575 无监听、`server.pid` 已删、本线 node/java 进程零（唯一剩余 node 是 Codex 自带的 `cua_node … server.mjs`，非本项目）。停工到收 bot 约 20 s。真机累计 10 分 12 秒。

### 五、第 4 步：判定表

| 现象 | 本轮 | 去向 |
|---|---|---|
| RESIDUE 名单外 `SCAFFOLD_PLACED` 格全 air | **是**（名单外 3/3 air，名单内 5/5 dirt） | **修缮 14 在门楼真身成立**（样本 8 格） |
| 名单外仍 dirt | 0 | — |
| `CLEAR_APPROACH` 后掉屋顶 | 0（`ROOF_FALL` 2 条均为脚本 tp） | — |
| `unverified_client_air` | 0 | — |
| `DRIVER_ROOF_TP` 后砖放上 | 2/2（`603,82,-18`、`600,82,-14`，各 4 s 内） | **#74 让屋顶推进**——记账 |
| 传送后仍站位超时 | 0 | — |
| `lock_already_held` → 重启 | 0 / 0 | 无样本 |
| 火把最末放后活塞再顶出 | 没盖到 | — |
| 开场三个数偏差 >3 | 0（pending +1 是 terminal_failed 复位） | — |
| （表外）城垛顶零候选三败 | `605,82,-18` ×3，33 s 一次，原地未动 | 老板问题 1 + 交修缮（noPath 起点判定） |
| （表外）每次开工先去箱子、屋顶算不到箱子 | 10 次不可达、20 行 noPath、脚本误传下地 2 次 | 交修缮/开发 |
| （表外）`604,81,-10` 不传送 46 s 放上 | 起步点不同、候选 legal 6 | 记数（修缮 13 边界未触发） |

### 六、第 5 步：文档

- 第 22 轮反馈整份挪到 `docs/协作/历史/第22轮-合后勤14与视线顺序成立与推一排.md`；`说明.md` 归档表补第 22 轮一行（开发同时改了 `说明.md` 的模型分档段 #77，一并提交）
- 台账 #73–#77 开发已追加，随本反馈一笔提交；他线文件显式提交：`docs/协作/后勤/给CC.md`（后勤 15 任务书）、`docs/协作/后勤/CC反馈.md`（后勤 15 反馈，后勤线写的）、`docs/协作/历史/修缮第14轮-隔空拆根治与假成功归零.md`、`docs/协作/历史/后勤第14轮-视线同发与供电源最后放.md`、`docs/协作/建造/给CC.md`（本轮任务书）
- 主仓 main 提交后再快进一次

## 交接

**给修缮**：① 隔空拆定论：5 格 `out_of_reach` 老实认账 + RESIDUE 账准确 + 名单外 0 残留 + `unverified_client_air` 0——**成立**；走位层 5 条 `move_timeout` 各 8.0 s、`moved` 2/7；② 城垛顶零候选（上段日志，起点 `600.5,83,-15.36` 站 `600,82,-15` 上，`total=3→41 noPath` 全灭，但原地够得着就能放）；③ 每次开工先补料、屋顶算箱子 `noPath allowList=0 excl=0`（不带脚手架方案）；④ 锁占死 / 回家间隔本轮无样本（`return_to_base` 0，锚点 184 次 HOLD 都在刷新）。

**给后勤**：活塞三行 6/6 `sent=true`、mismatch 0；供电源步 0/18；火把段没到。

**世界现状**：屋顶 `604,79,-10..-6` 五只活塞朝下未伸出；`604,81,-10..-6` 五只活塞朝下未伸出（本轮新）；`604,79,-11` **air**（#73 已拆）；`603,80,-5` air（火把最末）；`605,82,-18` air（pending retry 3）；y82 城垛 `600,82,-13..-15`、`603/604,82,-18` chiseled_deepslate（本轮新）；残留土 0（8 格扫描后清 5）；她登出点 `599.5,67,-18.5`；昼夜规则与 forceload 恢复原状。

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`docs/协作/说明.md`（含开发本轮新增 #77 段）、`AGENTS.md`、`docs/协作/建造/给CC.md`（第 23 轮）、`docs/协作/建造/CC反馈.md`（第 22 轮）、`docs/协作/历史/第21轮-合修缮13与清残留与拔火把.md`（执行表）、`docs/协作/历史/修缮第14轮-隔空拆根治与假成功归零.md`（矛盾段 L90/L192/L233）、`docs/协作/后勤/CC反馈.md`（头 6 行）、`docs/协作/老板决策台账.md`（diff）、`actions/build.js`（导出表、L3804–3966 grep）、`actions/move.js`（L562–599）、`package.json`（test 脚本）、`scripts/mc-server.ps1`、`scripts/start-detached.ps1`、`scripts/mc-rcon.js`、`data/memory/construction-runs.json`（只读 ×4）、`.tmp/cc-r22-fort-driver.js`、`.tmp/cc-r22-bot-log-*.log`（行格式取样）、`logs/bot-current.log`、`.tmp/cc-r23-driver-1/2.log`、`D:\code\MC-server\server.properties`（端口/密码键，密码只进进程环境）、`D:\code\MC-server\logs\latest.log`（尾部）、`.env`（只列键名）

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `d64c7777` | 合并 `feat/fix@621b2d91`（零冲突，4 文件 +450/−4，含修缮 14 两笔） |
| （随后一笔） | 本反馈、`docs/协作/说明.md`（+1 行归档 + 开发的 #77 段）、`docs/协作/历史/第22轮-合后勤14与视线顺序成立与推一排.md`（新）、`docs/协作/历史/修缮第14轮-*.md`（新，他线）、`docs/协作/历史/后勤第14轮-*.md`（新，他线）、`docs/协作/老板决策台账.md`、`docs/协作/后勤/给CC.md`、`docs/协作/后勤/CC反馈.md`、`docs/协作/建造/给CC.md` |
| （主仓） | `D:\code\MC` main 快进 `bbc56ecf → d64c7777`（本反馈提交后再快进一次） |
| （未入库） | `.tmp/cc-r23-fort-driver.js`、`.tmp/cc-r23-driver-1/2.log`（utf8）、`.tmp/cc-r23-driver-1/2.ps.log`（UTF-16）、`.tmp/cc-r23-bot-stdout-1.log`、`.tmp/cc-r23-bot-log-2026-09-05T1235Z.log`、`.tmp/cc-r23-server-start.log`、`.tmp/cc-r23-manual-actions.log` |
| （世界） | `604,79,-11` 活塞拆除（#73）；门楼 +12 步 verified；残留土清 5 格；其余未动 |

未改任何项目 JS 逻辑（合并带进来的除外；保姆脚本在 `.tmp/`）；未手改 `construction-runs.json`；未代放砖；未拆任何图纸格。

### 3. 执行的命令

`git status/log/merge-base/diff/merge 621b2d91`（零冲突）、`git -C D:\code\MC merge --ff-only`、`npm test` ×2（开场 @bbc56ecf、合后 @d64c7777，均 63 组）、`npm --prefix D:\code\MC test` ×1、单跑 3 组、`node --check` 脚本、`node -e` 只读解析存档 ×4 / 日志统计 ×1 / 残留扫描 ×1；`grep -n` 只读核对；`cp` 归档；`scripts\start-detached.ps1` ×4（服务器 1、bot 1、保姆脚本 2）、`scripts\mc-server.ps1 status/stop`、`Stop-Process` ×2（保姆脚本 36248 换补丁版；bot 12308，按端口 25565 + `bot.js` 核对、排除自身）、`Get-CimInstance` / `Get-NetTCPConnection`；RCON（scratchpad 单连接多命令包装 `rcm.js`，密码运行时从 `server.properties` 读）：`list` ×4、`time query` ×1、`forceload add/query/remove all`、`gamerule doDaylightCycle`（只查）、`execute if block` ×≈60（快照 ~25、施工中核对 ~15、残留扫描 8 + 前置 5 + 复核 7）、`setblock … air` ×6（#73 1 格 + 残留 5 格）、`data get entity LinXia Pos` ×3、`tp LinXia` ×1（手动救援，三败后、无效）、`save-all flush`；保姆脚本自身：`tp LinXia` ×6（`initial_park` 1、`chest_rescue_tp` 2、`DRIVER_ROOF_TP` 2、`stop_tp_worksite` 1）、`give` 0、`bot.chat` `debug_status` ×2 / `停止任务` ×3 / `build fort wall gate` ×7（均按两段脚本日志与 bot 日志 `CHAT_INPUT` 计数核对）。
未执行：`npm install/ci/update`、`git add -A/.`、`git reset/clean/checkout --`、`git stash`、进 `MC-fix`/`MC-ops`、连 25566、改项目 JS、手改存档、代放砖、拆图纸格、改 gamerule/time。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 @bbc56ecf | `npm test` 63 组 | 退出码 0 |
| 合修缮 14 后 @d64c7777 | `npm test` 63 组 + 单跑 3 组 | 均退出码 0 |
| 主仓 @d64c7777 | `npm --prefix D:\code\MC test` 63 组 | 退出码 0 |
| 真机 | 隔空拆：名单外 0 残留 PASS；#74 传送 2/2 砖放上 PASS；活塞三行 6/6 PASS；三个数偏差 0；三败停手 `605,82,-18`（城垛顶零候选）；锁占死无样本 |

### 5. 当前最新判定

- 第 1 步 合修缮 14：**完成**（`d64c7777`，零冲突，五处 grep 齐，63 组全绿，主仓快进 + 全绿）
- 第 2 步 #73：**完成**（快照前后表见上，只动一格）
- 第 3 步：矛盾回答**完成**；续建命中、首撞 46 s 放上、修缮 14 四种行计数 + 收工扫描"dirt 只在名单内"**完成**；传送账 2 笔**完成**；重启账 0（无样本）；三线记数**完成**；清残留 8/5/0**完成**；**真机按三败自停于 10 分钟**（未到 3 小时）
- 第 4 步 判定表：**完成**
- 第 5 步 文档：**完成**（本笔）

### 6. 生成文件 / 内存是否变脏

`acceptance/reports/latest-*`、`data/memory/task-memory.json`、`world-memory.json`：`npm test` 历史脏状态，未提交；`data/memory/construction-runs.json`：本轮真机更新（verified 1171 / pending 666 / state_repair 27），未提交、未手改；`logs/`：本轮一段日志（另存 `.tmp/`）；`.tmp/*`、`_backups/`、`probe/`、`acceptance/reports/building-case1-*`、`data/community-builds/cache/*` 他线或往轮产物，未动。

### 7. 是否建议提交

已提交合并一笔；本反馈 + 文档 + 他线文件随后一笔；主仓再快进一次。`data/memory/*`、`logs/*`、`.tmp/*` 不提交。

### 8. 剩余风险

- `605,82,-18` retry 3 待重放，下轮首撞；她若又站在城垛顶上，零候选会重演——脚本要补"钉在原地 → 救援传送"兜底（问题 1 选 A 即做），根因（A* 从城垛顶起点 noPath）在修缮线
- 每个续建任务先去箱子补料、从屋顶 ~60 s 白等；脚本已改成屋顶不救援，但补料本身的 60 s 每次都在
- 保姆脚本"停止任务后又把旧失败当新终态多发一次续建"的排队副作用要修（否则三败之间没有介入窗口）
- 修缮 14 本轮样本只有 8 格（5 残留 + 3 真拆），"治好了"是小样本结论；`unverified_client_air` 0 也是小样本
- 锁占死本轮没触发，#75 兜底逻辑（杀 bot / 重起 / 四信号）**未经真机验证**
- 门楼没盖到放火把那一步，"火把最后放后活塞是否再顶出"仍未验
