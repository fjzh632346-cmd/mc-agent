# 优先级线 协议与任务书（退役存档）

> 存档说明：以下两份原为 `D:\code\MC-priority\docs\协作\` 下的**未跟踪**文件，
> 只存在于那个 worktree 的磁盘上，git 里没有任何副本。
> 2026-08-30 修缮线第 1 轮摘除该 worktree 前，按任务书要求全文抄存于此。
> 优先级线已完成使命，本文只作历史查阅，不再执行。

---

# 附一：`D:\code\MC-priority\docs\协作\说明.md`

# 协作协议 · 优先级线（固定，不要改动本文件的结构）

**你在 `D:\code\MC-priority`，分支 `feat/priority`。这是优先级线。**

本项目有三方参与：

| 角色 | 是谁 | 干什么 |
|---|---|---|
| **老板** | 项目所有者 | 拍板决策。不看代码，只看白话结论 |
| **开发** | Cowork 里的 Claude | 出任务、审报告、翻译成白话给老板 |
| **施工** | 你（Claude Code） | 在本机读写代码、跑测试 |

老板**不再手工搬运任何内容**。三方全部通过这个目录里的两个文件交换信息。

---

## 你每次会话必须做的事

### 1. 开场

```powershell
cd D:\code\MC-priority
```

按顺序读（**全部在本 worktree 内，不要去别的目录**）：

```text
AGENTS.md            项目铁律，永远最高优先级
docs\协作\给CC.md     本轮要你干什么
docs\协作\CC反馈.md   上一轮你自己写的结果，看有没有未了结的
```

然后跑：

```powershell
git status --short
git log --oneline -10
git branch --show-current
npm test
```

### 2. 干活

严格按 `给CC.md` 的范围。它没写的不要做。

### 3. 收尾（这一步不能省）

把结果写进 `docs\协作\CC反馈.md`，格式见下面第 3 节。

> **不要发链接。不要只在聊天里回复。不要新建别的报告文件。**
> 老板看不到你的聊天窗口，开发只读这一个文件。
> 写进文件了才算交付；没写就等于没做。

写之前：如果反馈文件里还有上一轮的内容，先整份挪到
`docs\协作\历史\第N轮-简短标题.md`，再重写。**历史目录只增不删。**

---

## 另一条线的存在（你需要知道，但不要碰）

主 worktree `D:\code\MC-blueprint-ir-v1` 上有一条**建造线**在并行推进，
它有自己的一套 `docs\协作\`，跟你这套互不干扰。

**硬规矩：**

- **不要 `cd` 到主 worktree**，不要读写那边的任何文件
- **不要同时连游戏**。建造线在用专用服务器（`D:\code\MC-server`），
  两边同时连会互相污染，跑出来的报告谁的都不算数。
  需要进游戏时，先在反馈里写明「需要用游戏」，由开发协调档期
- **不要跑 `npm install` / `npm ci` / `npm update`**。
  两个 worktree 的 `node_modules` 是同一份（junction），
  你这边装包会写穿到主 worktree 正在用的依赖上
- 两边的分支最终要合并，但**合并不是你的活**，会单独开一轮

---

## 文件职责（文件名固定，永远不要改名）

```text
D:\code\MC-priority\docs\协作\
├── 说明.md        本文件。协议本身。除非老板要求，不要动
├── 给CC.md        开发 → 你。当前轮的任务与已拍板的决策
├── CC反馈.md      你 → 开发。当前轮的结果。每轮覆盖重写
└── 历史\          归档。只增不删
```

---

## `CC反馈.md` 的固定格式

**必须**按这个结构写，开发按固定位置读取。

````markdown
# 优先级 第 N 轮反馈

- 线: 优先级
- 轮次: N
- 日期: YYYY-MM-DD
- 状态: 进行中 | 已完成 | 卡住了 | 等老板拍板
- 分支 / HEAD: feat/priority / xxxxxxxx

---

## 一句话结论

（一句话。让一个不看代码的人知道这轮成没成。）

## 白话摘要

（3-6 句，写给不懂技术的老板看。禁止出现文件名、函数名、行号、英文术语。
说清楚：做了什么、结果怎样、下一步该干嘛。）

## 需要老板拍板的问题

（没有就写"无"。有的话每条按这个格式：）

### 问题 1：<一句话问题>
- 背景：（白话，两三句）
- 选项 A：…… （代价 / 风险）
- 选项 B：…… （代价 / 风险）
- 我的建议：A，因为 ……

## 技术细节

（写给开发看。file:line、日志片段、命令输出都放这里。
每条结论标注 **已证实 / 合理推断 / UNKNOWN**。）

## 按 AGENTS.md 第 11 节

1. 读过的文件
2. 改动的文件
3. 执行的命令
4. 测试结果
5. 当前最新判定
6. 生成文件 / 内存是否变脏
7. 是否建议提交
8. 剩余风险
````

### 三条硬规矩

1. **「一句话结论」和「白话摘要」里不许出现文件名、函数名、行号、英文术语。**
   这两段是给老板看的。技术内容一律放「技术细节」。
2. **每条结论必须标 已证实 / 合理推断 / UNKNOWN。**
   没证据就写 UNKNOWN，不要用「应该」「大概」「估计已修复」蒙混。
3. **卡住了就把状态写成「卡住了」并说清卡在哪。**
   不要为了让报告好看而把 FAIL 写成 PASS，不要隐藏 BLOCKED。

---

## 常驻红线（每轮都生效）

- 一轮只打一个根因；最小补丁；改代码必须配单测
- 不降低验收标准；不手改报告把 FAIL 变 PASS；不隐藏 BLOCKED
- 不重构 TaskManager；不做 `AGENTS.md` 第 10 节列的大改造
- 禁止 `git add -A` / `git add .` / `git reset --hard` / `git clean -fd` / `git stash` / `git checkout --`
- 提交必须显式指定文件；提交到 `feat/priority`，**不要合并回 main**
- 工作区脏的话，**不要顺手清理或丢弃别人的改动**
- 不要 cd 到主 worktree；不要同时连游戏；不要装包（见上面「另一条线」）
- 不确定要不要做的事，写进「需要老板拍板的问题」，不要自己决定

---

## 已知问题（背景，不用你现在处理）

- **`npm test` 在本 worktree 是 34 通过 / 4 失败，且链在第 4 组就断。**
  第 1 轮已证实这 4 组在改动之前就是红的。
  合理推断：主 worktree 有一批未提交改动是测试通过所必需的，本 worktree 缺了它们。
  主线正在专门清理那批改动。**在结论出来之前，`npm test` 全量结果不能作为验收依据**，
  请逐组单跑并说明。

---

## 历史归档

| 轮次 | 内容 | 位置 |
|---|---|---|
| 第 1 轮 | 优先级设计四处修正（EMERGENCY 档 / 传 reason / 战斗中保命打断 / 睡觉降档），提交 `0d013833` | `docs\协作\历史\` |


---

# 附二：`D:\code\MC-priority\docs\协作\给CC.md`

# 优先级线 · 本轮暂停

第 2 轮已完成并验收通过（见本目录 `CC反馈.md`）。老板已裁决：
**下一轮把这条线合并回主干，合并在主仓 worktree 里做。**

## 如果你是在 `D:\code\MC-priority` 里开的会话

**本轮这边不开工。** 不要在这个 worktree 里改代码、提交或跑测试。
去 `D:\code\MC-blueprint-ir-v1`，读 `docs\协作\建造\给CC.md`（第 5 轮任务书）。

## 第 2 轮遗留的三个问题，已有结论

| 问题 | 裁决 |
|---|---|
| 1. 日常测试清单里 4 组一直红 | **不用单开一轮修。** 开发推断建造线的清账已经把它们修好了，第 5 轮合并后逐组重跑证实或证伪 |
| 2. 仲裁测试不在日常清单里 | **批准加进去**，第 5 轮一并做 |
| 3. 战斗任务另开一张清单（偏离任务书那处） | **保持你的做法。** 理由成立：塞进现成那张会导致危险模式下拒接战斗任务，最需要打的时候反而打不了 |

真机手感（打起来会不会吃、会不会跑、撤退被追上会不会原地打转）第 5 轮合并后一起验。

（2026-08-31 开发下发）
