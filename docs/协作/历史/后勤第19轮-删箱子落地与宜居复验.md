# 后勤 第 19 轮反馈

- 线: 后勤
- 轮次: 19
- 日期: 2026-09-06
- 状态: 已完成（1–7 全做完；宜居硬闸全项通过，塔楼小筑离线到此清账）
- 实际运行模型 / 思考强度 / 是否 ultracode: **Opus 5 / UNKNOWN / 未开 ultracode**（任务书建议 `Sonnet 5` + `medium`，**实际起的是 Opus 5**——会话延续自上一轮，未切档；ultracode 按任务书「不开」执行）
- 分支 / HEAD: feat/ops / `c4c1919e`（本轮 2 笔：`c42649a3` 删箱子 + 测试期望值、`c4c1919e` 看板；main 未动、本轮无需接主干）

---

## 一句话结论

按您的拍板（决策 #94）把塔楼三层那只够不着的箱子删掉了——**只删了这一块**，删完「房子得能住人」那道硬检查**全项通过、一条不挂**，其余二十份图纸的施工单一个字节没变，测试全绿；这份中等民居离线到此清账，可以交真机了。

## 白话摘要

您选的 A：塔楼三层那只人够不着的箱子直接删掉。我只动了这一块，其余每一块砖的位置和材质都跟原稿逐字一致，主仓里您拍板的那份设计稿原件也一个字没碰。

删完复验：房子现在 **715 块**（特意留空的 13 格照旧），那道「盖出来得能住人」的硬检查**全项通过**——门、外壳不漏、屋顶盖满、三层楼、上下楼的路通、每个房间到得了、每件家具都有人站得到的位置，全部照旧；室内可用空间反而从 181 格涨到 183 格，因为那个角落空出来了。档位仍是 L4，六种说法照旧点得到它。材料只变一样：箱子每栋从六只减成五只，其他十八种一件不差。

因为少了一块砖，施工单的编号自然要换新，两个新编号都记在下面给建造线。其余二十份图纸的编号一个都没变，测试六十五组全绿。

**这份图纸离线该验的全验完了，可以交给带游戏的线连盖三次。** 只剩一件真机才定得了的事：图纸上写着「不搭脚手架」，可它十七格高，塔尖和烟囱顶都在十三格以上——建议第一次盖的时候盯着，够不着再回来改这一句（这是上一轮就报备过的第二个拍板问题，您还没定，我照旧没动）。

## 需要老板拍板的问题

无新增。

（上一轮的拍板问题 2「十七格高却写着不搭脚手架」仍然挂着，我按上轮的建议维持原样、交真机说话；您若想现在就改，一句话的事。）

## 技术细节

### 〇、开场（已证实）

- MC-ops 干净（除既有未跟踪缓存），`git log main --oneline -3` = `af27dae8 …`，**main 未前进**，本轮无需 `git merge main`
- 基线 `npm test`：**退出码 0，65 组 / 87 条通过行**（`.tmp/ops-round19-baseline.txt`）
- **未进游戏**；未跑任何社区同步；未 `npm install|ci|update`；未联网；未碰主仓 `data/memory/`；未进 `D:\code\MC` / `MC-fix` / `MC-priority`

### 一、任务 1：删箱子（提交 `c42649a3`）

**改动就是一处，8 行**，删掉的条目原文：

```json
  {
   "x": 11,
   "y": 9,
   "z": 1,
   "type": "chest",
   "phase": "functional_blocks",
   "role": "chest"
  },
```

**这是老板拍板后的改动（决策 #94），不是本轮自行变更外形。**

改动面逐格核对（**已证实**，脚本按坐标建表比对）：

| 比对基准 | 删 | 增 | 改 |
|---|---|---|---|
| 相对上一轮入库的那份 | `11,9,1` 一格 | 无 | 无 |
| **相对主仓附件原稿** | `11,9,1` 一格 | 无 | 无（元数据仅 `complexityTier` `L3 → L4`，那是后勤 18 已报备的标签订正） |

- **主仓附件两个文件一字未动**（`docs/协作/后勤/附件/tower_cottage.json` 与 `gen_tower_cottage.py`，大小与时间戳均未变；主仓 git 里这个目录还是未跟踪状态，我没有对它做任何写入）
- 删的方式是**按行精确摘除**（先断言那 8 行的内容逐行匹配再删），**没有整份重新序列化**，所以文件其余部分的缩进与字节完全没动

### 二、任务 2：复验（全部已证实）

#### 加载与块数

| 项 | 改前 | 改后 |
|---|---|---|
| `BlueprintLoader.loadBlueprint('tower_cottage')` | ok | **ok** |
| 总条目 | 729 | **728** |
| 其中 `air` | 13 | **13**（不变） |
| **实体块** | 716 | **715** ✓ |
| `chest` 只数 | 6 | **5**（余下 `1,1,1` / `1,1,2` / `10,1,1` / `11,1,1` / `4,5,1`） |
| `validator` 错误诊断 / 非错误诊断 | 0 / 0 | **0 / 0** |

#### 宜居硬闸：**通过**

```
宜居硬闸 ok = true    失败项: []
```

| 检查项 | 改前 | 改后 |
|---|---|---|
| **allFunctionalBlocksUsable** | **false**（`chest@11,9,1`） | **true，不可用清单空** ✓ |
| actualDoorCount | 2 | 2 |
| shellLeakCount / exposedInteriorCells | 0 / 0 | 0 / 0 |
| completeRoof / roofCoverage | true / 1.0 | true / 1.0 |
| detectedStories / storyLevels | 3 / [1,5,9] | 3 / [1,5,9] |
| continuousStairPath | true | true |
| allRequiredRoomsReachable / unreachableRooms | true / [] | true / [] |
| roomsStoryCrosscheck | ok | ok（declared [1,5,9] / heuristic [1,4,10]） |
| remainingScaffoldCount | 0 | 0 |
| **usableInteriorVolume** | 181 | **183**（+2：箱子那一格与它上方空出来了） |
| 体积 / 占地 | 2380 / 140 | 2380 / 140（上限 90000 / 2500） |
| 包围盒 | x −1..12, y 0..16, z −2..7 | 不变 |

#### 编译与档位

| 项 | 改前 | 改后 |
|---|---|---|
| planId（带脚手架） | `fffde74cbe41` | **`construction_plan_aa0541605b26`** |
| planId（按元数据跳过脚手架，**真机走这条**） | `0a1a7fbc9e36` | **`construction_plan_152054c01dfc`** |
| 放置步 / 脚手架 / 总步（带脚手架） | 716 / 17 / 847 | **715 / 17 / 846** |
| 放置步 / 脚手架 / 总步（跳过脚手架） | 716 / 0 / 813 | **715 / 0 / 812** |
| 三道顺序关卡推迟数（轴向 / 可点 / 点面） | 0 / 0 / 0 | **0 / 0 / 0**（仍无死结） |
| 复杂度档位 | L4 | **L4**（L3 仍超标 5 项：715/500 块、14/9 宽、10/9 深、140/81 占地、19/12 材料；L5 仍因 715/1000 不达标） |

### 三、任务 3：回归（已证实）

- **其余 20 份图纸 planId 全部不变**：门楼 `d1919e8b1032`、别墅 `26d666778fb5`、四份社区（`50a58a51d581` / `743d20b45561` / `71a464b18cd6` / `73adadf946ad`）、本地库四份（`84ee1fbfe570` / `bc4529011665` / `ddae7f294526` / `535e20c8bad8`）、生成式十份（`bc75737eda94` / `126d9aa6f555` / `b70b07efb201` / `f8cacf4318a8` / `33f837afb67b` / `0d370137d85c` / `585c6de25beb` / `0237a2822171` / `c8f90e70e711` / `522919310e49`）——逐个复核
- `npm test`：基线 **65 组 / 87 行 / exit 0** → 收尾 **65 组 / 87 行 / exit 0**
- **因块数变化更新的断言：只有 1 条**

  | 文件 / 断言 | 原值 → 新值 | 为什么 |
  |---|---|---|
  | `tests/intent-blueprint-names.test.js` · `testTowerCottageIsBuildableAtItsMeasuredTier` 里那句「选出来的必须是那份 N 块的原稿，不是被换掉的」 | `716` → **`715`** | 这条钉的是「解析到的图纸就是它本身、没被档位兜底换成别的」，**块数只是它的指纹**；箱子删掉后指纹自然变。断言的语义一个字没改，只换了指纹值，并在旁边注明是决策 #94 |

  另外把两处注释里的「716 块」顺手改成 715（纯注释，不是断言）。
- **F-1（六种说法）与 F-3（不抢别人的说法）不涉及坐标或块数，未改、仍绿**；F-2 除上面那一句指纹外的部分（档位 L4、`buildable`、`fixedScale=false`、`displayName`）也未改

### 四、任务 4：材料清单（已证实）

`getRequiredMaterials` 单栋 **711 件 / 19 种**（改前 712）。**只有箱子变了**：

| 材料 | 改前单栋 | 改后单栋 | ×3 | 组数 |
|---|---|---|---|---|
| **chest** | 6 | **5** | **15** | 1 |
| 其余 18 种 | — | **一件不差** | — | — |

（711 而非 715：床按「张」算 3 张占 6 格、门按「扇」算 1 扇占 2 格。）

### 五、任务 5–7

- **看板**：`m01` 改成「中等图纸定案：塔楼小筑 715 块，箱子问题已按老板决策解决，待建造 26 真机验收」，并把复验数字与那条「不搭脚手架」的提醒写了进去；`dashboard-lanes` 测试 PASS（82 张卡合法）
- **归档**：第 18 轮反馈已整份挪到 `docs/协作/历史/后勤第18轮-塔楼小筑离线接入与箱子拍板.md`
- **提交**：`c42649a3`（图纸 + 测试期望值）、`c4c1919e`（看板），均显式指定文件

## 交接

### 给建造线（建造 26 真机连盖三次照这个来）

1. **说法不变**：`建个塔楼小筑` / `建个带塔楼的房子` / `建个塔楼房子` / `建个中等房子` / `build a tower cottage` / `build a medium house` —— 六句都解析到 `tower_cottage`，自动带 **L4** 档位
2. **块数改了**：实体 **715**（原 716），另 13 格特意留空；占地 14×10，高 17，正门朝北（门 `2,1,0`/`2,2,0` 朝 south，门廊在更北的 z −1..−2，塔楼在东北角，烟囱在西山墙外 x=−1,z=3 从 y0 砌到 y13）
3. **planId 换新**：真机走 **`construction_plan_152054c01dfc`**（元数据 `skipScaffolding: true`）；带脚手架那条是 `construction_plan_aa0541605b26`。**看到旧的 `0a1a7fbc9e36` / `fffde74cbe41` 说明拿的是上一轮的版本**
4. **材料清单只改一样**：`chest` 单栋 6 → **5**，×3 = **15**（1 组）。其余 18 种与上一轮交接的表逐行相同：spruce_planks 182/546、stone_bricks 160/480、white_terracotta 91/273、dark_oak_stairs 85/255、cobblestone 52/156、spruce_log 48/144、glass_pane 23/69、dark_oak_planks 14/42、spruce_stairs 12/36、torch 10/30、dark_oak_log 10/30、spruce_fence 5/15、bookshelf 4/12、mossy_stone_bricks 3/9、white_bed 3/9、furnace 2/6、spruce_door 1/3、crafting_table 1/3
5. **塔楼三层现在只有一张床，没有箱子了** —— 若真机上看到她往 `11,9,1` 放东西，那是拿了旧图纸
6. **上一轮那几条真机要留意的点仍然有效**（未变）：楼梯井那 6 格 air（主屋 `2..4,4,5`、塔楼 `9..11,4,−1` 与 `(9,8,0)(9,8,1)(10,8,1)`）**是承重的，往里垫砖会当场断掉上楼的路**；最高的三处施工点是塔尖 `10,15,0` + 火把 `10,16,0`、锥顶 y13..14、主屋屋脊 y12 那九根 `dark_oak_log[axis=x]`；烟囱 `−1,y,3` 是一根 14 格高的独立柱；零红石 / 零漏斗 / 零活塞 / 零含水方块
7. **合并**：本轮 2 笔在 `feat/ops` 上（`c42649a3` / `c4c1919e`），加上第 18 轮的两笔共 4 笔；main 自 `af27dae8` 未动，收进去应无冲突

### 给开发

- **这处外形改动是老板拍板后的落地，不是后勤线自行决定** —— **决策 #94**（选 A：删掉塔楼三层 `11,9,1` 那只箱子）。本轮只删这一块，逐格比对证明其余方块位置 / 材质 / states / phase / role 与您那份附件原稿完全一致
- **主仓附件 `docs/协作/后勤/附件/` 两个文件未动、未删**（该目录在主仓 git 里仍是未跟踪状态，本轮对它零写入）
- 上一轮报备的**拍板问题 2（`metadata.skipScaffolding: true` 对 17 格高的房子是否合适）本轮仍未动**，等真机说话
- 后勤 18 记的 **#84 联动口子**（`ai/blueprint-name-index.js` 的 `buildBlueprintNameIndex(options)` 加 `options.extraEntries`）本轮未实现，仍只是一笔记录

## 按 AGENTS.md 第 11 节

1. **读过的文件**：主仓 `docs/协作/说明.md`、`docs/协作/后勤/说明.md`、`docs/协作/后勤/给CC.md`、上轮 `CC反馈.md`、附件 `tower_cottage.json`（只读，用于逐格比对）；MC-ops `blueprints/tower_cottage.json`、`tests/intent-blueprint-names.test.js`、`systems/blueprint-loader.js`、`systems/building-hard-gate.js`、`systems/building-complexity.js`、`systems/construction-compiler.js`、`package.json`。
2. **改动的文件**：MC-ops `blueprints/tower_cottage.json`（删 8 行）、`tests/intent-blueprint-names.test.js`（1 条断言指纹值 + 2 处注释）、`dashboard/board-data.json`。主仓只改 `dashboard/board-data.json`、`docs/协作/后勤/CC反馈.md`，新增归档 `docs/协作/历史/后勤第18轮-塔楼小筑离线接入与箱子拍板.md`（**只改不提交**）。**主仓附件目录零改动**。
3. **执行的命令**：`git status/log/add <显式文件>/commit -F`；`cp`（归档、备份、看板同步）；一段按行精确摘除的 Python 脚本（先逐行断言再删）；只读 `node -e` 探针（加载 / 宜居硬闸 / 编译两种口径 / 档位 / 材料清单 / 逐格比对）；`node tests/intent-blueprint-names.test.js`、`node tests/dashboard-lanes.test.js`；`npm test`（开场基线 + 收尾）；复跑 `.tmp/ops-round15-hopper-census.js` 核对全部 planId 金值。**未执行**：社区同步、网络、`npm install/ci/update`、服务器/bot/端口/RCON、被禁 git 命令。
4. **测试结果**：基线 **65 组 / 87 行 / exit 0** → 收尾 **65 组 / 87 行 / exit 0**；`dashboard-lanes` PASS。
5. **当前最新判定**：
   - 任务 1–2 **完成**（已证实）：只删一块、逐格比对干净；加载 ok；**宜居硬闸 ok=true 零失败项**；块数 715 / air 13；两个新 planId 报全
   - 任务 3 **完成**（已证实）：其余 20 份 planId 不变、全绿；**因块数变化只改了 1 条断言的指纹值**，语义未动并已写明
   - 任务 4–7 **完成**：材料清单（chest 6→5，其余不变）、看板、归档、两笔提交
   - **真机未验**：全部结论来自读码 + 离线探针 + 单测。这份图纸的离线关卡到此全清
6. **生成文件 / 内存是否变脏**：否。主仓 `data/memory/*`、`acceptance/reports/latest-*` 零触碰；MC-ops `.tmp/` 新增备份与探针输出（未跟踪、未提交）。
7. **是否建议提交**：`feat/ops` 上本轮 2 笔（`c42649a3` / `c4c1919e`）。**建议建造 26 连同第 18 轮那两笔一并收进主干**（共 4 笔）。
8. **剩余风险**：
   - **不搭脚手架 + 17 格高**：塔尖 y15/16、锥顶 y13/14、烟囱顶 y13 能不能够到，**UNKNOWN**，要真机说话（上一轮拍板问题 2，未定）
   - 楼梯井那 6 格 air 是承重的，任何往里垫砖的行为（包括临时参照）都会断掉上楼的路（**已证实**，后勤 18 离线试算三次全断）
   - 塔楼三层现在是 9 格里一张床、无箱子，站位仍只有三个（**已证实**）——设计上够用，但没有余量
   - 主仓那份附件原稿与入库版本现在**差一块砖 + 一个档位标签**，两边不再逐字相同（**已证实，且是刻意的**）：原稿是您拍板的设计稿，入库版是决策 #94 之后的施工版。若日后有人拿原稿重新入库，会把这只箱子带回来
   - 第 10 轮遗留的 `waterlogged` 两把尺子不一致仍未处理（**已证实**；对这份图纸不生效）
