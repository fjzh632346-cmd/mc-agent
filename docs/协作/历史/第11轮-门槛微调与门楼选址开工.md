# 建造 第 11 轮反馈

- 线: 建造
- 轮次: 11
- 日期: 2026-09-01
- 状态: 进行中（第 1、4 步已完成；门楼已开工、盖到约三分之一，按任务书"不硬赶"条款如实报进度，下轮续建）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / d1920d41（门槛微调一笔；本反馈与文档随后一笔）

---

## 一句话结论

住宅门槛两个数按拍板改好了、两栋备选小屋确认双双够格；门楼重新选好了址、正式开工并盖起了三分之一（一千二百三十三块里四百零七块就位、零多余），但她的程序每隔约半小时会"假死掉线"一次、加上一批放置顺序的老毛病，这一轮盖不完，工地和进度都已存好档、随时能续建。

## 白话摘要

第一件事，按您的拍板把当住宅的门槛各松了一格（宽度和室内空间各降一点），配套的测试全部改好、全量测试全绿。顺带用后勤上一轮的实测数字复核：两栋备选小屋原先各差的正是这两项、其余项目全过——新门槛下它们双双够格了。

第二件事，门楼重新选址。我把周边四大片地都扫了一遍：东边那片看着最平整的平原，恰恰是上上轮查出的"薄壳空洞区"（表面一层壳、底下是空的），果断排除；最后选在别墅南边的开阔草坡上，距别墅、瀑布、出生点都留足了安全距离，地面高差只有四格，脚下也探过没有空洞。

第三件事，开工前把备料核了一遍：图纸要的三十七种材料在新版本里全部存在、发放渠道真机验证可用——上次拦住门楼的"版本墙"确认彻底消失。

第四件事，正式开工。她自己找对了工地、认对了图纸，一路盖到约三分之一。但过程坎坷：一是她的程序每隔约半小时会整个"假死"一次，跟服务器断开还不自知、也不会自己重连，我重启了两回；二是一批放置类老毛病集中爆发——最典型的是"装饰梁柱"这类要靠着别的方块才放得上的东西，图纸里它们的依靠物排在它们后面才盖，她就永远卡在原地。这类死结方块我按您第九轮批准过的"手术"办法直接替她补上并逐块记账（共一百三十四块，占总量约一成一），其余三百来块是她自己放的。所有毛病的证据都留好了，转给修缮线。

下一步：工地进度已存档，喊一声就能接着盖。但我建议先让修缮线把"假死掉线"和"放置顺序"这两个根子修掉再续建，否则每盖半小时就要人工救一次、手术账还会越滚越大——这个取舍请您拍板。

## 需要老板拍板的问题

### 问题 1：门楼下一步怎么续？

- 背景：工地与进度已存档、随时可续。但两个根子问题（程序半小时假死一次、放置顺序死结）没修的话，续建全程要人守着，且"手术代放"的方块占比会继续升高（目前约一成一）。
- 选项 A：**先修后建**——让修缮线先修掉假死重连和放置顺序两个根因，建造线下一轮再续建。代价：门楼晚一两轮完工；好处：续建又快又干净，手术账不再涨。
- 选项 B：**下轮硬续**——继续保姆脚本＋手术代放往前推。代价：人工干预密集，最终"她亲手盖的比例"越来越低；好处：完工日期早一点。
- 我的建议：**A**。门楼不赶工期，修好根因再盖，账目和成色都好看。

## 技术细节

### 〇、开场基线

- 开场 `git status` 清点、`npm test` 全绿（退出码 0）——**已证实**
- 第 10 轮反馈已归档 `docs/协作/历史/第10轮-正式升级与孤本入库与三线合并.md`

### 一、第 1 步：决策 #30 落实（完成，已提交 `d1920d41`）

- `systems/faithful-community-validator.js` villa 档：`minWidth 18→17`、`minInteriorVolume 16→15`，只动这两个数（附注释注明决策出处）；modern_cottage 档过时注释同步改写，判据未动
- 单测：`tests/faithful-community-import.test.js` 新增 `testVillaRulerBoundaryAfterDecision30`——宽 17 整栋过 / 宽 16 在 minWidth 上挂；密闭盒式探针精确构造室内体积 15 过 / 14 挂（每开放柱恰好 1 个可站立格）。`tests/modern-cottage-classification.test.js` 的 VILLA_LIMITS 常量同步 17/15
- `npm test` 全量**全绿**（改动前后各一次，退出码 0）——**已证实**
- 两候选复核（**合理推断**，依据为后勤第 5 轮实测留档，本地无这两份缓存蓝图、未做网络重算）：`mcbuild-a-small-modern-house-16786` 唯一失败项 minWidth（实测 17 vs 旧门槛 18）；`mcbuild-wood-modern-house-15774` 唯一失败项 usableInteriorVolume（实测 15 vs 旧门槛 16）；其余判据两栋全过 → 新门槛 17/15 下**双双够格**

### 二、第 2 步：门楼选址（完成）

- 探针扫了 4 片候选区（`.tmp/cc-r11-site-probe2.js`、`-refine*.js`，只读）：
  - 东平原 (640-700, -140..-80)：**表面 y64 完全平整但即第 7 轮空腔薄壳带**（滑窗内 8-18 列 y62-63 空洞）——排除
  - 西场 (480,-120)：632 列水域——排除；旧门楼址以东 (716,-190)：25 列水+4 列空洞、高差 14——排除
  - **选定：原点 (599, 67, -19)**，南平原。足印 11×23 内高差仅 67→71（北 13 排全平 y68）、无水无岩浆、仅 1 处 y63 单格空洞（三层实体之下，无踩穿风险）
- 间距核验：与别墅包围盒 z 向净距 48（含夹具扰动区仍 ≥30）、瀑布 59、出生点 (536,91,25) 约 46、避开东缘沟与已知空腔带——**已证实**
- 落位方式：把 accept_tester 登出点停在锚点 (535.2,67,-21.2)，验收夹具按 case2 偏移 (+64,+2) 算出的原点与选定值逐位一致——**已证实**（`BUILDING_RUN_DECISION` origin=599,67,-19）

### 三、第 2 步：备料核（完成，PASS）

- 离线：`getRequiredMaterials` 37 种材料（tuff 系 5 种 218 块、氧化铜门 2 种 8 扇等）在 minecraft-data 1.21.8 物品表**零缺失**；`checkBlueprintVersionCompatibility(1.21.8)` ok=true、missingBlockTotal=0，blockWithoutItem 仅 redstone_wire×44 / redstone_wall_torch×1（备料链路已正确映射为红石粉/红石火把）——**已证实**
- 真机：夹具 give/item replace 对 chiseled_tuff_bricks、tuff_brick_stairs、waxed_oxidized_copper_door 等 1.21 新材料全部命中——**已证实**（第 6 轮版本墙确认消失）

### 四、第 2 步：开工与过程（进行到 33%）

- 官方首跑：`ACCEPTANCE_BUILDING_SCENARIOS=case2_fort_wall_gate npm run acceptance:minecraft -- --cases=building`。夹具就位（清场、y66 石垫层、双材料箱 (596/594,67,-23)、工具/食物/垫方块），`BUILD_SITE_SCAN` 1233 目标 0 障碍 0 危险；开工 62 秒后 `place_failed:unstable_air`（sticky_piston@604,68,-10），官方判定**如实落档 FAIL**（`latest-summary.md`：0 PASS / 1 FAIL，reason=build_task_failed:place_failed:unstable_air；未改动报告）
- 续建走 run 存档：`construction_run_d2f3cbb572ceb6b7`，全部 `BUILDING_RUN_DECISION` 均 resume 命中同 runId/同指纹（1022c03b…，与第 6 轮门楼一致）——**已证实**
- 保姆驱动 5 班（`.tmp/cc-r11-fort-driver.js`，输出 5 份日志留档）：断料补给、跑丢救援传送、重复卡点手术，全程 `resume` 推进
- **终值（停服前探针全量扫描，0 未加载）**：`present/type-correct 407/1233 (33.0%)`、**extra 0**、放置态 stateFidelity 0.87（差异集中在转发器 delay/facing 与红石线自动连形）；其中**夹具手术代放 134 块**（133 根黑橡木原木梁柱 + 1 块錾制石砖，全部守护式 `execute if block …air run setblock`，坐标清单在本轮会话与 `.tmp` 脚本留档）、她自放约 273 块
- 夹具补给记账：小额 8 类×8=72 件（红石组件）；大额一次 16 类共 381 件（对照 missingMaterials 清单足额）；均 `give LinXia`
- 世界扰动：全部限于新工地及其夹具扰动区（x575-657 / z-35..-21 一带与足印区），别墅及周边**一格未动**（活动 z ≥ -35，别墅 z ≤ -69）；探针 forceload add/remove 全部成对撤销；停服走 RCON 优雅停——**已证实**

### 五、执行层老毛病取证（全部只取证不修，转修缮线）

1. **放置顺序死结（本轮最大量产源）**：轴向方块（原木梁/柱 `axis=x/y/z`）的放置参照物在施工序列中排在其后 → 该步永远失败，且任务失败后从头扫描重试仍卡同一块=死循环。实证：y72 梁层与 y76-82 塔柱层反复 `stateful_axis_no_y/x_reference`（如 605,72,-4 / 602,72,-3 / 607,78,-12，各连续 3+ 次同点）。临时参照机制（`BUILD_PLANNED_REFERENCE_REPAIR_TRIGGER`）对这类块未能救回——**已证实**
2. **place_failed:unstable_air**：sticky_piston@604,68,-10、chiseled_tuff_bricks@607,73,-4、chiseled_stone_bricks@604,75,-2（后者 3 次同点，手术解）——**已证实**
3. **朝向/状态错放**：`state_mismatch:face:wall!=ceiling`（dark_oak_button@607,77,-5，天花面按钮）；终扫中 repeater `delay 2→3/1`、`facing west→north` 批量错——**已证实**（与第 9 轮骷髅头朝向同族）
4. **block_equip_unconfirmed:chiseled_deepslate:dark_oak_log**（换手确认失败）——**已证实**
5. **INVENTORY_FULL 触发 return_to_base 拉去 594 格外老基地**，中断施工跑丢——**已证实**（16:27 起，救援传送处置）
6. **箱子交互卡死**：站在箱旁 2 格 `CHEST_OPEN_FAILED`（windowOpen 20s 不触发）+ `CHEST_PATH_UNREACHABLE` 循环、`STAGING_CRAFT_DEPOSIT_FAILED`——**已证实**（79 次 UNREACHABLE 记录）
7. **任务队列悬死**：#24 被 `manager_stop` 打断后，已入队的 #25 十七分钟不启动，需外部停止+重发——**已证实**
8. **进程假死掉线（本轮根本阻断项）**：约每 30 分钟一次（16:36、17:06、~17:39），服务器侧 `LinXia lost connection: Timed out`、客户端 ECONNRESET；**无自动重连**，且假死后 [状态] 行仍打印缓存位置、任务机器继续本地空转报错，极具误导性——**已证实**（bot 进程重启 2 次处置）

### 六、第 3 步收尾与续建关卡

- run `construction_run_d2f3cbb572ceb6b7`：非终态（最后状态 BLOCKED_MATERIAL_SHORTAGE，为假死进程本地误报，实际材料已补齐在她背包/箱内），**下轮 `build fort wall gate` 即续**；原点/指纹/进度均在存档
- 材料箱 ×2 留在 (596/594,67,-23)（内有余料），工地保持现状
- 续建的实际前置：技术细节第五节第 1、8 条（顺序死结+假死重连）不修则续建效率极低——对应拍板问题 1

### 七、第 4 步：归档表（完成）

`docs/协作/说明.md` 历史归档表补第 9、10 轮两行（仅补表，协议正文未动）

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/*`、第 6/7 轮历史归档、`systems/faithful-community-validator.js`、`tests/{modern-cottage-classification,faithful-community-import,structure-use-classification}.test.js`、`acceptance/cases/building.acceptance.js`、`acceptance/adapters/minecraft.adapter.js`（runServerCommand 段）、`utils/site-planner.js`（接口）、`scripts/mc-server.ps1`、`scripts/mc-rcon.js`、门楼缓存蓝图、`dashboard/board-data.json`（小屋实测数字）、`data/community-builds/sources.json`、`data/memory/construction-runs.json`、`logs/bot-current.log`、`D:\code\MC-server\logs\latest.log` 与 `server.properties`（只读）

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `d1920d41` | villa 门槛 17/15 + 边界单测（faithful-community-validator.js、两个测试文件） |
| （随后一笔） | 本反馈 + 第 11 轮任务书 + 第 10 轮归档件 + 说明.md 归档表两行 |
| （世界） | 新门楼工地：夹具场地准备（标准链路）、手术代放 134 块、补给 453 件、施工 407/1233；别墅零改动 |
| （未入库） | `.tmp/cc-r11-*.js` 探针/驱动 8 份及输出日志 |

### 3. 执行的命令

`git status/log/add/commit`（显式指定文件）、`npm test` ×2（全绿）、单套件 ×3、`mc-server.ps1 start/stop/status`（RCON 优雅停）、bot `Start-Process` ×3（含 2 次假死重启，仅杀本仓 bot.js 进程）、官方验收 case2 一次、保姆驱动 5 班、RCON：list/forceload(成对)/tp(救援与探针)/give(补给记账)/execute if block…setblock(手术 134，全部守护式)/data get。
未执行：`git add -A`、`git stash`、`git reset`、`git clean`、任何 sync、`ops.json`/`server.properties` 修改、对修缮线端口(25566)或其他 worktree 的任何访问、别墅范围任何方块改动。

### 4. 测试结果

- `npm test`：开场全绿；门槛改动后全绿（含新增边界用例）——**已证实**
- 官方验收 case2：FAIL（build_task_failed:place_failed:unstable_air）如实落档，报告未改
- 终扫：407/1233 present、0 extra、stateFidelity 0.87（探针 0 未加载）

### 5. 当前最新判定

- 第 1 步门槛：**完成**（两数改+边界单测+全绿+候选双够格复核）
- 第 2 步选址：**完成**（(599,67,-19)，理由与数据齐）
- 第 2 步备料：**完成 PASS**（37 种全存在、真机发放验证过）
- 第 2 步开工：**进行中 33%**（择日续建，run 非终态）
- 第 3 步收尾：**按"未盖完"分支完成**（终值+续建关卡已写清）
- 第 4 步归档表：**完成**

### 6. 生成文件 / 内存是否变脏

`latest-*`（case2 FAIL 记录，有意保留不提交）、`task-memory`/`world-memory` 真机会话正常滚动（不提交）、`construction-runs.json` 新增门楼 ACTIVE run（有意）。世界净变化：新工地施工+夹具产物（垫层/箱/手术块，全部记账）；别墅、出生点、孤本档案、隔离目录零接触。

### 7. 是否建议提交

门槛一笔已提交；本反馈+任务书+归档件+说明.md 随后一笔提交。生成报告与 memory 不提交。

### 8. 剩余风险

- 假死掉线根因未明（keep-alive 超时，疑事件循环阻塞），未修前长时间无人值守施工不可行（转修缮，拍板问题 1）
- 放置顺序死结未修前，续建中每遇轴向/依附类方块仍会卡死；手术代放占比（现 11%）会继续上升
- 转发器 delay/facing 错放已在世界中存在（红石机关最终可能不工作），属朝向类根因的一部分，终验时按忠实度闸门如实计
- 假死期间 run 存档的 BLOCKED_MATERIAL_SHORTAGE 状态与实际库存有偏差，续建首扫会自行对账（合理推断）
- feedback-cooldown 1ms flake 仍在（第 10 轮已报，不在本轮范围）
