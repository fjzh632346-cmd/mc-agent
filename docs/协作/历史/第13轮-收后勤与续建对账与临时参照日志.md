# 建造 第 13 轮反馈

- 线: 建造
- 轮次: 13
- 日期: 2026-09-02
- 状态: 已完成
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / 57181975（合并笔 baa45b59 + 诊断日志与单测笔 57181975；本反馈与文档随后一笔，主仓快进到同一 HEAD）

---

## 一句话结论

后勤第六轮的放置顺序修补已收进建造线并推到主干、测试全绿；离线核实证明修补后门楼续建仍会接着原来的施工记录走、不会被当成新工程从头来；临时柱子失败原因的日志补上了；续建开工清单写好，只等修缮线交假死修补。

## 白话摘要

第一件事，合并。把后勤第六轮那笔"梁柱先排参照物再排自己"的修补收进建造线，一处冲突没有；本线测试从五十一组增加到五十二组、全部通过；主干仓库顺利快进到同一版本，再跑一遍同样全绿。按第三十九条拍板，本轮没有进门楼工地，一格没动。

第二件事，后勤最担心的对账问题查清了：她认"这是不是同一个工程"靠三样东西——图纸指纹、落点坐标、世界标识，**不看施工顺序编号**。顺序编号这次确实变了，但那三样一个没变。我把施工存档复制到临时目录（原件没碰），用合并后的程序对门楼在原落点重新编排，再走一遍她的匹配逻辑：命中原来那条记录。已经核验过的九百九十八步逐条对得上，一步不少。还有一个意外的好消息：后勤推到序列末尾的那三十一根"没有参照、要靠临时柱子"的梁柱，上一轮手术已经全部代放到世界里了，存档里都记为已核验——也就是说这次续建根本不会碰到"临时柱子立不立得起来"那一关。写了三组测试把这个结论钉死。

第三件事，日志。临时柱子失败的原因以前只记在内部数据里不打日志，现在补了一行，把目标、方向、失败原因、试过的位置都打出来，行为一点没改；配了断言。顺带核实后勤说的"竖向柱子的临时参照只试正下方一个位置"属实。

第四件事，续建清单已写在技术细节里，下轮照着走。其中最要紧的一条：开工时必须带世界标识环境变量，不带的话她不但认不出旧记录，还会把它直接作废，这是程序里写死的。

下一步：等修缮线第七轮的假死修补交卷、合并进来，再续建门楼。本轮没有需要您拍板的事。

## 需要老板拍板的问题

无

## 技术细节

### 〇、开场基线

- 开场 `git status --short`：已跟踪脏文件仅 `acceptance/reports/latest-*`（3）、`data/memory/task-memory.json`、`world-memory.json`、`docs/协作/建造/给CC.md`（开发下发的第 13 轮任务书）；未跟踪的 `.tmp/`、`dashboard/`、`docs/协作/后勤/`、`docs/协作/修缮/`、`docs/协作/老板决策台账.md`、各线历史归档件、`probe/`、`_backups/` 一律未动——**已证实**
- HEAD = `9772e2b6`（第 12 轮反馈小修笔），与任务书预期一致；`npm test` 51 组全绿退出码 0——**已证实**
- 第 12 轮反馈整份挪到 `docs/协作/历史/第12轮-合并轮与证据包与假死线索.md`（`cmp` 与原件一致）后重写本文件——**已证实**

### 一、第 1 步：合并（完成）

- 目标提交 `0eef49b5` 的父提交就是 `545b69a5`（本线第 12 轮合并笔），merge-base = `545b69a5`，`HEAD..0eef49b5` 恰好一笔；`feat/ops` 尖端也是 `0eef49b5`——**已证实**
- 冲突面预核：`git log 545b69a5..HEAD -- package.json utils/site-planner.js systems/construction-compiler.js systems/building-system.js` 为空，本线自合并点后未动过这四个文件——**已证实**
- `git merge 0eef49b5 -m …`（显式提交号）→ 合并提交 **`baa45b59`**，ort 策略，**零冲突**：6 文件 +441/−19——`utils/site-planner.js` +114/−1、`systems/construction-compiler.js` +35/−16（净 +19）、`systems/building-system.js` +7、`package.json` +5/−2、新增 `tests/axis-reference-order.test.js`（298 行）与 `tests/fixtures/fort-wall-gate-axis.json`（88 KB）——**已证实**
- 组数：主链 `node tests/` 计数 51 → **52**（新增 `axis-reference-order` 在 `construction-compiler` 之后）；`npm test` 全绿退出码 0，74 行通过（与后勤报的 74 一致）——**已证实**
- 主仓推账：`git -C D:\code\MC merge --ff-only refactor/building-blueprint-ir-v1` → `Updating 9772e2b6..baa45b59 Fast-forward`，未被未跟踪文件拦住；`npm --prefix D:\code\MC test` 全绿退出码 0、74 行——**已证实**
- 未合 `feat/fix`（修缮线第 7 轮仍在其上）；`blueprint-compatibility-adapter.js` / `blueprint-validator.js` / `construction-run-store.js` 不在本次合并改动集里——**已证实**

### 二、第 2 步：续建对账离线核实（完成，结论：**会命中**）

#### 2.1 续建命中靠什么（读码，已证实）

- 入口 `systems/building-system.js:1212` `prepareConstructionRun()` → `constructionRunStore.findActiveCompatible(criteria)`，criteria 由 `constructionRunCriteria()`（`building-system.js:6355`）组装：`blueprintId` / `blueprintRevision` / `blueprintHash` / `planId` / `placementContext` / `world` / `bounds` / `stagingChests`
- 但真正比对的是 `systems/construction-run-store.js` `constructionRunCompatibility()`：只比 **`blueprintId`、`blueprintHash`、`placementContext`（原点+旋转+镜像，`stableStringify` 全等）、`world`（维度 + worldId）** 四项；**`planId` 根本不在比对列表里**，`blueprintRevision` 也不在
- 步骤对账 `reconcileConstructionRun()`（`building-system.js:7760`）经 `mergeRuntimeAndStoredSteps()` 按 **step id** 合并；step id 由 `construction-compiler.js:419` `createStableStepId()` 生成，哈希输入 = 蓝图 revision + 蓝图 hash + placement + sourceBlockKey + action + kind + target + blockName + current，**不含序号**；`planId`（`createPlanId()`，`construction-compiler.js:398`）才是把整个步序哈希进去的那个
- `blueprintHash`（`construction-compiler.js:441` `createBlueprintHash()`）只哈希蓝图 IR（id/name/bounds/blocks 的 key/position/block/phase/role/dependencies/clearancePolicy），与施工顺序无关；后勤的改动没碰适配器/校验器，IR 不变 → hash 不变
- 旁证：存档里这条 run 的 `planId = construction_plan_82c288475728`，而它自己 `blueprintFreeze.planId = construction_plan_08d372daeae1`，两者本来就不一致（planId 含地形相关的脚手架/清障步，每次编译都可能不同），而这条 run 在第 11 轮已经 `resumeHistory` 连续续建 **18 次**——planId 从来不是命中条件——**已证实**

#### 2.2 离线验证（存档副本，原件未动，已证实）

脚本 `verify-resume-match.js`（会话 scratchpad，未入库）：把 `data/memory/construction-runs.json` 复制到 scratchpad（md5 `2339e18e…` 前后一致，原件 mtime 仍为 9/1 10:40），用合并后代码走两条编译路径，再直接调用 `ConstructionRunStore.findActiveCompatible()` 与 `constructionRunCompatibility()`：

| 项 | 存档 run `d2f3cbb572ceb6b7` | A：缓存蓝图→适配→校验→编译 @(599,67,-19) | B：run 里的 `frozenBlueprintIR` 直接编译 |
|---|---|---|---|
| blueprintId | fort_wall_gate | 同 | 同 |
| blueprintRevision | `092eef09…dffd4f` | **相同** | **相同** |
| blueprintHash | `1022c03b…6582` | **相同** | **相同** |
| planId | `82c288475728` | `56b2efd53b0d`（**不同**，与后勤报的一致） | `56b2efd53b0d` |
| placementContext | origin 599,67,-19 / rot 0 / 无镜像 | 全等 | 全等 |
| `constructionRunCompatibility` | — | `{ok:true}` | `{ok:true}` |
| `findActiveCompatible` | — | **命中 `construction_run_d2f3cbb572ceb6b7`** | **命中** |
| 编译 summary | — | axisReferenceDeferred **31** / roots **7** | 31 / 7 |
| place 步 id 落在存档里 | 1863 步（verified 998 / state_repair 37 / pending 828） | **1233/1233** | 1233/1233 |
| 存档 verified 被本次编译覆盖 | — | **998/998** | 998/998 |
| 存档 state_repair 被覆盖 | — | **37/37** | 37/37 |
| 存档里有、本次编译没产出的步 | — | **0** | 0 |
| A 与 B 步 id 序列 | — | 逐项相同 | — |

- 7 个根：`605,72,-4` `605,72,-2` `604,72,-16` `603,72,-13` `602,72,-12` `606,72,-13`（dark_oak_log axis=y）+ `602,72,-16`（axis=z），与后勤 2.5 表完全一致——**已证实**
- **推迟到末尾的 31 步在存档里的状态：31/31 = `verified`**（第 11 轮手术代放的原木，17:40Z 那次 reconcile 已记为核验）。即本次续建**不会走到临时参照那一段**，后勤 UNKNOWN ②（悬空根节点靠运行时临时柱能不能立起来）在门楼续建里**得不到真机答案**；只有非尾巴的轴向块再失败时新日志才会出现——存档状态**已证实**，运行时再 reconcile 仍判 verified 为**合理推断**（第 12 轮已注明 17:40Z 那次 reconcile 依据缓存区块，可信度存疑；续建时以 `[BUILD_CONSTRUCTION_RUN] verified=` 实数为准）
- 离线与真机的差异：离线 `siteSnapshot.blocks=[]`，多编出 11 步 `foundation_fill`（y=66）和 333 步脚手架（存档 314）。运行时 y66 有石垫层不会有 foundation_fill（**合理推断**）；不在存档里的脚手架步会被 `mergeRuntimeAndStoredSteps()` 的 `isTransientScaffoldStep` 丢掉，不影响对账（**已证实**，读码）
- **反向结论**：若 `MC_WORLD_ID` 未设 → criteria.world.worldId=null → `world_identity_unverified` → `findActiveCompatible` 返回 null → `building-system.js:1214-1222` 用 `findActiveForBlueprint` 找到这条 run 后**直接 `abandonRun`**（状态 ABANDONED，不可逆）。`说明.md` 只写了"认不出、无法续建"，实际后果是**作废**——代码路径**已证实**，运行时是否先被 `decideConstructionRunStart` 的救援分析拦下为 **UNKNOWN**，不要拿真机试

#### 2.3 单测（`tests/construction-resume-plan-id.test.js`，新增 3 组，已入主链 52→53）

用后勤固定件 `tests/fixtures/fort-wall-gate-axis.json` 走生产链编译 @(599,67,-19)：

| # | 用例 | 断言 |
|---|---|---|
| 1 | 反序存的旧 run（planId 写死为存档值 `82c288475728`）仍被 `findActiveCompatible` 命中 | planId 与新编译不同；`constructionRunCompatibility` `{ok:true}`；命中 runId 相同 |
| 2 | 稳定 step id 不受重排影响 | 1233 个 place 步 id 全部在旧 run 里；31 步推迟尾巴的 id 也在 |
| 3 | 命中靠什么 | planId 随便改仍 ok；hash 改 → `blueprintHash_changed`；原点偏 1 格 → `placementContext_changed`；worldId 缺 → `world_identity_unverified` 且 `findActiveCompatible` 为 null |

单跑 PASS；`package.json` 主链在 `construction-run-store` 之后注册，另加 `test:construction-resume-plan-id` 脚本。

#### 2.4 结论

**会命中**（**已证实**：读码 + 存档副本离线匹配 + 单测三重）。续建时按后勤 2.7 第 2/3 条看日志即可：`[BUILD_CONSTRUCTION_RUN] mode=resume runId=construction_run_d2f3cbb572ceb6b7 verified=…`，且**不应出现** `[BUILD_CONSTRUCTION_RUN_ABANDON]`。无需改匹配逻辑，不需要老板拍板。

### 三、第 3 步：临时参照失败补日志（完成，只加日志不改行为）

- 位置 `actions/build.js:853-864`（`placeBlock` 内 `ensureTemporaryPlacementReference` 失败分支）。新增一行：

  ```text
  [BUILD_TEMP_REFERENCE_FAILED] target=x,y,z block=<name> axis=<x|y|z|none> error=<原 stateful 错误> reason=<ensuredReference.error> candidates=<同轴候选;…>
  ```

  `reason` 就是原来只挂在 `data.temporaryReferenceError` 的那个值；`candidates` 用与 `ensureTemporaryPlacementReference` 同一个纯函数 `temporaryPlacementReferenceCandidates()` 算出（含保留格过滤后的结果）。返回值、`data`、控制流一字未改；轴向块与非轴向块（台阶/活板门）的失败都打——**已证实**（diff +10 行，全是注释、两个局部变量和一条 `context.logger?.log?.()`）
- 单测：`tests/actions.test.js` `testPlaceBlockRejectsHorizontalLogWithoutAxisReference` 加 logger 捕获，断言恰好一行、含 `target=2,64,0`、`block=spruce_log axis=z error=stateful_axis_no_z_reference`、`reason=` 与 `result.data.temporaryReferenceError` 一致、`candidates=2,64,-1;2,64,1`——PASS——**已证实**
- 后勤 2.8「axis=y 临时参照只试正下方」核实：`actions/build.js:3528` `if (axis === 'y') return [{ x: 0, y: -1, z: 0 }]`——**属实，已证实**；未改（候选池）。补充：axis=x 试 ±x 两个、axis=z 试 ±z 两个（`:3526-3527`）
- 提交 `57181975`，`npm test` **53 组全绿**退出码 0、75 行通过；主仓快进 `baa45b59..57181975` 后 `npm --prefix D:\code\MC test` 全绿 75 行——**已证实**

### 四、第 4 步：门楼续建开工清单（下轮照此走）

**前置**：修缮线第 7 轮假死修补合并进本线 + 主干（#36/#37）。合并前不开工。

**A. 开场**

1. 服务器：`scripts\mc-server.ps1 start`（`MC_SERVER_JAVA` 指 JDK 21），`status` 见 25565 监听
2. bot 环境变量（四个都要；**`MC_WORLD_ID` 缺失会把 run 作废**，见 2.2 反向结论）：
   ```powershell
   $env:MC_WORLD_ID = 'building-A:新的世界'
   $env:MC_PORT = '25565'
   $env:MC_VERSION = '1.21.8'
   $env:MC_SERVER_JAVA = 'C:\Program Files\Microsoft\jdk-21.0.6.7-hotspot\bin\java.exe'
   ```
   `Start-Process node bot.js`；ONLINE-GATE 四信号：`[端口] 使用 .env 指定端口: 25565` / `[TickLoop] started` / `[Bot] 上线成功` / `[DEBUG_STATUS]`（需玩家聊天 `debug_status` 触发）
3. 她上线 1 s 内会被生存系统拉去 `return_to_base`（老基地 11,106,8，TOO_FAR_FROM_BASE），保姆脚本首步 `停止任务`（第 11 轮驱动脚本已有）
4. 传送：她的登出点现在在工地以西 87 格 (512,79,-0.5)。保姆脚本 SITE=(597,67,-22)，开场 `tp LinXia 597 68 -22`（脚本 `cc-r11-fort-driver.js:62` 已有）。accept_tester 锚点 (535.2,67,-21.2) 只有走官方 case2 夹具时才用得着
5. `forceload`：工地 bounds x599..609 / y67..85 / z−19..3，材料箱 x590..596 / z−23 → 建议 `forceload add 588 -26 611 5`，收工 `forceload remove 588 -26 611 5` **成对**
6. 停服走 RCON 优雅停（`mc-server.ps1 stop`），不杀进程

**B. 备料**

- 材料箱（存档 `stagingChests`）：主箱 (596,67,-23) `build_storage_1`、(594,67,-23) `build_storage_2`；另登记 (592,67,-23) `_3`、(590,67,-23) `_4`（第 11 轮夹具只摆了前两只，后两只是否真有箱子**待查**）
- 现存清单：**待查项**，续建时 `data get block 596 67 -23 Items` / `594` 各一次抄进反馈
- 上次记账：小额 8 类×8=72 件（红石组件）+ 大额 16 类 381 件 = 453 件，均 `give LinXia`
- 存档最后一次缺料清单（`blockedReason`，9/1 17:40Z）可作预备料参考：dark_oak_log 1、iron_bars 8、chiseled_polished_blackstone 24、polished_blackstone_brick_slab 75、polished_blackstone_bricks 30、lantern 3、chiseled_tuff 4、repeater 5、sticky_piston 5

**C. 放置顺序补丁判据（后勤 2.7）**

- 开工日志应有 `[BUILD_ORDER_AXIS_DEFERRED] blueprint=fort_wall_gate count=31 roots=7 first=605,72,-4:dark_oak_log,…`（`building-system.js:347`；`[BUILD_ORDER_PLAN]` summary 含 `axisReferenceDeferred:31`）
- 不再出现同坐标连续 `stateful_axis_no_*_reference`
- **注意**：31 步尾巴在存档里已 verified（2.2），续建时它们应被跳过；若仍有 `stateful_axis_no_*` 失败，一定落在**非尾巴**的轴向块上，属于补丁没覆盖到的情形，坐标抄全报后勤
- 新增 `[BUILD_TEMP_REFERENCE_FAILED]` 若出现，整行抄全（含 candidates）

**D. 假死判据**：（留空，等修缮 7 交卷后由开发填：心跳不漏拍、断线自动重连）

**E. 对账判据**

- `[BUILD_CONSTRUCTION_RUN] mode=resume runId=construction_run_d2f3cbb572ceb6b7 verified=<≈998> pending=<≈828> stateRepair=<≈37>`；**不得出现** `[BUILD_CONSTRUCTION_RUN_ABANDON]`
- 手术代放清单 `历史\第11轮-证据包\手术代放清单.txt` 134 块扫描应为 already_correct / verified（其中 133 根原木已在存档 verified 里）
- 存档里 37 步 `state_repair` 会被重做，含红石件：repeater 603,68,-10、sticky_piston 604,68,-10 / 604,68,-9、piston 604,69,-10、hopper 604,74,-2（`protected_block`）、comparator 604,75,-1、天花按钮 607,77,-5（`state_mismatch:face:wall!=ceiling`）——这些是老毛病点位，失败时如实记，不手术

**F. 终验**

- `correct / placements / obstructions` 终值 + 忠实度闸门（转发器 delay/facing 错放已在世界里，按闸门如实计，不改报告）
- run 转终态（`terminalState=COMPLETED`）；之后才排 #35 三连

### 五、第 5 步：文档（完成）

- `docs/协作/老板决策台账.md`（开发新建，#24–#39）连同本反馈、第 13 轮任务书一笔提交
- `docs/协作/说明.md` 历史归档表补第 12 轮一行（`:215`），协议正文未动

### 六、收尾说明

- 本轮零游戏：未起服务器、未起 bot、未连任何端口、未跑 RCON；工地 / 材料箱 / 别墅 / `construction-runs.json`（md5 前后一致）一格未动——**已证实**
- 提交顺序：`baa45b59`（合并）→ `57181975`（日志 + 单测）→ 本反馈与文档一笔；主仓在 `baa45b59` 与 `57181975` 各快进并测试一次，反馈笔后再快进一次（docs-only 不重跑）
- 后勤 UNKNOWN ①：**已了结**（会命中）；UNKNOWN ②：**门楼续建答不了**（尾巴已手术放好），日志已备好，等别的图纸或非尾巴块失败时说话

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/给CC.md`、`docs/协作/建造/CC反馈.md`（第 12 轮）、`docs/协作/后勤/CC反馈.md`（第 6 轮，只读）、`docs/协作/老板决策台账.md`、`docs/协作/历史/第11轮-门槛微调与门楼选址开工.md`、`docs/协作/历史/第11轮-证据包/cc-r11-fort-driver.js`、`package.json`、`systems/construction-run-store.js`（全文）、`systems/building-system.js`（`prepareConstructionRun` / `constructionRunCriteria` / `decideConstructionRunStart` / `reconcileConstructionRun` / `mergeRuntimeAndStoredSteps` / `applyConstructionExecutionStepsForRun` / `BUILD_ORDER_AXIS_DEFERRED` 处）、`systems/construction-compiler.js`（`compile` / `createPlanId` / `createStableStepId` / `createBlueprintHash`）、`actions/build.js`（`placeBlock` 临时参照分支、`ensureTemporaryPlacementReference`、`temporaryPlacementReferenceOffsets`、`formatPos`）、`tests/axis-reference-order.test.js`、`tests/actions.test.js`（轴向用例）、`tests/construction-run-store.test.js`（样式）、`tests/construction-resume-verifier.test.js`（头部）、`data/memory/construction-runs.json`（只读 + 副本）、`data/community-builds/cache/blueprints/github-ifnullthenvoid-fort-wall-gate.json`（只读）、`tests/fixtures/fort-wall-gate-axis.json`

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `baa45b59` | 合并 `feat/ops@0eef49b5`：`utils/site-planner.js`、`systems/construction-compiler.js`、`systems/building-system.js`、`package.json`、新增 `tests/axis-reference-order.test.js`、`tests/fixtures/fort-wall-gate-axis.json` |
| `57181975` | `actions/build.js`（+10，日志）、`tests/actions.test.js`（+12，断言）、新增 `tests/construction-resume-plan-id.test.js`（160 行）、`package.json`（主链 +1 组、脚本 +1） |
| （随后一笔） | 本反馈、`docs/协作/建造/给CC.md`、`docs/协作/老板决策台账.md`（新）、`docs/协作/历史/第12轮-合并轮与证据包与假死线索.md`（新）、`docs/协作/说明.md`（+1 行） |
| （主仓） | `D:\code\MC` main 快进 `9772e2b6 → baa45b59 → 57181975`，反馈笔后再快进一次 |
| （未入库） | scratchpad：`verify-resume-match.js`、`construction-runs.copy.json`（存档副本）、四份 npm test 输出 |

### 3. 执行的命令

`git status/log/branch/merge-base/diff/show/merge/add/commit`（add 全部显式指定文件）、`git -C D:\code\MC merge --ff-only …` ×2 + `log`、`npm test` ×3（本线：开场 51 / 合并后 52 / 终局 53）、`npm --prefix D:\code\MC test` ×2、`node tests/construction-resume-plan-id.test.js`、`node tests/actions.test.js`、`node --check actions/build.js`、`node verify-resume-match.js`（只读副本）、`cp`/`cmp`/`md5sum`（归档与副本核对）、`node -e` 若干只读分析。
未执行：`git add -A/.`、`git stash`、`git reset`、`git clean`、`git checkout --`、任何 sync、`npm install`、服务器/bot/RCON/端口、对 `D:\code\MC` 的 `cd` 或文件改动、对 `MC-fix`/`MC-ops` 的任何访问。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 | `npm test`（51 组） | 退出码 0 |
| 合并后 | `npm test`（52 组） | 退出码 0，74 行通过 |
| 主仓 @baa45b59 | `npm --prefix D:\code\MC test` | 退出码 0，74 行 |
| 单跑 | `construction-resume-plan-id`（3 组）、`actions` | PASS |
| 终局 | `npm test`（53 组） | 退出码 0，75 行通过 |
| 主仓 @57181975 | `npm --prefix D:\code\MC test` | 退出码 0，75 行 |

### 5. 当前最新判定

- 第 1 步合并：**完成**（`baa45b59`，零冲突，51→52 组，main 快进 + 主仓全绿）
- 第 2 步对账：**完成，会命中（已证实）**；3 组单测入链
- 第 3 步日志：**完成**（`57181975`，只加日志；y 轴只试正下方属实）
- 第 4 步清单：**完成**（第四节）
- 第 5 步文档：**完成**（台账随反馈笔提交；归档表补行）
- 门楼工地 / run / 材料箱 / 别墅 / 孤本与隔离目录：**一格未动**
- 主仓状态：main = 本线 HEAD（反馈笔快进后）

### 6. 生成文件 / 内存是否变脏

`latest-*` 三份与 `task-memory.json` / `world-memory.json` 仍是第 11/12 轮遗留的脏状态，本轮未碰、不提交；`construction-runs.json` md5 前后一致、mtime 未变；`logs/bot-current.log` 仍是第 12 轮冒烟第 2 段，本轮无新日志。其它线留在本 worktree 的未跟踪文件（`dashboard/`、`docs/协作/后勤/`、`docs/协作/修缮/`、`probe/`、`_backups/`、历史归档件）原样未动、未提交。世界零变化。

### 7. 是否建议提交

已提交两笔（合并、日志+单测）；本反馈及文档随后一笔；主仓快进三次。生成报告与 memory 不提交。

### 8. 剩余风险

- 31 步尾巴"已 verified"依据的是 9/1 17:40Z 那次 reconcile（第 12 轮已注明依据缓存区块）；续建时若 `verified=` 明显低于 998，说明世界实测与存档有出入，以 `[BUILD_CONSTRUCTION_RUN]` 实数为准
- `MC_WORLD_ID` 缺失 → run 作废是代码路径已证实的硬后果；保姆脚本/起 bot 命令必须带
- 后勤 UNKNOWN ②（悬空根节点靠运行时临时柱）门楼续建验证不到；`[BUILD_TEMP_REFERENCE_FAILED]` 只在非尾巴轴向块再失败时才会出现
- 37 步 `state_repair` 红石件/漏斗/天花按钮是老毛病点位，续建时可能再失败并触发保姆脚本的"同点三次放弃"
- 红石线当参照 4 处（后勤 2.5 严格口径）仍无真机证据
- main 现领先 `feat/fix`，修缮线第 7 轮合并时按常规处理
