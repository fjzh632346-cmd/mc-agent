# 建造 第 16 轮反馈

- 线: 建造
- 轮次: 16
- 日期: 2026-09-03
- 状态: 已完成（长活起法"活过会话退出"仍是 UNKNOWN——不是傀儡死了，是宿主进程自上轮起根本没退出过，试验没发生；已留新傀儡等下一次真正的退出）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / 99ae1fde（合并笔；本反馈与文档随后一笔，主仓快进到同一 HEAD）
- 实际运行模型 / 强度: Claude Fable 5.1（`claude-fable-5-1`），思考强度为会话默认档，本端读不到具体档位；任务书建议的 Opus 5 + high 未生效于本会话

---

## 一句话结论

后勤第九轮的朝向修补收进主干、测试全绿，门楼续建匹配离线复核仍命中；傀儡进程活得好好的，但这台机器上的工具宿主从上一轮到现在一直没退出过，所以"能不能活过退出"这题还没考成，旧傀儡已收、新傀儡已留。

## 白话摘要

第一件事，核傀儡。上一轮留的那个进程还在，心跳文件写到开场前四秒，看起来是"活过来了"；但仔细一查，两只本该随会话死的对照进程也还活着，而工具的宿主进程从上一轮之前就一直开着——也就是说中间根本没发生过"会话退出"，这不算证据。按任务书把旧傀儡全部收掉，另外留了一组新的（一个脱离会话起的、一个常规起的做对照），下次工具真的退出过之后再看，才能定论。

第二件事，合并。后勤第九轮那笔（按钮拉杆先等墙面、对账不再计较那些放置动作本来就定不了的状态）收进来，零冲突；测试从五十九组增加到六十组全部通过，主干仓库快进后再跑一遍同样全绿。

第三件事，复核续建匹配。后勤又改了一次施工顺序、计划编号又变了。用合并后的程序对真档案做了一遍只读核对：她认工程靠的是图纸指纹、落点、世界标识三样，编号不参与；三样都没变，门楼那条记录仍然命中，一千零三十三步已核验记录逐条对得上，档案文件一个字节没动。至于"新尺子下有多少处待修状态可以免修"，档案里没有存当时观察到的实际状态，离线算不出，只能按方块种类估个大概。

下一步：门楼续建仍等修缮线修站位；傀儡的最终证据要等一次真正的工具退出。本轮没有需要拍板的事。

## 需要老板拍板的问题

无

## 技术细节

### 〇、开场基线

- 开场 HEAD `c9dadb1b`，`npm test` 59 组全绿（81 行）——**已证实**
- 第 15 轮反馈整份挪到 `docs/协作/历史/第15轮-收档案瘦身与长活起法与清残留.md`（`cmp` 一致）——**已证实**
- 机器上另有一台 java 服务器（pid 3688，监听 **25566** / RCON 25576）和一只 `node bot.js`（pid 28828，连着 25566）——修缮线的，未动；25565 无监听——**已证实**

### 一、第 1 步：核傀儡（结论：试验未发生，UNKNOWN 维持；旧傀儡已收，新傀儡已留）

开场 14:32:55Z 的进程表（`Get-CimInstance Win32_Process`）：

```text
pid=3448  parent=18020 created=09/03 06:55:43  mc-r15-puppet-startprocess   （对照，Job 内）
pid=25124 parent=35460 created=09/03 06:55:43  mc-r15-puppet-cmdstart       （对照，Job 内）
pid=25740 parent=5620  created=09/03 07:03:13  mc-r15-puppet-wmi2           （WMI 包装起，Job 外）
wrapper 5620 alive? True
```

心跳 `.tmp/cc-r15-puppet-wmi2.heartbeat`（单值文件，只保留最后一次）：开场读 `2026-09-03T14:32:51.533Z`，当时 14:32:55Z，差 4 s；收掉前再读 `14:38:27.079Z`，当时 14:38:31Z——**傀儡活着、心跳新鲜，已证实**

**但判据的前提不成立**：

- 两只对照傀儡 3448 / 25124（都在 Job 里）**也活着**
- 本工具 shell 的祖先 `claude.exe` pid 35976 创建于 **09/03 06:41:39（本地）**，比第 15 轮起傀儡（06:55）还早，第 16 轮的 shell 仍挂在同一个 35976 之下；机器上 15 个 `claude.exe` 全部创建于 06:41 那一分钟（只有一个 07:32 的是本轮新开的子进程）
- 结论：**第 15 轮到第 16 轮之间宿主没有退出过**，Job 没关过，所以"活过会话退出"没被测到；对照傀儡活着**不能**推出"Job 关闭不杀进程"，机制推断不改——**已证实**（进程创建时间）
- 第 14→15 轮之间宿主确实退出过（第 14 轮全灭就是那次），当时还没有傀儡

处置：

- 按任务书 `Stop-Process` 收掉 25740、5620、3448、25124；复查进程表 0 只 r15 傀儡（复查时误把含过滤字串的自家 shell 也杀了一次，无副作用）——**已证实**
- **留新傀儡等下一次真正退出**（任务书没写这条，是我的判断：不留就永远测不到；随时 `Stop-Process` 可收）：`scripts\start-detached.ps1` 起 `mc-r16-puppet-wmi`（包装 PowerShell **34476** → node **32716**，心跳 `.tmp/cc-r16-puppet-wmi.heartbeat`，每 5 s）；对照 `Start-Process` 起 `mc-r16-puppet-control`（node **35248**，Job 内）。**下轮判据**：先看 `claude.exe` 的创建时间是否晚于 07:38（本地）——晚于才说明宿主退出过；然后 32716 活 + 心跳新鲜 且 35248 死 → 起法**已证实**；32716 也死 → 起法**证伪**，走「老板独立终端起」；宿主没退出过 → 继续等

### 二、第 2 步：合并 `feat/ops@11e1c03e`（完成）

- 预核：merge-base = `1f097855` ✔；`HEAD..11e1c03e` 恰 3 笔 `c97c0c96` / `42560c17` / `11e1c03e` ✔——**已证实**
- `git merge 11e1c03e` → 合并提交 **`99ae1fde`**，ort，**零冲突**：`utils/site-planner.js` +39、`systems/building-system.js` +44/−3、`tests/oriented-placement-state.test.js` 新 299 行、`package.json` +1 组、`dashboard/board-data.json` 5 处；本线的 `scripts/start-detached.ps1` 与文档原样保留——**已证实**
- `npm test`：**60 组**全绿，82 行；单跑 `oriented-placement-state`（11）、`construction-resume-plan-id`（3）、`construction-run-archive`（11）、`faithful-state-comparison` 全 PASS——**已证实**
- 主仓 `merge --ff-only` → `c9dadb1b..99ae1fde`；`npm --prefix D:\code\MC test` 全绿 82 行——**已证实**

### 三、第 3 步：离线复核门楼续建匹配（完成，命中；真档案只读）

脚本 `.tmp/cc-r16-recheck.js`：store 以 `isWritable: () => false` 打开（离线闸门关死，不迁移不写盘），生产链编译缓存图纸 @(599,67,-19)：

| 项 | 存档 run `d2f3cbb572ceb6b7` | 合并后编译 |
|---|---|---|
| 档案 | schema 2、50 条 run、11,474,440 B、md5 `ecaa0ae4…`；`MIGRATED` 0 行、`WRITE_DEFERRED` 0 行 | — |
| 状态 | `ACTIVE` / terminal null / steps 1863（verified 1033 / state_repair 38 / pending 792） | — |
| planId | `construction_plan_82c288475728` | **`construction_plan_b262d82f1416`**（与后勤报的一致；**不同**） |
| blueprintHash | `1022c03b…6582` | **相同** |
| blueprintRevision | `092eef09…` | **相同** |
| placementContext | origin 599,67,-19 / rot 0 / 无镜像 | 全等 |
| 步数 | — | 1912（1233 place + 333 脚手架放 + 333 脚手架拆 + 11 foundation_fill + 2 validate；比存档 1863 多的是离线空场地多算的脚手架/地基，第 13 轮已解释过，运行时不进对账） |
| 推迟 summary | — | axis 31/7、clickable 2/2（空场地口径；真机因块已在世界里为 0，第 14 轮已证实） |
| `constructionRunCompatibility` | — | **`{ok:true}`** |
| `findActiveCompatible` | — | **命中 `d2f3cbb572ceb6b7`** |
| place 步 id 落在存档 | — | **1233/1233**；verified **1033/1033** 全覆盖 |
| 主档 md5 前后 | `ecaa0ae4…` = `ecaa0ae4…` | **未变**；旁挂 49 份、备份 102,737,035 B 未动 |

**匹配靠什么**（读码，`systems/construction-run-store.js` `constructionRunCompatibility()`，与第 13 轮一致，本轮合并未改此函数）：只比 `blueprintId`、`blueprintHash`、`placementContext`、`world` 四项；**`planId` 不参与**（criteria 里带着但函数不读）；步骤按 `createStableStepId()` 的 id 对账，哈希不含序号——**已证实**。后勤 6/7/9 三次改序，planId 三次变（`d6e3eb…→56b2ef…→15d31d…→b262d8…`），命中三次不变——**已证实**

**尽力项（state_repair 38 步在新口径下有多少免比）**：存档步骤字段里**没有任何观察到的实际状态**（只有 `block/resolvedBlock` 期望态、`stateNormalization`、`retry.lastError`），**离线算不了**——**UNKNOWN**。按方块种类估（**合理推断**，以真机 reconcile 为准）：redstone_wire 11（四向连接键已排除，若只差连接态则免比）、piston 5 + sticky_piston 5（`extended` 已排除）、observer 1 / dropper 2 / redstone_torch 2（`powered`/`triggered`/`lit` 已排除）——这 26 步大概率转 verified；repeater 5（`delay` 仍比，后勤故意留）、comparator 3（`mode` 仍比）、hopper 2（`enabled` 排除但 `facing` 仍比）、dark_oak_button 2（`face`/`facing` 仍比）——这 12 步取决于真实差异键

### 四、第 4 步：文档（完成）

- `docs/协作/说明.md` 归档表补第 15 轮一行（`:235`）
- `docs/协作/老板决策台账.md`（#50–#52 与确认段）随本反馈一笔提交
- 他线文件显式提交：`docs/协作/后勤/给CC.md`、`docs/协作/后勤/CC反馈.md`（提交时的当前内容）、`docs/协作/历史/后勤第9轮-朝向状态错放三层归因.md`；主仓历史目录没有新的 `修缮第9/10轮` 归档件，不等
- 主仓 main 反馈笔后再快进一次

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/给CC.md`、`docs/协作/建造/CC反馈.md`（第 15 轮）、`docs/协作/老板决策台账.md`（diff）、`systems/construction-run-store.js`（匹配函数，复核）、`data/memory/construction-runs.json`（只读）、`data/community-builds/cache/blueprints/github-ifnullthenvoid-fort-wall-gate.json`（只读）、`.tmp/cc-r15-puppet-wmi2.heartbeat`

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `99ae1fde` | 合并 `feat/ops@11e1c03e`（5 文件 +406/−13） |
| （随后一笔） | 本反馈、`docs/协作/建造/给CC.md`、`docs/协作/老板决策台账.md`、`docs/协作/说明.md`（+1 行）、`docs/协作/历史/第15轮-收档案瘦身与长活起法与清残留.md`（新）、`docs/协作/后勤/给CC.md`、`docs/协作/后勤/CC反馈.md`、`docs/协作/历史/后勤第9轮-朝向状态错放三层归因.md` |
| （主仓） | main 快进 `c9dadb1b → 99ae1fde`，反馈笔后再快进一次 |
| （未入库） | `.tmp/cc-r16-recheck.js`、`.tmp/cc-r16-puppet-wmi.{log,heartbeat}`；`.tmp/cc-r15-puppet-wmi2.heartbeat` 留着 |
| （进程） | 收掉 r15 傀儡 4 只；留活 r16 傀儡：包装 34476 → node 32716（`mc-r16-puppet-wmi`）、对照 35248（`mc-r16-puppet-control`） |

### 3. 执行的命令

`git status/log/merge-base/diff/merge <显式提交号>/add <显式文件>/commit`、`git -C D:\code\MC merge --ff-only …` + 只读 `log`、`npm test` ×2（59 / 60 组）、`npm --prefix D:\code\MC test` ×1、单跑 4 组、`node .tmp/cc-r16-recheck.js`（只读）、`md5sum` ×3、`Get-CimInstance Win32_Process` / `Get-NetTCPConnection`（只读）、`Stop-Process` ×4（r15 傀儡）+ 1 次误杀自家 shell、`scripts\start-detached.ps1` ×1 + `Start-Process` ×1（r16 傀儡）、`cp`/`cmp`（归档）。
未执行：任何服务器/bot/RCON/端口操作、施工令、`schtasks`、对 `construction-runs.json` 的任何写、`git add -A/.`、`stash`、`reset`、`clean`、`checkout --`、sync、`npm install`、对 `MC-fix` / `MC-ops` 的访问。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 | `npm test` 59 组 | 退出码 0，81 行 |
| 合并后 | `npm test` 60 组 | 退出码 0，82 行 |
| 主仓 @99ae1fde | `npm --prefix D:\code\MC test` | 退出码 0，82 行 |
| 单跑 | 4 组 | PASS |
| 复核 | 真档案只读 | `{ok:true}`、命中、1233/1233、md5 不变 |

### 5. 当前最新判定

- 第 1 步 核傀儡：**试验未发生**（宿主未退出）；傀儡状态**已证实**活着；起法跨会话存活 **UNKNOWN 维持**；r15 傀儡已收、r16 傀儡已留
- 第 2 步 合并：**完成**（`99ae1fde`，59→60 组，main 快进 + 主仓全绿）
- 第 3 步 复核：**完成，命中**（planId 变、四项不变；档案未动）
- 第 4 步 文档：**完成**
- 门楼：一格未动；run `ACTIVE` verified 1033，续建等修缮 10（#48/#49）
- 进程表：本线零服务器、零 bot；仅 r16 傀儡 3 只（含包装）；修缮线的 25566 服务器与 bot 未动

### 6. 生成文件 / 内存是否变脏

`construction-runs.json` md5 前后一致（未动）；`task-memory.json` / `world-memory.json` / `latest-*` 仍是历史遗留脏状态（不提交）；`logs/` 未变；`.tmp/` 新增见上表。修缮线 `docs/协作/修缮/` 目录仍未跟踪，未动。

### 7. 是否建议提交

已提交合并笔；本反馈 + 文档 + 他线文件随后一笔；主仓再快进一次（本笔无 JS 改动，不重跑主仓测试）。

### 8. 剩余风险

- 长活起法的最终证据仍悬着：只有宿主真退出一次才能定论；r16 傀儡的判据写在第一节末尾
- 复查傀儡时用命令行关键字过滤会把自己的 shell 也匹配进去，`Stop-Process` 前要排除 `$PID`（本轮踩了一次）
- 尽力项的 26/12 分类是按方块种类估的，真机 reconcile 的 state_repair 数字以日志为准
- `.tmp/` 里第 15 轮的 102.7 MB 兜底副本还在，可随时删

