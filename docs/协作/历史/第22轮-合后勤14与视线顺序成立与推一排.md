# 建造 第 22 轮反馈

- 线: 建造
- 轮次: 22
- 日期: 2026-09-05
- 状态: 已完成（第 1–5 步全做完；真机按 #48 同砖三败自停于 `604,81,-10`；期间因一个新病因（锁被打断的回家任务占住）重启过一次 bot）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / `02394b1e`（合后勤 14）；本反馈与文档随后一笔
- 实际运行模型 / 强度: Fable 5.1（`claude-fable-5-1`，已证实）/ 思考强度本端读不到档位，UNKNOWN；全程未切模型

---

## 一句话结论

后勤线两样修复在门楼真身上都成立了：两个活塞一次放对朝向，红石火把没有再提前放；上一轮"活塞莫名变对、莫名消失"的谜也解开了（整排活塞被通电的那只往北推了一格）；但她随后从屋顶掉到地面后再也爬不回去，一块砖连撞三次停手，另外发现一个"被打断的回家任务把锁占死、之后每次开工都撞锁"的新毛病。

## 白话摘要

先把后勤线交来的五批代码收进主线，六十三项测试全部通过，主仓跟着更新。离线核对了门楼的施工顺序：二十四个"通电源"（红石火把、按钮）确实排到了整栋楼最后，红石线没有被挪。

真机上她认出原工地，开场三个数与上一轮预估完全一致。头两块砖就是那两只活塞：她抬头、发包前把视线同步给服务器，两只都一次放对朝向，我用服务器命令核实过；三只上一轮卡在"伸出"状态收不回去的活塞，在她放邻格的瞬间被带动全部缩了回去，谜团二解开。谜团一也解开了：改前快照发现上一轮那只"装反"的活塞原封不动地躺在它北边一格——通电时被顶出来的那只活塞把整排往北推了一格，不是重放放对的，也不是她拆的。

之后她从屋顶掉到了地面，屋顶上剩下的砖都要先搭十来格高的柱子才够得着，她每次柱子搭一半就超时；三十七分钟里又盖了十三块，最后在一根高处活塞上连撞三次，脚本按规矩停手，我六秒内把她收了。期间冒出一个新毛病：她失败后系统自动派的"回家"任务被续建命令打断，可它的脱困动作还在跑、重新抓走了移动锁，之后每次开工都因为"锁被占"立刻失败，脚本一度把这误算成三败；我重启机器人清锁后才恢复。脚手架"隔空拆"这轮拿到了硬数据：四格半以内拆的十三块全干净，四格半以外拆的二十一块里十八块还在，收工按规矩清掉了二十四块残留土。

下一步：视线同发和供电源最后放两条可以定论交后勤；锁占死、隔空拆距离表、掉屋顶后爬不回去、失败即回家四条交修缮；红石通电顶活塞的老问题仍待老板拍板（问题 1）。

## 需要老板拍板的问题

### 问题 1：屋顶那只推错位的朝北活塞（`604,79,-11`，不在图纸格上）要不要拆掉？
- 背景：上一轮通电时，装反的那只活塞把整排往北推了一格，它自己现在停在图纸范围外的 `604,79,-11`，朝北、没通电。图纸不需要这格有东西；对账不会碰它（不是图纸格），但它离红石线只有一格，以后火把最后一步放上去通电时它可能又被顶动或者把邻格推走。
- 选项 A：允许我下轮开场用服务器命令把这一只活塞删掉（只删这一格，逐格记账）。代价：改世界一格，非图纸格。
- 选项 B：留着不动，等门楼盖到最后放火把时看它会不会再惹事。代价：可能重演"通电推一排"。
- 我的建议：A，因为它是上一轮事故的残留物，不属于任何图纸格，留着只有风险没有用处。

### 问题 2：她从屋顶掉下来之后就回不去了——要不要允许保姆脚本在"目标在高处、她在地面"时把她传送回屋顶？
- 背景：这轮两只活塞放完后五秒内她从屋顶（高八十）掉到了地面（高六十七），之后屋顶剩下的每块砖都要先搭十几格高的柱子，柱子每次搭到一半就超时，一块砖平均七分钟、三次里成一次，最后三败停手。修缮线在修站位问题但没排期到这一条。
- 选项 A：允许脚本在检测到"目标高于她十格以上且她连续两次站位超时"时把她传送到屋顶上离目标最近的实心格（只传送她，不放砖、不拆砖），记账。代价：算是人工干预，真机数据里要标注哪些砖是传送后盖的。
- 选项 B：不传送，让她自己爬，接受每轮只盖十几块。代价：门楼屋顶还有一百多块高处砖，按这个速度要十轮以上。
- 我的建议：A，因为传送她本人不违反"不代放砖"，而且能让后勤、修缮两线在剩下的高处砖上拿到更多真机数据。

## 技术细节

### 一、第 1 步：合后勤 14 `feat/ops@7c3af0a2`（完成，`02394b1e`）

- 预核：`git merge-base HEAD 7c3af0a2` = `fd4505fc` ✔；`HEAD..7c3af0a2` **7 笔**而非任务书说的 5 笔——多出的 `85b08696` / `53d577d8` 是后勤 13（图纸名派生、看板），之前没有进过主干，按可达性一起合进来属正常；`fd4505fc..HEAD` 主干侧只有修缮 13 `edfbb642` + 文档笔，`actions/build.js` 是唯一双方都动的代码文件——**已证实**
- `git merge 7c3af0a2`：**零冲突**（`ort` 自动合并 `actions/build.js`），16 文件 +1122/−31——**已证实**
- grep：`[BUILD_PLACE_LOOK_SYNCED]` 在 `actions/build.js:4579`，`sendPlacementLookPacket` 调用 L4578，紧接 `BUILD_ORIENTED_PLACE_KEEP_YAW`（L4570）之后、`_genericPlace`（L4580）之前 ✔；函数定义 L5052 / L5083；`REDSTONE_POWER_SOURCE_BLOCKS` 在 `utils/site-planner.js:1381`（导出 L1462）✔；`FIXED_SCALE_NAMED_BLUEPRINTS`（`systems/building-complexity.js:184`）含 `small_house` / `two_story_wood_house` / `simple_farmhouse` / `garden_manor` 四个 id ✔；导出表并集：`orientedPlacementLookDirection` / `placementLookAngles` / `sendPlacementLookPacket` / `placeBlockAgainstReference`（L8116–8119）与修缮 13 的 `verticalAccessStandColumnSupport` / `hasClearScaffoldColumnBelow`（L8126–8127）、修缮 12 的 `selectTruncatedVerticalAccessPreviews`（L8134）全在——**已证实**
- `npm test` **63 组**退出码 0（`package.json` 并集 63 条）；单跑 `oriented-placement-state` / `axis-reference-order` / `clickable-reference-order` / `blueprint-building` / `intent-blueprint-names` / `actions` 均退出码 0——**已证实**
- 离线复核顺序（`node` 只读，用存档里门楼 run 的 `frozenBlueprintIR` + `placementContext` 过 `ConstructionCompiler.compile`）：planId **`construction_plan_62e98b560881`** ✔；1912 步，最末 24 个放置步全是供电源（`redstone_torch` 3 / `redstone_wall_torch` 1 / `dark_oak_button` 20），之后只剩 `walkability_validate` / `validate` 两步；首个供电源步 index 1886、最后一个非供电源放置步 index 1885；供电源之后 `redstone_wire` 0 步——**已证实**。续建对账干跑没有现成入口，跳过，靠第 3 步开场三个数
- 主仓：`git -C D:\code\MC merge --ff-only` 从 `e49e4e22` 快进到 `02394b1e`；`npm --prefix D:\code\MC test` 63 组退出码 0——**已证实**

### 二、第 2 步：改前快照（起服未起 bot，只读；**已证实**）

服务器 `start-detached.ps1` 起，java pid 19168，`Done (3.436s)`；`time set day`（原 8980）、`forceload add 588 -26 611 5`、`doDaylightCycle` true。`execute if block` 逐格（每格试 air / piston / piston[facing=down,extended=true|false] / piston[facing=north] / piston_head / moving_piston）：

| 格 | 改前 | 备注 |
|---|---|---|
| `604,79,-12` | air | |
| **`604,79,-11`** | **`piston[facing=north,extended=false]`** | 不在图纸格（存档核对）；就是第 20 轮那只装反的活塞 |
| `604,79,-10` | `piston[facing=down,extended=true]`，`604,78,-10` = piston_head | 第 21 轮只查到 `-9` 有头，没查 `-10` |
| `604,79,-9` | `piston[facing=down,extended=true]`，`604,78,-9` = piston_head | |
| `604,79,-8` / `-7` | air / air（`604,78,-8` / `-7` air） | 图纸 piston facing=down，pending |
| `604,79,-6` | `piston[facing=down,extended=true]`，`604,78,-6` = piston_head | |
| `603,80,-5` | air | 火把待放 |
| `603,80,-10..-6`、`604,80,-5`、`603,79,-4` | `redstone_wire[power=0]` 全部 | 零电 |
| `603,75,-4` / `603,77,-4` | redstone_torch（第 20 轮已放、verified） | 下层电路的源，与屋顶无关 |

### 三、第 3 步 0.：第 20 轮日志取证（`.tmp/cc-r20-bot-log-2026-09-05T0123Z.log` 只读）

- ① `604,79,-10`：整份日志只有**一次** `BUILD_STEP`（01:44:32 index=493），02:06 之后除 `TASK_TICK` 外**零行**提到它，没有任何重放 / `BUILD_PLACE_RETRY` / 成功放置行——「重放放对了」**不成立，已证实**
- ② `604,79,-8` 的 `kind=clear` 步（02:06:26）：三次站位（`607,76,-7` / `603,78,-11` / `605,78,-11`）全部 `stand_move_timeout`，日志里 `[BUILD_CLEAR_*]` 标签 **0 行**（代码里清障成功会打 `BUILD_CLEAR_*`，`actions/build.js` L262–426），只有脚手架的 `BUILD_STAIR_TEMP_REFERENCE_CLEAR`——「她拆到了一次」**不成立，已证实**
- ③ 02:15:40 对账前后没有逐格 `blockAt` 读数日志（`BUILD_STATE_REPAIR` / `RECONCIL` 标签 0 行）——UNKNOWN
- **真正的解释（合理推断，由改前快照坐实关键一环）**：02:03 火把通电时，`604,79,-7` 那只朝北的活塞伸出，把 `-8`（朝下、当时未伸）、`-9`、`-10` 三只未伸出的活塞**整排往北推了一格**：`-8`→`-9`、`-9`→`-10`、`-10`（朝北）→`-11`；`-7` 的头占了 `-8`。推完之后各格再通电伸出：`-9`、`-10`（现在都是原来朝下的活塞）头到 `604,78,-9` / `-10`。所以 02:15 对账读到 `-10` 是朝下（记 verified 没错）、第 21 轮读到 `-10` 朝下伸出、`-11` 有一只朝北未通电的活塞（红石线到 `-10` 为止）。`-7` 活塞本体和 `-8` 的头为什么在第 20 轮 02:06 之后、第 21 轮开场之前消失——**仍 UNKNOWN**（第 20 轮 02:29 收工 RCON 没复查 `-7`；日志无拆除行）

### 四、第 3 步：续建真机（07:28:37Z–08:04:59Z 施工，中途 07:41 重启 bot 一次）

**ONLINE-GATE**（**已证实**）：bot pid 29676（父 38716 包装，`Get-NetTCPConnection` 只连 25565）；`[端口] 使用 .env 指定端口: 25565` 07:27:50.792Z、`[TickLoop] started` 07:27:51.838Z、`[Bot] 上线成功` 07:27:51.840Z、`[DEBUG_STATUS] {"botUsername":"LinXia",…}` 07:28:37.090Z。保姆脚本 `.tmp/cc-r22-fort-driver.js` = r20 + 三处改动（回声加 `LOOK_SYNCED|POWER_SOURCE|CLEAR_UNREACHABLE|clearColumn|BUILD_CLEAR_`；缺料 `give` 全清单且**不计三败**；停手 `停止任务`→0.8 s→`tp 599 68 -19`→0.8 s→退出）；中途又补一处（`lock_already_held` 不计三败、等 6 s 再续建）。

**续建命中原 run + 开场三个数**（**已证实**）：
```
[07:28:43.705Z] [BUILD_CONSTRUCTION_RUN_RESUME_PLACEMENT] runId=construction_run_d2f3cbb572ceb6b7 blueprint=fort_wall_gate origin=599,67,-19
[07:28:46.536Z] [BUILD_CONSTRUCTION_RUN_RESUME_EVIDENCE] runId=construction_run_d2f3cbb572ceb6b7 sequence=38 before=1147/1864 after=1146/1864 processRestarted=true lanPort=25565 …
[07:28:46.587Z] [BUILD_CONSTRUCTION_RUN] mode=resume runId=construction_run_d2f3cbb572ceb6b7 verified=1146 pending=691 repair=0 stateRepair=27
```
与第 21 轮预估（1146 / 691 / 27）**完全一致**；`repair=0`——三只伸出的活塞没被当障碍（#71 ✔），全程 `kind=clear` 步 0、`BUILD_CLEAR_*` 0。

**供电源最后放（#64）**（**已证实**）：首批 `BUILD_STEP` = `604,79,-8 piston`（index 496）、`604,79,-7 piston`（497）、`605,80,-5 stone_bricks`（548）、`600,80,-2 iron_bars`（553）；整轮 28 个 `BUILD_STEP`（16 个不同目标）里 `redstone_torch` / `dark_oak_button` / `lever` **0 个**；火把 `603,80,-5` 收工仍 air（存档 pending retry 0）。门楼没盖到尾声，「放火把后活塞是否再顶出」本轮验不到。

**视线同发（后勤 14）——2/2 成立**（**已证实**，三行原样）：
```
[07:29:54.414Z] [BUILD_PLACE_ORIENT] target=604,79,-8 block=piston facing=down source=piston_facing look=0,0 pitch=1
[07:29:54.417Z] [BUILD_ORIENTED_PLACE_KEEP_YAW] target=604,79,-8 reference=603,79,-8 block=piston facing=down source=piston_facing sneak=false
[07:29:54.420Z] [BUILD_PLACE_LOOK_SYNCED] target=604,79,-8 block=piston facing=down sent=true pitch=1.571 yaw=-3.142
[07:30:04.511Z] [BUILD_PLACE_ORIENT] target=604,79,-7 block=piston facing=down source=piston_facing look=0,0 pitch=1
[07:30:04.514Z] [BUILD_ORIENTED_PLACE_KEEP_YAW] target=604,79,-7 reference=603,79,-7 block=piston facing=down source=piston_facing sneak=false
[07:30:04.516Z] [BUILD_PLACE_LOOK_SYNCED] target=604,79,-7 block=piston facing=down sent=true pitch=1.571 yaw=-3.142
```
两只都**无重试、无 `state_mismatch`**（整轮 `state_mismatch` 0 次，第 20 轮 13 次）；07:30:20 RCON `604 79 -8` / `-7` = `piston[facing=down,extended=false]` Test passed；存档两步 verified。她放 `-8` 时站在 `601.5,79,-10.6`（原地站着、不是刚走完位——正是第 20 轮失败的情形）。`sent=false` 0 次。`REPEATER_DELAY_TUNE` / `COMPARATOR_MODE_TUNE` 0（没轮到）。

**`604,79,-10` 对读**（**已证实**）：07:30:20（她放完 `-7` 后 16 s）RCON `604,79,-10` = `piston[facing=down,extended=false]`、`604,78,-10` = air；`-9`、`-6` 同样 `extended=false`、头消失。即：**三只"断电仍伸出"的活塞在她放邻格 `-8` / `-7` 的方块更新一到就全部缩回**——第 21 轮的「断电不缩回」是没有收到过邻格更新（合理推断：活塞只在方块更新时重新判定，RCON `setblock` 拔火把不会给 `-9` / `-10` / `-6` 发更新）。日志侧无 `blockAt` 读数（同上 UNKNOWN），客户端/服务器一致性只能从存档 verified + RCON 一致来判：**一致**。

**修缮 13 判据（`clearColumn`）**（**已证实**，4 次 `PREVIEW_EXTENDED` 全带 `clearColumn`）：
```
[07:50:38.338Z] [BUILD_VERTICAL_ACCESS_PREVIEW_EXTENDED] target=600,80,-2 truncated=5 extended=4 clearColumn=5 budgetMs=4000 order=vertical_1(clear)|vertical_2(clear)|vertical_0(clear)|vertical_3(clear)
[07:50:39.008Z] [BUILD_VERTICAL_ACCESS_CANDIDATE] target=600,80,-2 id=vertical_1_extended stand=601,78,-3 endpoint=601,78,-3 preview=success … previewSlices=16 pathNodes=22
[07:58:57.023Z] [BUILD_VERTICAL_ACCESS_PREVIEW_EXTENDED] target=604,81,-10 truncated=29 extended=4 clearColumn=15 budgetMs=4000 order=vertical_0(clear)|vertical_1(clear)|vertical_10(clear)|vertical_3(clear)
[07:59:01–07:59:13] vertical_0/1/10_extended preview=partial previewSlices=100 ×3；vertical_3_extended stand=608,79,-10 endpoint=607,79,-11 preview=partial_reachable_endpoint previewSlices=100
[07:59:13.527Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=604,81,-10 total=29 previewed=29 extended=4 legal=1 noPath=32 …
```
（08:02、08:03 对 `604,81,-10` 又两次同型：4 行 partial ×100 片。）即：`600,80,-2` 补算首个 `(clear)` 候选 0.7 s 成功；`604,81,-10`（y=81，她在地面 y=67）三次补算都把 4 s 预算耗尽、四行 partial——**修缮 12 预告的边界在门楼真身出现了**，`partial_reachable_endpoint` 被算成 legal=1 但随后站位 `607,79,-11` 走位超时。两盏 y82 灯笼没轮到（RCON 均 air，存档 pending）。

**新病因 1：被打断的回家任务把锁占死**（**已证实**，给修缮）：
```
[07:37:33.982Z] [task-manager] enqueue task=return_to_base id=2 reason=survival_system params={"reason":"TOO_FAR_FROM_BASE"}   （build#1 失败后 0.3 s）
[07:37:34.300Z] [action-lock] acquire type=movement owner=2 result=ok
[07:37:37.063Z] [WORKSITE_RETURN_CANCELLED] task=return_to_base#2 returnReason=TOO_FAR_FROM_BASE trigger=player_build_command
[07:37:37.075Z] [action-lock] release type=movement owner=2 result=ok
[07:37:37.078Z] [Task:return_to_base#2] interrupt: player_build_command
[07:37:38.890Z] [action-lock] acquire type=movement owner=2 result=ok        ← 已被打断的任务的脱困链继续跑并重新拿锁
[07:37:38.330Z] [ESCAPE_RUNG_FAILED] taskId=2 key=return_to_base rung=nearby_exit reason=move_timeout
[07:37:45.602Z] [action-lock] acquire type=movement owner=2 result=ok   …（此后再无 owner=2 release）
[07:38:28.881Z] [task-manager] fail task=build_blueprint id=4 reason=lock_already_held
[07:38:39.516Z] [action-lock] acquire type=movement,inventory,building owner=5 result=lock_already_held currentOwner=2
```
之后 build#4–#11 共 **8 次** `lock_already_held`（连 `停止任务` + tp + 续建都清不掉）。保姆脚本按"最近一步目标 `602,68,-7` 三次"误判 `GIVE_UP`（07:38:58，`SHUTDOWN` → tp 回工地）；我 07:40 打补丁（`lock_already_held` 不计三败）重起脚本仍撞锁，07:41 `Stop-Process` 本线 bot（pid 29676，按端口 25565 与父进程核对）重起（pid 34164）后锁清、续建正常。第一段日志另存 `logs/bot-2026-09-05-00-27-49.log`（未丢）。

**新病因 2：从屋顶掉到地面后回不去**（**已证实**现象，掉落原因 UNKNOWN）：位置采样 07:30:04 `604.66,80.00,-10.50` → 07:30:09 `600.50,67.00,-5.18`（5 s 内下降 13 格，血 20→19.5→后来 15.7，无摔落日志）。之后每个屋顶目标（y=79–81）从地面起：`BUILD_INITIAL_MOVE_DEFERRED_FOR_TEMP_REFERENCE`（`actions/build.js:688`，铁栏杆要先放临时参照）→ 临时参照的站位 `601,78,-3` 等用普通走位（不带脚手架方案）→ `MOVE_PATH_STATUS status=timeout allowList=0 excl=1234` → `stand_move_timeout_blacklisted`（整轮 30 次）→ `VERTICAL_ACCESS_CANDIDATES total=0 legal=0`（`FRESH_REPLAN_SKIPPED`，21 次 legal=0 都是这种 total=0 的，不是截断）。`600,80,-2 iron_bars` 三次才成（07:37 / 07:50 失败，07:51 成功）；`604,81,-10 piston` 三败（08:00:02 `placement_still_unreachable_after_xz_move`、08:02:14 `move_timeout`、08:04:54 `placement_still_unreachable_after_xz_move`）→ 存档 `terminal_failed retry.count=3` → 脚本 `GIVE_UP` → 08:04:59 tp 回 `599.5,67,-18.5` → 08:05:05 我 `Stop-Process` bot（6 s 内，**停工即收做到了**）。

**隔空拆取证（给修缮 14）**（**已证实**，34 条 `BUILD_STAIR_TEMP_REFERENCE_CLEAR` 逐条配 5 s 位置采样算眼睛到目标格中心的距离，收工 RCON 扫 36 格）：

| 拆除时距离 | 条数 | 收工仍是 dirt | 例 |
|---|---|---|---|
| ≤ 4.5 格 | 13 | **0** | `601,68,-8` 1.8 格、`600,77..79,-2` 2.5–3.8 格、`603,75/76,2` 0.2–2.1 格 |
| > 4.5 格 | 21 | **18** | `598,69..73,-18` 10–13 格（她在 `601.5,79,-10.6`）、`598,68..72,-20` 18–20 格（她在 `601.5,79,-3.5`）、`601,67..70,2` 6.5–9.4 格；4.6–5.5 格的 `601,71/72,2`、`608,77,-2` 三条例外地清掉了 |

另 8 格残留（`600,67..70,-5`、`601,67..70,-17`）是逃生柱 / 脚手架放了但从未有 CLEAR 行。`BUILD_VERTICAL_ACCESS_CLEANUP` 2 次、`SCAFFOLD_PLACED` 27 次、`vertical_access_cleanup_failed` / `protected_building_dig_blocked` 0。收工按 #65：36 格扫描 → 24 格 dirt（存档核对 **0 格在图纸格上**，12 格在 bounds 内但非图纸格）→ 逐格 `execute if block … dirt` 通过后 `setblock … air` → 复核 24/24 air。**三个数：查 36 / 删 24 / 跳过 0**。

**其它记数**（**已证实**）：`MOVE_PATH_STATUS` 36 行（`timeout allowList=0 excl=1233|1234` 19、`noPath allowList=0 excl=0` 8、`timeout allowList=5|8|10 excl=1233` 7、`noPath allowList=12 excl=1233` 2）；`CHEST_PATH_UNREACHABLE` 2（596/594,67,-23 各 1，`CHEST_PATH_ESCAPE_ATTEMPT waypoint=600.5/601.5,71,-4.5 reason=direct_move_timeout`，无 `_DROP` / `_REFUSED`——她在地面 y=67 时箱子路 noPath，不是落差）；`TASK_CREATED return_to_base` 5 次，距上一次 build 失败 0.3 / 26.2 / 77.2 / 0.3 / 30.6 s（0.3 s 的两次都是任务失败即触发，与第 20 轮同型）；`EVENT_LOOP_STALL` 12 次（2.1–4.9 s，全在每次 `build fort wall gate` 续建命令的图纸加载/编译处，`loopMaxMs` 最高 5096），施工中无；`BLOCKED_MATERIAL_SHORTAGE` 1 次（脚本 `give` 8 项后续建，未计三败）；`destination full` / `chest_item_not_found` / `lost connection` 0；`place_failed` 1（`605,81,-5 redstone_wire unstable_air`，重试成功）。保姆脚本 `chest_rescue_tp` 2、`rescue_tp_site` 1。

**三个数改前 / 改后**（**已证实**，存档 `reconciliation.at=08:02:21Z` + 收工存档）：verified 1146 → **1159**（+13：`604,79,-8` / `-7` piston、`605,80,-5`、`605,81,-10..-6` stone_bricks ×5、`605,81,-5` wire、`600,80,-2` / `608,80,-2` / `608,81,-14` / `600,81,-14` iron_bars、`602,68,-7` wire 重验）；state_repair 27 → 27（名单未动：repeater 5 / sticky_piston 5 / piston 5 / comparator 2 / hopper 2 / observer 1 / dropper 2 / dark_oak_button 5，全在 y=68–77 内部，本轮没轮到）；pending 691 → 677；terminal_failed 0 → **1**（`604,81,-10` retry.count=3）。run status `ACTIVE`。**下次首撞**：`604,81,-10 piston`（terminal_failed 优先重放；同类 y=81 高处、她若又在地面会重演）；其次 `600,81,-2 iron_bars`（pending retry 1）；两盏 y82 灯笼未到。

**收工**（**已证实**）：08:05 `Stop-Process` 本线 bot（pid 34164，父 27916，端口 25565；同时在线的 pid 17696 连 25566 是修缮线的，**原样未动**）→ `list` 0 人 → 残留清理 24 格 → `forceload remove all` → `doDaylightCycle` true → `save-all flush` → `mc-server.ps1 stop` 经 RCON → `All dimensions are saved` / `RCON Listener stopped` → 25565/25575 无监听 → 本线 node 进程零（修缮线 bot 与其服务器 java 9412 在跑，未动）。真机累计 37 分钟（07:28–08:05），未到 3 小时上限。

### 五、第 4 步：判定表

| 现象 | 本轮 | 去向 |
|---|---|---|
| 活塞 `LOOK_SYNCED sent=true` + RCON facing 对 + 对账 verified | 2/2（`604,79,-8` / `-7`），0 重试 0 mismatch | **后勤 14 视线同发在门楼成立**——定论 |
| `sent=false` | 0 | — |
| `sent=true` 仍 mismatch | 0 | — |
| 供电源步全在最末 / 火把没提前放 | 28 步里供电源 0 步；离线末 24 步全供电源 | **#64 成立**（"放火把后是否再顶出"没盖到那步，留待） |
| 火把中途被放 | 否 | — |
| y82 灯笼 `clearColumn` | 没轮到；`clearColumn` 在 `600,80,-2`（5）/ `604,81,-10`（15）出现 4 次 | `600,80,-2` 首个 `(clear)` 候选成功；`604,81,-10` 三次四行 partial ×100 片（预算耗尽）→ 抄段交修缮（修缮 12 边界 + 修缮 13 交接 2） |
| `SCAFFOLD_PLACED` 后 `CLEAR` 距离 >4.5 且仍 dirt | 21 条里 18 条 | 距离表交修缮 14 |
| 失败后 <10 s `return_to_base` | 2 次（0.3 s） | 记次交修缮 14 |
| 三个伸出活塞被对账当障碍 | 否（repair=0，`kind=clear` 0） | — |
| 开场三个数偏差 | 0 | — |
| （表外）打断的回家任务占锁 | 8 次 `lock_already_held` | **新病因**，交修缮（`return_to_base` 的 interrupt 不停脱困链、锁不释放） |
| （表外）掉屋顶后爬不回 | 三败停手 | 交修缮 + 老板问题 2 |
| （表外）朝北活塞残留在 `604,79,-11` | 快照坐实 | 老板问题 1 |

### 六、第 5 步：文档

- 第 21 轮反馈整份挪到 `docs/协作/历史/第21轮-合修缮13与清残留与拔火把.md`；`说明.md` 归档表补第 21 轮一行
- 台账 #70–#72 开发已追加，随本反馈一笔提交；他线文件显式提交：`docs/协作/后勤/CC反馈.md`（第 14 轮）、`docs/协作/历史/修缮第13轮-净空柱优先与量化推翻预设.md`、`docs/协作/建造/给CC.md`（本轮任务书）
- 主仓 main 提交后再快进一次

## 交接

**给后勤**：视线同发 2/2 成立（原地站着放也对），`state_mismatch` 从 13 → 0；供电源最后放离线 + 真机（28 步 0 供电源）都成立；`604,79,-10` 对读一致（RCON 与存档 verified 同为朝下）。活塞 `-9` / `-10` / `-6` 在邻格更新时自动缩回，不需要专门处理。

**给修缮**：① 锁占死日志段（上）；② 距离 vs 残留表（上）；③ 跑家 0.3 s ×2；④ `604,81,-10` 三次补算四行 partial ×100 片（`truncated=29 clearColumn=15`）；⑤ 掉屋顶 + 临时参照站位不带脚手架方案（`BUILD_INITIAL_MOVE_DEFERRED_FOR_TEMP_REFERENCE` → `temporary_reference_reach` 站位全超时）。

**世界现状**：屋顶 `604,79,-10..-6` 五只活塞全部朝下、未伸出、无头；`604,79,-11` 朝北活塞（非图纸格，问题 1）；`603,80,-5` air；`604,81,-10` air（terminal_failed）；残留土 0（36 格扫描后清 24）；她登出点 `599.5,67,-18.5`；昼夜规则与 forceload 恢复原状。

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`docs/协作/说明.md`（含本轮新增两条）、`AGENTS.md`、`docs/协作/建造/给CC.md`（第 22 轮）、`docs/协作/建造/CC反馈.md`（第 21 轮）、`docs/协作/老板决策台账.md`（#70–72）、`docs/协作/后勤/CC反馈.md`（头部 + planId 段）、`actions/build.js`（L262–426 清障标签、L680–700 临时参照延后、L1060–1125、L3747–3900、L4560–4585、L5034–5100、导出表）、`systems/construction-compiler.js`（L60–75、L398）、`systems/building-complexity.js`（L184–200）、`utils/site-planner.js`（L1381–1462）、`tests/construction-resume-plan-id.test.js`（L27–52）、`node_modules/mineflayer/lib/plugins/physics.js`（未再读，沿用第 20 轮结论）、`data/memory/construction-runs.json`（只读 ×5）、`.tmp/cc-r20-bot-log-2026-09-05T0123Z.log`、`logs/bot-2026-09-05-00-27-49.log`、`logs/bot-current.log`、`.tmp/cc-r22-driver-1/2/3.log`、`D:\code\MC-server\server.properties`（只读端口/密码，密码只进本会话进程环境）、`D:\code\MC-server\logs\latest.log`（尾部）

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `02394b1e` | 合并 `feat/ops@7c3af0a2`（零冲突，16 文件 +1122/−31，含后勤 13 两笔） |
| （随后一笔） | 本反馈、`docs/协作/说明.md`（+1 行）、`docs/协作/历史/第21轮-合修缮13与清残留与拔火把.md`（新）、`docs/协作/历史/修缮第13轮-净空柱优先与量化推翻预设.md`（新，他线）、`docs/协作/老板决策台账.md`、`docs/协作/后勤/CC反馈.md`、`docs/协作/建造/给CC.md` |
| （主仓） | `D:\code\MC` main 快进 `e49e4e22 → 02394b1e`（本反馈提交后再快进一次） |
| （未入库） | `.tmp/cc-r22-fort-driver.js`、`.tmp/cc-r22-driver-1/2/3.log`（UTF-16）、`.tmp/cc-r22-bot-log-1-2026-09-05T0727Z.log`、`.tmp/cc-r22-bot-log-2-2026-09-05T0741Z.log`、`.tmp/cc-r22-bot-stdout*.log`、`.tmp/cc-r22-server-start.log` |
| （世界） | 门楼 +13 步 verified；`604,79,-8` / `-7` 活塞朝下；`-9` / `-10` / `-6` 缩回；残留土清 24 格；其余未动（`604,79,-11` 朝北活塞留着） |

未改任何项目 JS 逻辑（合并带进来的除外；保姆脚本在 `.tmp/`）；未手改 `construction-runs.json`；未代放砖；未碰三只伸出活塞与活塞头。

### 3. 执行的命令

`git status/log/merge-base/diff/merge 7c3af0a2`（零冲突）、`git -C D:\code\MC merge --ff-only`、`npm test` ×2（开场 62、合后 63）、`npm --prefix D:\code\MC test` ×1、单跑 6 组、`node` 只读：离线编译门楼计划 ×1、解析存档 ×5、日志统计 ×3；`grep -n` 只读核对；`cp` 归档；`scripts\start-detached.ps1` ×6（服务器 1、bot 2、保姆脚本 3）、`scripts\mc-server.ps1 status/stop`、`Stop-Process` ×4（bot 29676 / 34164、保姆脚本 17264，均按端口 25565 + 父进程核对；修缮线 pid 17696 连 25566 未动）、`Get-CimInstance` / `Get-NetTCPConnection`；RCON（scratchpad `rc.ps1` 包装）：`time query/set day`、`gamerule doDaylightCycle`（查询 ×2）、`forceload add/query/remove all`、`execute if block` ×≈230（快照 ×~110、施工中核对 ×~30、残留扫描 36 + 复核 24 + 前置 24）、`setblock … air` ×24（残留土，#65）、`data get entity LinXia Pos/Health` ×~10、`list` ×3、`save-all flush`；保姆脚本自身：`tp LinXia` ×7（`initial_park` 3、`chest_rescue_tp` 2、`rescue_tp_site` 1、`stop_tp_worksite` 2）、`give` ×8、`bot.chat` `debug_status` ×3 / `停止任务` ×5 / `build fort wall gate` ×14。
未执行：`npm install/ci/update`、`git add -A/.`、`git reset/clean/checkout --`、`git stash`、进 `MC-fix`/`MC-ops`、连 25566、改项目 JS、手改存档、代放砖、拆活塞/活塞头。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 @e49e4e22 | `npm test` 62 组 | 退出码 0 |
| 合后勤 14 后 @02394b1e | `npm test` 63 组 + 单跑 6 组 | 均退出码 0 |
| 离线 | 门楼 planId `62e98b560881`、末 24 步供电源、wire 0 | 通过 |
| 主仓 @02394b1e | `npm --prefix D:\code\MC test` 63 组 | 退出码 0 |
| 真机 | 视线同发 2/2 PASS；供电源顺序 PASS；`-10` 对读一致；修缮 13 `clearColumn` 出现 4/4、成功 1/2 目标；三败停手 `604,81,-10`；新病因 2（锁占死、掉屋顶）；隔空拆 18/21 复现 |

### 5. 当前最新判定

- 第 1 步 合后勤 14：**完成**（`02394b1e`，零冲突，四处 grep 齐，63 组全绿，planId 与顺序复核通过，主仓快进 + 全绿）
- 第 2 步 改前快照：**完成**（表见上，发现 `604,79,-11` 朝北活塞）
- 第 3 步 取证三问：**完成**（重放不成立、拆到不成立、对账读数 UNKNOWN；真因 = 推一排）；续建：**完成**（三个数一致、活塞 2/2、火把 0 步、三败自停、停工 6 s 内收、残留 36/24/0）
- 第 4 步 判定表：**完成**
- 第 5 步 文档：**完成**（本笔）

### 6. 生成文件 / 内存是否变脏

`acceptance/reports/latest-*`、`data/memory/task-memory.json`、`world-memory.json`：`npm test` 历史脏状态，未提交；`data/memory/construction-runs.json`：本轮真机更新（verified 1159 / pending 677 / terminal_failed 1），未提交、未手改；`logs/`：两段本轮日志（另存 `.tmp/`）；`.tmp/*`、`_backups/`、`probe/`、`acceptance/reports/building-case1-*`、`data/community-builds/cache/*` 他线或往轮产物，未动。

### 7. 是否建议提交

已提交合并一笔；本反馈 + 文档 + 他线文件随后一笔；主仓再快进一次。`data/memory/*`、`logs/*`、`.tmp/*` 不提交。

### 8. 剩余风险

- `604,81,-10` 已 terminal_failed，下次续建首撞它；她若仍从地面起，补算四行 partial 会重演——不解决"掉屋顶回不去"（问题 2 或修缮排期），高处砖每轮只能盖十来块
- 锁占死是硬 bug：任何一次 build 失败 → 回家任务 → 被续建打断 → 之后全部开工失败，只能重启 bot；修缮 14 不修的话下轮大概率再撞，保姆脚本已改成不计三败但也只能靠重启
- `604,79,-11` 朝北活塞紧邻红石线（问题 1）
- 门楼没盖到放火把那一步，"火把最后放后活塞是否再顶出"未验
- 隔空拆 4.6–5.5 格三条例外地清掉了，边界值给修缮 14 参考，未深究
