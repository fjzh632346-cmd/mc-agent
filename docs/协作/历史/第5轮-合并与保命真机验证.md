# 建造 第 5 轮反馈

- 线: 建造
- 轮次: 5
- 日期: 2026-08-31
- 状态: 已完成
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / ed0303ba（反馈文件随后一笔提交）

---

## 一句话结论

两处裁决落实完毕，两条线已经合并到一起、全部四十二组测试逐组全绿、四组老红灯确认早已治好，保命逻辑的真机验证也做了大半——她被打到血危时真的会丢下战斗撤退，甚至死过一次重生之后还能接着盖那栋房子；唯一没合上去的是那个两个半月没动过的老主干，它所在的目录里还堆着另一批没归档的旧改动，得单独安排。

## 白话摘要

第一件事，落实您的两个裁决。那处"状态比较放宽"已经入库，配了十九个测试钉死它：十种游戏自算状态的豁免逐项有用例，八种真实差错（放错、放反、漏放）逐项验证照样拦住，门合页那一处唯一的让步在用例里显式标注了原因。"别墅提取盒"已按原文留档后撤销，那个文件现在和仓库记录完全一致；顺带评估了之前"禁止全量重算素材库"的禁令——理论上可以解除，但建议解除前先做一次带备份的对照验证，十分钟的事，下轮顺手做。

第二件事，合并。查清了家底：优先级线其实是从建造线上分出去的，独有的工作只有两笔，所以合并很干净——只有一处文件冲突（两边都写过同一份说明文件），已按新版协议的指路条解决。合并后四十二组测试逐组跑了一遍，全绿；您之前听说的"四组一直红着"确认早在上一轮清账时就顺带治好了，不用再单开一轮。仲裁测试也已加进日常测试清单。但"合回老主干"这半步卡住了：老主干检出在最早那个项目目录里，那边还躺着三十三处没归档的旧改动，直接合并会被安全机制拒绝（已实测确认，未造成任何改动）。建议照上轮清账的方子，给那个目录单独开一轮。

第三件事，保命逻辑真机验证。核心结论：**打起来她真的会跑**——血量危急时正在进行的战斗被立刻打断、撤退任务带着"紧急"标记上位，整轮验证里发生了两次，一次没漏。旧的逃生本能也规矩了，全程没有再抢过正规任务的方向盘。更狠的是一个计划外事件：她在一处洼地里撤退连续失败、被围殴致死，重生之后跑官方续建检查——**通过**，盖了一半的房子照样认得。这比原计划的验证力度还大。两项没验成的：「饿到危急会不会吃」（喂饥饿的手法没把她饿到位，场景没搭起来，不是系统问题）和「撤退被追防抖」（观察到的是另一个问题：洼地地形里撤退反复失败打转，这是个新发现的真实风险，建议列入下轮候选）。

## 需要老板拍板的问题

### 问题 1：老主干怎么办？

- 背景：合并的最后半步是把合好的成果推回主干。但主干检出在最早的项目目录里，那边有三十三处没归档的旧改动（六月时期的熔炼、记忆工具等等），其中六个文件恰好挡在合并路径上，安全机制拒绝覆盖（已实测，那边一个字节没动）。这堆旧改动和上一轮清掉的是同一类东西——干完没归档的活。
- 选项 A：**照上轮清账的方子，给那个目录单独开一轮**：逐个盘点、能归档的归档、再合并主干。代价：多一轮；好处：又一批家底进保险箱，主干真正跟上进度。
- 选项 B：**先不动主干**，两线继续在合并后的分支上干活。代价：主干永远停在两个半月前，那个目录里的旧改动继续无人认领；好处：零风险零成本。
- 我的建议：**A**。上轮清账证明这类烂账里往往埋着已入库代码依赖的"另一半"，越拖越难考据。

## 技术细节

### 〇、本轮游戏占用时段（供两线档期协调）

真机验证约 UTC 04:24–04:35（本地 12:24–12:35 前后）。在线玩家仅 LinXia + accept_tester。结束后 bot 已停、服务器已 RCON 优雅停服。

### 一、第 1 步：两处裁决落实。已完成

**① 状态放宽入库**（`3a6af774`）：`systems/faithful-community-validator.js` +22 照原样入库（此前经 SHA256 字节校验从未被改动过）。新增 `tests/faithful-state-comparison.test.js` 19 个测试：
- 豁免侧 11 个：importer 记账键（legacyId/legacyData/legacyVariant）、树叶 distance、梯子 distance/bottom、legacy 灯笼键（signal_fire/lit/facing）、酿造台 has_bottle_*、讲台 has_book、栅栏/墙/玻璃板/铁栏杆 的 north/south/east/west/up 连接、**门 hinge（唯一真实让步，用例与注释显式标注：游戏按点击位与邻块自动定合页，机器人无法控制，最坏情况门开向镜像；本轮裁决 A 正式认可）**
- 反向侧 8 个：楼梯朝向、门朝向（钉死豁免仅限合页）、栅栏门朝向（钉死连接豁免不漏到门）、灯笼 hanging（钉死灯笼豁免仅限 legacy 垃圾键）、半砖上下、原木轴向、放错方块、漏放——全部照拦
- 区分度已验证：豁免函数中和 → 豁免用例全红；恢复 → 全绿。已注册进两条测试链
**② 提取盒撤销**：手工按 HEAD 原文回填（未用任何被禁 git 命令），`git status` 确认该文件与 HEAD 逐字节一致、同文件其它内容零触碰。**留档如下（恢复时原样抄回）**：

```text
── 撤销前（工作区版本）──
        "box": { "minX": 28, "maxX": 62, "minY": 28, "maxY": 35, "minZ": 25, "maxZ": 60 },
        "reason": "The raw community schematic is a large hillside build; this extracts the upper real main-house region that retains doors, glass, roof, multi-story interior, and continuous stairs while avoiding the buried hillside mass that is not feasible for real-time acceptance building.",
── 撤销后（=HEAD）──
        "box": { "minX": 28, "maxX": 62, "minZ": 25, "maxZ": 60 },
        "reason": "The raw community schematic is a large hillside build; this extracts the real main-house region that retains doors, roof, multi-story interior, and continuous stairs while fitting the P8R build site budget.",
```

**全量 sync 禁令评估**（只评估未执行）：
- 已证实：`sources.json` 现与 HEAD 一致；运行时 `blueprintHash`（`construction-compiler.js:436` `createBlueprintHash`）只哈希 schemaVersion/id/name/bounds/blocks 等内容字段，**不含任何导入时间戳**——只要重导出的方块一致，续建指纹就一致
- 合理推断：现在跑全量 sync 会以原提取盒+同版导入代码重算别墅 → 方块应当一致 → 续建安全。残余不确定：缓存生成时点与当前导入代码之间的版本漂移无法凭空排除
- **建议**：禁令**有条件解除**——解除动作=一次受控验证（备份 cache → 全量 sync → 对比 pirbaba/别墅蓝图的内容字段一致性 → 一致即解禁、不一致立即回滚），约 10 分钟，建议下轮顺手做。本轮按指示未跑

### 二、第 2 步：合并。已完成（主干半步除外，见拍板问题 1）

**拓扑盘点**（已证实）：
```text
main                             394ae89c（2026-06-20，两线共同祖先，落后两线 0/103 与 0/87）
refactor/building-blueprint-ir-v1  103 笔领先 main
feat/priority                     基线=建造线的 2e642a5d（B-01 提交），独有 2 笔：
  0d013833 保命动作抢占战斗（EMERGENCY 档、入队带 reason/critical、
            战斗打断走专用清单、resolver 按数值抢占、睡觉降 LOW）
  aa67293e 优先级线第 2 轮反馈存档
```
优先级线本质是建造线的延伸分支 → 合并顺序无歧义：priority → building（`19a34fe5`），主干随后快进即可。

**冲突 1 处**：`docs/协作/CC反馈.md`（add/add）。两边意图：优先级线在分线重整前把第 2 轮反馈提交到了顶层路径；建造线持有分线后的迁移指路条。**解法：取指路条**（更新的协议裁决），他们的反馈原文完整保留于 aa67293e 历史与优先级 worktree 实体中。`systems/survival-system.js`、两个测试文件自动合并成功——双方改动分处不同区域（他们：入队参数与战斗打断集合；我们归档的：暂停恢复助手），已人工复核语义并由测试背书。

**42 组逐组整表**（`.tmp/cc-suite-table.json` 存底，全部 PASS）：

```text
action-lock 385ms | task-arbitration 376ms | tick-loop-world-state 103ms | movement-timeout 107ms
actions 65891ms | door-traversal 273ms | systems 494ms | equipment-system 109ms
auto-preparation 2222ms | crafting-system 2925ms | execution-tasks 1251ms | plan-executor 113ms
faithful-community-import 418ms | community-blueprint-sync 280ms | structure-use-classification 115ms
faithful-state-comparison 133ms | blueprint-ir 113ms | construction-compiler 197ms
construction-run-store 42401ms | construction-resume-evidence 121ms | construction-resume-verifier 154ms
world-diff-result 116ms | building-command-policy 491ms | blueprint-building 46819ms
storage-system 1042ms | farming-system 3322ms | exploration-system 1009ms | survival-system 444ms
feedback-cooldown 107ms | integration-stability 486ms | command-router 478ms | smelting-intent 154ms
memory 364ms | goal-system 129ms | planning-system 164ms | message-generator 114ms
llm-timeout 608ms | chat-history 98ms | bot-chat-loop 935ms | default-persona 927ms
llm-call-site-timeouts 915ms | acceptance-adapter 628ms
== 42 PASS / 0 FAIL ==
```

**4 组老红灯推断：证实**（已证实）。actions（sand→glass 在 `ac9d4af1`）、construction-compiler（床对依赖在 `13f6a2ec`）、blueprint-building（清障排序在 `683f510d`）、command-router（拾取夹具坐标在 `683f510d`）——全部被第 4 轮清账治好，**优先级线问题 1 自动了结，无需单开一轮**。

**仲裁测试进日常清单**（`ed0303ba`）：`tests/task-arbitration.test.js` 排在 action-lock 之后，实测跑得到且绿（376ms）。

**主干半步（意外，按任务书"停下来写进反馈"处理）**：main 检出于 `D:\code\MC`（33 条脏项：SmeltingSystem、memory 工具、foundation/ 等六月期未归档工作）。快进探测 `git -C D:\code\MC merge --ff-only`（已证实）被覆盖保护拒绝：

```text
error: Your local changes to the following files would be overwritten by merge:
  .claude/settings.local.json / systems/storage-system.js / systems/survival-system.js
  tests/storage-system.test.js / tests/survival-system.test.js / tests/systems.test.js
Aborting（那边 HEAD 与工作区零变化）
```

无法在不处理那批旧改动的前提下安全推进主干 → 拍板问题 1。

### 三、第 3 步：真机保命验证。四项各有结论

场景：专用服务器，accept_tester（op）以防晒僵尸+伤害指令搭建危机；日志时间线存 `.tmp/cc-survival-timeline.txt`。

**① 打起来会不会跑：已证实。** 两次完整复现：

```text
04:26:44 guard_player#5 启动（DANGER_NEARBY，持斧战斗中）
04:26:51 血量降至 10（阈值≤10）→ TASK_INTERRUPTED taskId=5（战斗被打断）
04:26:51 TASK_CREATED return_to_base {"reason":"CRITICAL_HEALTH","critical":true}
04:27:58 第二次复现：guard_player#10 → 血量 1 → INTERRUPTED → return#11 critical:true
```

**① 打起来会不会吃：UNKNOWN。** 饥饿效果（hunger 220 放大 20 秒）没把食物条压过阈值（饱和度缓冲吃掉了消耗，整个窗口 food=20）——场景搭建失败，非系统问题。下轮改用更长效果或分段消耗重试。

**①' 离家太远照旧打断不了战斗：合理推断。** 战斗窗口内 TOO_FAR_FROM_BASE 从未产生打断尝试；机制上它不在战斗可打断集合（`COMBAT_INTERRUPTING_PRIORITIES` 仅 CRITICAL_HEALTH/LOW_FOOD_CRITICAL），且危险评估优先级压过远离基地。未构造出"战斗中远离触发打断未遂"的直接对照，故不标已证实。

**② 撤退防抖：UNKNOWN，且有替代发现（新风险，候选下轮）。** 未观察到"打-跑摇摆"；观察到的是**洼地撤退死循环**：她出生点附近洼地里 `return_to_base` 连续失败（`no_safe_block_to_dig` / `move_timeout`），每约 30 秒重排一轮（#1→#2→#3→#4→#6→#7→#11），持续被围殴直至 **04:28:18 死亡**。撤退的仲裁层没问题（每次都正确上位），**执行层在爬不出的地形上无逃生手段**——这是比防抖更硬的真实风险。

**③ 保命打断后的断点续建：已证实（最强形式：死亡+重生）。** 死亡重生（keepInventory=true，物品无损）后跑官方续建验收：

```text
[acceptance] summary 1 PASS / 0 FAIL / 0 BLOCKED / 0 ERROR
[acceptance] building: PASS reason=building_resume_machinery_passed
```

**④ 旧版逃生守卫（顺带实证）：已证实。** 全程数十条 `LEGACY_ESCAPE_DEFERRED reason=managed_task_active`——旧逃生在受管任务持有移动权时一律推迟，从未劫持。

**盖房中途被保命打断的直接观测：UNKNOWN**（本轮建造任务在她远离工地被围殴期间从未进入放置阶段）；机制上 build_blueprint 属最低打断门槛集合、战斗尚且被打断，合理推断成立，留下轮与"饿到吃"一并补。

### 四、顺带记录

- 出生点洼地是天然的"逃生地形陷阱"复现场（坐标约 498,69,-48），下轮验证撤退执行层可直接取用
- `docs/协作/建造/给CC.md`（第 5 轮任务书）与本反馈随后一并提交入库
- 服务器/bot 已优雅停止；别墅 run 保持 ACTIVE（死亡重生未影响，官方 PASS 背书）

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/*`、`systems/faithful-community-validator.js`（statesMatch/isComparableStateKey 全链）、`systems/construction-compiler.js`（blueprintHash）、`systems/survival-system.js`（阈值与合并结果复核）、`git show 0d013833/aa67293e`、`git -C D:\code\MC status`（只读）、`logs/bot-current.log`、`acceptance/reports/latest-*`

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `3a6af774` | validator +22 入库 + tests/faithful-state-comparison.test.js（19 测试）+ package.json 注册 |
| `19a34fe5` | 合并 feat/priority（冲突 1 处，取指路条） |
| `ed0303ba` | 仲裁测试进日常清单（package.json） |
| （工作区） | sources.json 提取盒回填至与 HEAD 一致（无 diff 故无提交） |
| （随后一笔） | 本反馈 + 第 5 轮任务书 |

只读探针：`.tmp/cc-run-all-suites.js`、`.tmp/cc-suite-table.json`、`.tmp/cc-drive-survival-phases.js`、`.tmp/cc-survival-timeline.{js,txt}`、`.tmp/validator-r5-backup.js`（区分度实验字节备份，已复原验证）

### 3. 执行的命令

`git status/log/diff/show/add/commit/merge/merge-base/rev-list/worktree list`、`git -C D:\code\MC merge --ff-only`（探测，被拒，零变化）、`npm test` ×4、42 组逐组单跑、`node --check`、
`scripts/mc-server.ps1 start/stop/status`、`Start-Process node bot.js`（带 MC_PORT/MC_WORLD_ID）、accept_tester 危机驱动探针（summon/damage/effect/clear/give，全部指向 LinXia 与召唤怪，未触碰世界建筑）、官方 case3 验收。
未执行：`git add -A`、`git stash`、`git reset`、`git checkout --`、`git clean`、`--ours/--theirs 一把梭`、`npm install/ci/update`、全量 sync。

### 4. 测试结果

- 42 组逐组：**42 PASS / 0 FAIL**（整表见技术细节二）
- `npm test` 全量（含新增仲裁+状态比较组）：全绿，每笔提交后各一次
- 真机：战斗被危急撤退抢占 ×2 已证实；死亡重生后官方 case3 **PASS**

### 5. 当前最新判定

- 裁决①放宽入库+正反用例：**完成**；裁决②提取盒撤销+留档+禁令评估：**完成**
- 两线合并：**完成**（1 冲突已解、42/42 绿、4 组红推断**证实**、仲裁进清单）；主干快进：**卡住**（D:\code\MC 脏检出，拍板问题 1）
- 真机四项：跑=已证实、吃=UNKNOWN（场景未搭成）、防抖=UNKNOWN（替代发现：洼地撤退死循环+死亡）、断点续建=已证实（死亡重生后 PASS）

### 6. 生成文件 / 内存是否变脏

`latest-*` 被 case3 覆盖为 PASS 记录（有意）。`construction-runs.json` 正常更新。世界净变化：召唤的僵尸已清除、效果已清、她死亡重生一次（物品无损）、给予的牛排留在背包。原单人存档零变化。

### 7. 是否建议提交

已提交 3 笔+反馈笔。主干快进按拍板问题 1 处理。

### 8. 剩余风险

- **洼地撤退死循环**（本轮新发现，已致死一次）：撤退仲裁正确但执行层在爬不出地形上无手段、失败即重排无退避升级——建议列下轮根因候选（与既有的"清障盲走"同属执行层韧性）
- 「饿到吃」与「盖房中途被打断」两项真机观测仍欠，下轮可与上条并跑
- 主干未合，`D:\code\MC` 的 33 条旧改动无人认领（拍板问题 1）
- 全量 sync 禁令解除需先做受控验证（见技术细节一），未验证前维持禁令
