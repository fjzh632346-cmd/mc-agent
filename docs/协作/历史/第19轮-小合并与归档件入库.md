# 建造 第 19 轮反馈

- 线: 建造
- 轮次: 19
- 日期: 2026-09-04
- 状态: 已完成
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / 464f26b4（合并修缮 11 + 后勤 11）；本反馈与文档随后一笔
- 实际运行模型 / 强度: Claude Sonnet 5（`claude-sonnet-5`），与任务书建议一致；思考强度本端读不到具体档位，按会话默认档运行

---

## 一句话结论

这轮是纯收尾轮，没有开游戏：把修缮线和后勤线各自修好的两小批代码收进了主线，测试全绿，顺手把之前一直没归档的二十多份历史记录文件也补进了仓库。

## 白话摘要

修缮线这次修的是"搭脚手架时误伤自己搭的桥"和"走不通时不说原因"这两个小问题，代码收进来后测试全部通过。后勤线这次修的是"放置带朝向的方块时转错方向"和"红石延迟器、比较器放完不会自动调档位"这两个问题，代码收进来后测试也全部通过。两批代码合并时只撞了一次车——两边都在同一份测试清单里加了新测试项，我把两边的内容合并保留，没有丢东西。这轮完全在电脑上完成，没有连接游戏服务器，工地没有任何变化。最后把三条线以前攒下来的二十多份旧记录文件正式归档进了仓库，避免以后再丢失。下一步：建造线下一轮再收后勤和修缮线的新成果，然后继续盖门楼。

## 需要老板拍板的问题

无。

## 技术细节

### 一、第 1 步：合修缮 11 `feat/fix@a123b121`（完成）

- 预核：`git merge-base HEAD a123b121` = `ee30d137` ✔；`git log --oneline HEAD..a123b121` 恰 2 笔 `0c9682c2` / `a123b121` ✔；`git diff --stat ee30d137 HEAD -- actions/move.js tests/actions.test.js tests/scaffold-exclusion-path.test.js` 只有 `tests/actions.test.js`（+12，无关的诊断断言），另两个文件为空——**已证实**，与任务书预期一致
- `git merge a123b121`：**唯一冲突在 `package.json` 第 8 行 `"test"`**，与任务书预告完全一致。解法：取并集，`door-traversal.test.js && ` 后依次插入 `scaffold-allowlist-path.test.js && scaffold-exclusion-path.test.js && `，其余行照主干。核对 `git diff ee30d137 a123b121 -- package.json` 只改了这一行，没有别的 script 条目——**已证实**，不算逻辑冲突，未停手。合并提交 `fecef100`
- `grep -n "exclusionAreasPlace" actions/move.js`：**零个 `.push`**（白名单、排除集两条代价回调都没了）；`installScaffoldPlacementCellFilter` 定义（L301）与调用（L298）仍在；`trackMovePathStatus`（L521）与 `moveTo` 挂载（L384）都在——**已证实**，合对了
- `npm test`：**62 组**（`package.json` 的 `test` 脚本按 `&&` 切分为 62 段）全绿，退出码 0；单跑 `tests/scaffold-exclusion-path.test.js`（`scaffold exclusion path tests passed`）、`tests/scaffold-allowlist-path.test.js`（`scaffold allow-list path tests passed`）、`tests/actions.test.js`（`actions tests passed`）均退出码 0——**已证实**

### 二、第 2 步：合后勤 11 `feat/ops@79d8155e`（完成）

- 预核：`git merge-base HEAD 79d8155e` = `9454538a` ✔；`git log --oneline HEAD..79d8155e` 恰 3 笔 `e907697e` / `637c3535` / `79d8155e` ✔；`git diff --stat 9454538a HEAD -- actions/build.js tests/oriented-placement-state.test.js dashboard/board-data.json` 为空——**已证实**，主干在合并基之后没碰这三个文件
- `git merge 79d8155e`：**零冲突**（`ort` 策略自动完成），与任务书预期一致。`actions/build.js` +219/−?、`dashboard/board-data.json` +30/−?、`tests/oriented-placement-state.test.js` +412/−? 三文件共 647 行新增。合并提交 `464f26b4`
- `npm test`：**62 组**全绿，退出码 0；单跑 `tests/oriented-placement-state.test.js`（`oriented placement state tests passed`，应含 22 组新断言，未逐条清点断言数，只核实退出码 0 与文件行数 +412）、`tests/dashboard-lanes.test.js`（`dashboard lanes tests passed`）——**已证实**
- 只读复核：`grep -n "alignPostPlaceTunedStates" actions/build.js` → 挂载点在 `confirmPlacedBlock()` 第 4183 行，位于第 4178–4179 行的 `expectedPlacementStateMismatch` 放置闸门**之后**，与后勤所述一致——**已证实**
- 主仓快进：`git -C D:\code\MC merge --ff-only refactor/building-blueprint-ir-v1` 从 `2e2dc773` 快进到 `464f26b4`（7 文件 +891/−33）；`npm --prefix D:\code\MC test` 退出码 0，62 组全绿——**已证实**

### 三、第 3 步：他线归档件入库（完成）

按开发决定，把之前一直未跟踪的文档全部显式 `git add`：

- `docs/协作/历史/` 下 21 个早轮归档件：修缮第 1–7 轮、修缮第 11 轮、后勤第 1–7 轮、后勤第 10 轮、后勤第 11 轮（两份：`停手等建造17` 与 `朝向语义表落地与转了白转`）、优先级第 2 轮、优先级线退役存档、第 4 轮（清账与零候选与续建关卡，旧文件名未带"建造"前缀）
- `docs/协作/修缮/` 整个目录（`CC反馈.md`、`给CC.md`、`说明.md`、`指路条.md`）
- `docs/协作/后勤/说明.md`

均为纯文档，历史目录只增不删，未动其余未跟踪文件（`.tmp/`、`_backups/`、`probe/`、`acceptance/reports/building-case1-*`、`data/community-builds/cache/*`、`data/memory/construction-runs.json*`）——**已证实**（`git status --short` 核对）

### 四、第 4 步：文档（完成）

- 第 18 轮反馈整份挪到 `docs/协作/历史/第18轮-合修缮10与607盖上与灯笼卡预演.md`；`说明.md` 归档表补第 18 轮一行
- `docs/协作/老板决策台账.md` 核对：开发已追加 #57–#60，本轮随反馈一笔提交（无需我再改内容）
- 他线文件显式提交：`docs/协作/后勤/给CC.md`（后勤 12 任务书）、`docs/协作/后勤/CC反馈.md`（后勤 11 反馈）；主仓历史目录未出现修缮 12/后勤 12 新归档件，未等
- 主仓 main 已快进到 `464f26b4`

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`docs/协作/说明.md`、`AGENTS.md`、`docs/协作/建造/给CC.md`（第 19 轮）、`docs/协作/建造/CC反馈.md`（第 18 轮）、`package.json`（冲突段）、`actions/move.js`（grep 核对段）、`actions/build.js`（L4166–4190 `confirmPlacedBlock`）、`docs/协作/老板决策台账.md`（#57–60 核对）、`docs/协作/未跟踪归档件清单`（`git status --short` 输出）

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `fecef100` | 合并 `feat/fix@a123b121`（`package.json` 一行手工并集：`door-traversal → scaffold-allowlist-path → scaffold-exclusion-path`；`tests/actions.test.js`、`tests/scaffold-exclusion-path.test.js` 随合并带入） |
| `464f26b4` | 合并 `feat/ops@79d8155e`（`actions/build.js`、`dashboard/board-data.json`、`tests/oriented-placement-state.test.js`，零冲突） |
| （随后一笔） | 本反馈、`docs/协作/说明.md`（+1 行）、`docs/协作/历史/第18轮-合修缮10与607盖上与灯笼卡预演.md`（新）、21 个他线历史归档件（新）、`docs/协作/修缮/` 整目录（新）、`docs/协作/后勤/说明.md`（新）、`docs/协作/老板决策台账.md`、`docs/协作/后勤/给CC.md`、`docs/协作/后勤/CC反馈.md` |
| （主仓） | `D:\code\MC` main 快进 `2e2dc773 → 464f26b4` |
| （未入库） | `.tmp/`、`_backups/`、`probe/`、`acceptance/reports/building-case1-*`（他线探针产物）、`data/community-builds/cache/*`、`data/memory/construction-runs.json*` |

### 3. 执行的命令

`git status/log/merge-base/diff/merge a123b121/merge 79d8155e/add/commit`、`git -C D:\code\MC merge --ff-only` + `npm --prefix D:\code\MC test`、`npm test` ×3（开场 60 组、合修缮 11 后 62 组、合后勤 11 后 62 组）、单跑 `tests/scaffold-exclusion-path.test.js`、`tests/scaffold-allowlist-path.test.js`、`tests/actions.test.js`、`tests/oriented-placement-state.test.js`、`tests/dashboard-lanes.test.js`、`grep -n` 只读核 `actions/move.js`、`actions/build.js`、`node -e` 只读统计 `package.json` 脚本段数、`cp` 归档第 18 轮反馈。
未执行：任何 JS 逻辑改动（合并带进来的除外）、`npm install/ci/update`、`git add -A/.`、`git reset/clean/checkout --`、`git stash`、进 `MC-fix`/`MC-ops`、起服务器/bot、连 25565/25566。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 @2e2dc773 | `npm test` 60 组 | 退出码 0 |
| 合修缮 11 后 @fecef100 | `npm test` 62 组 | 退出码 0 |
| 同上单跑 | `scaffold-exclusion-path`、`scaffold-allowlist-path`、`actions` | 均退出码 0 |
| 合后勤 11 后 @464f26b4 | `npm test` 62 组 | 退出码 0 |
| 同上单跑 | `oriented-placement-state`、`dashboard-lanes` | 均退出码 0 |
| 主仓 @464f26b4 | `npm --prefix D:\code\MC test` | 退出码 0 |

### 5. 当前最新判定

- 第 1 步 合修缮 11：**完成**（`fecef100`，`package.json` 一行手工并集，grep 确认排除集/白名单回调都已删除，62 组全绿）
- 第 2 步 合后勤 11：**完成**（`464f26b4`，零冲突，`alignPostPlaceTunedStates` 挂载点位置核对与后勤所述一致，62 组全绿，主仓快进 + 全绿）
- 第 3 步 归档件入库：**完成**（21 个历史文件 + `docs/协作/修缮/` + `docs/协作/后勤/说明.md`，全部纯文档，清单见上）
- 第 4 步 文档：**完成**
- 本轮未进游戏：零服务器、零 bot、门楼工地未动一格

### 6. 生成文件 / 内存是否变脏

`acceptance/reports/latest-report.json`、`latest-report.md`、`latest-summary.md`、`data/memory/task-memory.json`、`data/memory/world-memory.json` 为跑 `npm test` 产生的历史脏状态（本轮未跑真机 acceptance），未提交，与往轮一致。`data/memory/construction-runs.json*`、`.tmp/*`、`probe/*`、`acceptance/reports/building-case1-*`、`data/community-builds/cache/*` 均为他线或往轮真机产物，未提交、未动。

### 7. 是否建议提交

已提交：合修缮 11 一笔（`fecef100`）、合后勤 11 一笔（`464f26b4`）；反馈 + 文档 + 归档件随后一笔（待提交）；主仓已快进两次。

### 8. 剩余风险

- 未逐条清点后勤 11 新增的 22 组 `oriented-placement-state` 断言内容，只核实了整体退出码与文件行数变化，若断言粒度有问题需要下一轮开发复核测试文件本身
- 本轮未进游戏，`604,77,-2 lantern` 的新病因（预演全 `preview_partial`）仍等修缮 12 的根治，建造 20 才会真机验证
- 后勤 12 的"放置时保住视线"6 行、修缮 12 的灯笼根治均在并行进行，与本轮无交叉，下一轮再收
