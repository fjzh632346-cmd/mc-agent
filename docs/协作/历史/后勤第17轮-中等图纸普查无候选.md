# 后勤 第 17 轮反馈

- 线: 后勤
- 轮次: 17
- 日期: 2026-09-06
- 状态: 等老板拍板（第 1 步找不到合适图纸，第 2、3 步依赖第 1 步的结果，本轮暂停在这）
- 实际运行模型 / 思考强度 / 是否 ultracode: **Sonnet 5 / medium / 未开 ultracode**（本轮是逐一核对几份候选图纸的块数与资格，属于「一支探针一次扫完」的普查，人工顺序核对比拆多个子代理更省事，所以没开）
- 分支 / HEAD: feat/ops / `5d0e0494`（本轮未新增提交——没有选定图纸，第 2、3 步无从下手，代码零改动）

---

## 一句话结论

给中等房子找了一圈图纸，现有能拿到手的素材里，没有一份落在您要的 600-850 块这个区间，需要您在两条路里选一条。

## 白话摘要

我把手头能查的图纸全部量了一遍块数：本地存的房子最大是双层小屋，四百六十多块；仓库自己会画的房子最大也就四百多块；社区那边缓存下来的几份房子，要么快两千块、要么四千多块，都比中等房子大得多。我还试着按规矩去接了一份新的社区房子（一份叫「简单小屋」的素材），结果下下来一看，那份东西造假——不是真的建筑素材，是拼凑出来的，系统的审查环节直接把它拦下了，而且它撑死了也就一百多块，够不着区间。找了一圈，600-850 块这一档目前是空的。往下要么去外面再找一份新的社区素材来源，要么把现成的双层小屋加盖一层凑到区间——两条路都会改到现有的素材清单或素材本身，所以停在这一步等您拍板，没有自己动手改。

## 需要老板拍板的问题

### 问题 1：600-850 块的中等民居图纸，从哪条路来？

- 背景：现有的房子图纸只有两档——基础的小木屋（一百八十多块）和双层小屋（四百六十多块），社区库里存的几份都超过一千块。中等房子这一档目前彻底是空的，需要新增一份素材才能填上。
- 选项 A：再找一个新的社区素材来源（去找一份公开的、块数合适的房子模型，走现在这套「先审查再入库」的流程接进来）。代价：可能要花一轮甚至更多轮去找、去试，找到的素材质量、能不能通过审查都不确定；风险小（新素材是加进去的，不会碰现有任何一份）。
- 选项 B：在现成的双层小屋基础上加盖一层或加一个附属房间，把块数凑到六百到八百五之间。代价：这属于新造一份素材（改现有图纸的块数据是红线动作，必须您点头才能动），做出来的房子好不好看、耗时多久说不准；好处是有现成的底子，比从零画一份快。
- 我的建议：选 A，因为咱们的红线明确写着「现有图纸块数据一字不动，要改造凑区间必须先拍板」——选 A 不用碰任何现有素材，风险最低；只是找到合适来源可能要多花一两轮功夫。若您赶时间要一份就位，也可以选 B，我这边随时能接着做。

## 技术细节

### 候选图纸块数谱（本轮离线量出来的，全部已证实）

| 来源 | 图纸 / id | 块数 | 结论 |
|---|---|---|---|
| 本地文件 `blueprints/*.json` | `small_house`（真实文件，非同名生成款） | 26 | 太小 |
| 程序生成 | `starter_shelter` | 56 | 太小 |
| 程序生成 | `castle_garden` | 96 | 太小 |
| 程序生成 | `garden_manor` | 91 | 太小 |
| 程序生成 | `simple_farmhouse` | 91 | 太小 |
| 程序生成 | `modern_villa` | 86 | 太小 |
| 程序生成 | `two_story_wood_house` | 102 | 太小 |
| 程序生成 | `simple_wood_cabin` | 183 | 太小 |
| 程序生成 | `simple_two_story_cabin` | 468 | 最接近，但仍差 132 块够不到 600 |
| 社区缓存（未跟踪） | `mcbuild-a-small-modern-house-16786` | 1185 | 太大，且**本轮查出它在索引里本来就是 `rejected_hard_gate`**（早先某轮同步遗留的记录，本轮未改动这条结论） |
| 社区缓存（未跟踪） | `mcbuild-wood-modern-house-15774` | 1893 | 太大，同上，索引里也是 `rejected_hard_gate` |
| 社区缓存（未跟踪，本地已有编好的文件） | `mcbuild-modern-house-on-a-hilltop-18658`（即别墅孤本） | 4431 | 太大（这就是台账里的别墅长跑用例） |
| 社区新源（本轮试接） | `github-interlude6465-hunterx-simple-house` | 未知，但结构声明只有 7×4×7（上限约 196 格，含空气） | **本轮增量同步已拦下**：`data/community-builds/cache/index.json` 记为 `rejected_format_or_version`，原因 `community_structure_json_without_real_file_provenance`——下载下来的 `.schem` 文件实际是纯 JSON 文本，不是真的 NBT 结构文件，系统的「忠实社区导入」校验器判定这是假素材，直接挡下。即使放行，声明的尺寸也远够不到区间 |
| 社区库其余未同步来源 | `github-dennis-muenster-castle-schematic` / `github-ifnullthenvoid-fort-wall-gate`（1233 块，已知）/ `github-ifnullthenvoid-fort-watchtower`（755 块，已知，**唯一落在区间内的**） | — | 三份都是 `castle_garden`（城堡/瞭望塔）品类，**不是民居**，任务书要的是「中等民居」，watchtower 虽然块数（755）正好落进区间，但用途和风格都不对，不建议冒用 |

### 各数据点的证据来源（已证实 / UNKNOWN 标注）

- 本地与程序生成的块数：**已证实**——`node -e` 直接 `require` `BlueprintLoader` 与 `ProceduralBlueprintGenerator`，逐个 `generate()` 后数 `blueprint.blocks.length`，未进游戏、未联网
- `mcbuild-*` 三份社区缓存块数：**已证实**——直接读 MC-ops 与主仓（只读，未改动）已编好的 JSON 文件计数
- `mcbuild-a-small-modern-house-16786` / `mcbuild-wood-modern-house-15774` 的 `rejected_hard_gate` 状态：**已证实**——`data/community-builds/cache/index.json` 里的 `rejected` 数组本轮同步前就有这两条记录（本轮的 `--only=` 增量同步没有触碰这两个 id，只是把已有状态原样带出来）
- `fort-wall-gate`（1233）/ `fort-watchtower`（755）块数：**已证实**——这两份已经在主仓（建造线的 worktree）编译好，本轮只读取数字作参考，**未touch 主仓任何文件、未跨线改动**
- `hunterx-simple-house` 的假素材判定：**已证实**——本轮实际跑了 `npm run syncCommunityBlueprints -- --only=github-interlude6465-hunterx-simple-house`，回执与 `index.json` 里的 `rejected_format_or_version` 记录、下载下来的原始文件内容（纯 JSON、7×4×7 声明尺寸）都对得上
- `dennis-muenster-castle-schematic` 是否可能落在区间：**UNKNOWN**——本轮没有同步它（属于城堡品类，不是民居，就算块数合适也不该用来充民居）

### 本轮唯一一次网络/同步动作

`npm run syncCommunityBlueprints -- --only=github-interlude6465-hunterx-simple-house`（MC-ops，`feat/ops` 分支）。**严格 `--only`，未触碰其余 7 个来源，未全量**。结果：下载成功，解析/格式校验失败，`data/community-builds/cache/index.json` 因此多了一条 `rejected` 记录（其余记录逐字保留，`mergeRecords` 只替换 `--only` 指定的 id）；缓存了原始 `.schem` 文件（`data/community-builds/cache/raw/github-interlude6465-hunterx-simple-house.schem`，2231 字节）。**均为未跟踪文件，未提交**。

### 为什么没有直接动手实现选项 A 或 B

任务书第 1 步的兜底路径写得很明确：找不到就把候选块数谱列出来、把「要不要改造凑区间」交您拍板。选项 A（找新社区源）本身需要去找一个新的、没验证过的 GitHub 仓库或素材站点加进 `sources.json`——这已经超出「在现有素材里挑一份」的范围，属于扩大本轮任务面；选项 B 改块数据是红线动作。两条都不该我自己拍板往下走，所以停在这里。

### 第 2、3 步现状

**未开始**——任务书第 2 步（接聊天名字解析）和第 3 步（离线核对 + 看板 + 归档）都是在「选定图纸」之后才能做的事，本轮没有选定的图纸，两步都无从下手。

### 看板

已在主仓 `dashboard/board-data.json` 加了一张新卡 `m01`（新分组 `g-basic-playable`），把这轮的候选谱和拍板请求原样摘要进去；`node tests/dashboard-lanes.test.js`（主仓）跑过，PASS。

### 归档

第 16 轮反馈已整份挪到 `docs/协作/历史/后勤第16轮-漏斗三口子执行层接通.md`，本文件重写。

## 交接

### 给开发

- **联动口子（决策 #84，仅记一笔，未实现）**：本轮没有走到「接名字解析」这一步（`ai/blueprint-name-index.js`），所以没有机会顺手记录联动接缝的位置。留到选定图纸、真正接线那一轮再看。
- 若您倾向选项 A，我下一轮可以先把「找新社区源」这件事的范围问清楚——是任由我自己去找公开仓库，还是您/开发这边先给一两个候选链接更稳妥（毕竟上一次找到的 `hunterx-simple-house` 结果是假素材）。

### 给建造线

无——本轮未产出任何可供真机验证的改动。

## 按 AGENTS.md 第 11 节

1. **读过的文件**：主仓 `docs/协作/说明.md`、`docs/协作/后勤/说明.md`、`docs/协作/后勤/给CC.md`、上轮 `CC反馈.md`、`AGENTS.md`；MC-ops `AGENTS.md`、`ai/blueprint-name-index.js`、`systems/blueprint-selector.js`、`scripts/sync-community-blueprints.js`、`data/community-builds/sources.json`、`data/community-builds/cache/index.json`、本地 `blueprints/*.json`、`data/community-builds/cache/blueprints/*.json`；主仓（建造线 worktree）`data/community-builds/cache/blueprints/github-free-minecraft-schematics-house-pirbaba.json`、`github-ifnullthenvoid-fort-wall-gate.json`、`github-ifnullthenvoid-fort-watchtower.json`（只读取块数，未改动）；主仓 `dashboard/board-data.json`。
2. **改动的文件**：主仓 `dashboard/board-data.json`（加一个分组一张卡）、`docs/协作/后勤/CC反馈.md`（本文件）、新增归档 `docs/协作/历史/后勤第16轮-漏斗三口子执行层接通.md`（**只改不提交，按后勤线协议**）。MC-ops **代码零改动**；`data/community-builds/cache/index.json`、`cache/raw/github-interlude6465-hunterx-simple-house.schem` 因同步命令产生（未跟踪，未提交）。
3. **执行的命令**：MC-ops `git status --short` / `git log --oneline -10`；`npm test`（基线，全绿）；多条 `node -e` 只读探针（数图纸块数、读生成器、读索引文件）；`node scripts/sync-community-blueprints.js --only=github-interlude6465-hunterx-simple-house`；主仓 `node tests/dashboard-lanes.test.js`。**未执行**：`npm install/ci/update`、全量 sync、服务器/bot/端口/RCON、被禁 git 命令、任何跨线目录（`D:\code\MC`、`D:\code\MC-fix`）的读写。
4. **测试结果**：MC-ops `npm test`：**exit 0**（后台跑完，含全部测试文件，日志显示全部标记 `passed`，含新增的 `intent blueprint name tests passed`）。主仓 `node tests/dashboard-lanes.test.js`：**PASS**。
5. **当前最新判定**：
   - 第 1 步：**未完成，卡在「无合适候选」**——已证实：本地 4 份、生成 10 份、社区缓存 3 份 + 新试接 1 份，共 18 份候选逐一量过，无一落在 600-850 且属于民居品类
   - 第 2、3 步：**未开始**，依赖第 1 步
   - 交接的「给开发」联动口子记录：**未产出**（没走到接线那一步）
6. **生成文件 / 内存是否变脏**：MC-ops `data/community-builds/cache/index.json` 与 `cache/raw/*.schem` 因本轮同步命令有变化（未跟踪、未提交，非「脏」的生成报告或运行内存）；主仓 `data/memory/*`、`acceptance/reports/latest-*` 零触碰。
7. **是否建议提交**：**不建议**——本轮 MC-ops 无代码改动可提交；主仓两处改动（看板、协作文件）按协议只改不提交。
8. **剩余风险**：
   - 若老板选 A（找新社区源），下一轮的产出速度取决于能不能找到一个真实、块数合适、审查能过的公开素材源，**存在找不到的风险**
   - 若老板选 B（加盖凑区间），改造质量（好不好看、是否住宅资格达标）需要额外一轮验证，**不是拍板即完事**
   - `mcbuild-a-small-modern-house-16786` / `mcbuild-wood-modern-house-15774` 两份社区缓存长期处于 `rejected_hard_gate`，本轮未深究具体拒绝原因（不在本轮任务范围内，仅顺带发现）——**UNKNOWN**，若未来有需要可另起一轮查
