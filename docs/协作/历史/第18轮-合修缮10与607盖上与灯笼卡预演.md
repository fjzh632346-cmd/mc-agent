# 建造 第 18 轮反馈

- 线: 建造
- 轮次: 18
- 日期: 2026-09-04
- 状态: 已完成（修缮 10 合入；门楼真机续建 13 分钟、首撞 607,73,-16 盖上；随后在另一块灯笼上按 #48 同点三败停手取证；进程清零）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / ca07bcc6（合并修缮 10）；本反馈与文档随后一笔，主仓已快进到 ca07bcc6
- 实际运行模型 / 强度: Claude Fable 5.1（`claude-fable-5-1`），与任务书建议一致；思考强度本端读不到具体档位，按会话默认档运行

---

## 一句话结论

上一轮卡了三次的那块砖这次一次就盖上了，修缮线的修复在门楼真身上成立；她接着又盖了六十来块，然后在屋顶下方一盏吊灯笼处换了个新病因连卡三次，按规矩停手取证，工地完好。

## 白话摘要

先把修缮线交来的两笔修复收进了主线，测试全绿，只有一处测试清单的同一行两边都加了东西，我手工并成一行，请开发过目。然后开了服务器让她续建门楼：她准确认出了原来的工地档案，第一块就是上次三次都站不到位的那块，这次她先垫了三块土搭出台阶，走到高处平台，把砖放上了，事后查地图确认那块砖在。接着她又连续盖了六十多块，中途三次因为按钮朝向不对停下重来，这是后勤线正在修的已知问题，只记数不追。

十三分钟后她要装一盏吊在屋顶下面的灯笼，规划时把所有能站的地方都算了一遍，全部说"走不通"，连站都没站起来就放弃，同一盏灯笼连续三次，按老板定的规矩停手。这个病因和上一轮的不一样：上一轮是"想好了路但走不动"，这次是"路都想不出来"，要交修缮线。另外，我让她停工三分钟后，她自己判定"离家太远"跑出去八十多格卡在半路，我把她拉回工地才收工；这是工地锚点三分钟宽限到期后的正常行为，不是修复失效，但值得开发知道。

下一步：修缮线拿这盏灯笼的日志段找根因；建造 19 收后勤 11 后再续。

## 需要老板拍板的问题

无。

## 技术细节

### 一、第 1 步：合修缮 10 `feat/fix@ee30d137`（完成）

- 预核：`git merge-base HEAD ee30d137` = `95898337` ✔；`git log --oneline HEAD..ee30d137` 恰 2 笔 `ee30d137` / `daddfe04` ✔——**已证实**
- 第三条预核 `95898337..HEAD -- 六文件` 字面非空（10 笔），与第 17 轮同一拓扑现象（`95898337` 是 `760eced8` 的合并父提交）。真实内容核：`git diff --stat 95898337 HEAD -- 六文件` 只有 `package.json`（+8/−3，后勤加的五条 `test:*` 与 `test` 总清单）和 `tests/actions.test.js`（+12，第 13 轮 `BUILD_TEMP_REFERENCE_FAILED` 断言）；`760eced8..HEAD -- 六文件` 为空——**已证实**：主干在合并基之后没有新动过这六个文件的逻辑
- `git merge ee30d137` → **一处冲突**：`package.json` 第 8 行 `"test"` 单行脚本（主干侧是后勤加过 `axis-reference-order` / `clickable-reference-order` / `oriented-placement-state` / `construction-run-archive` / `construction-resume-plan-id` / `dashboard-lanes` 的版本，修缮侧在 `door-traversal` 后插了 `scaffold-allowlist-path`）。解法：取主干那行，在 `door-traversal.test.js && ` 后插入 `node tests/scaffold-allowlist-path.test.js && `，其余五文件自动合并（`tests/actions.test.js` 两侧改动分属不同函数：主干 +12 行诊断断言、修缮 100→0 断言，均在）。合并提交 `ca07bcc6`，父 `9454538a` + `ee30d137`——**已证实**。任务书预期零冲突，这不算逻辑冲突（同一行两边各加一项，并集唯一），我没停手，请开发复核那一行（`git show ca07bcc6 -- package.json`）
- `npm test`：**61 组**全绿，退出码 0（脚本 `node` 调用数 61，含新 `scaffold-allowlist-path`）；单跑 `scaffold-allowlist-path`（`scaffold allow-list path tests passed`）、`actions`、`execution-tasks` 均退出码 0——**已证实**
- `git -C D:\code\MC merge --ff-only`：`9454538a..ca07bcc6` 快进（7 文件 +181/−9，新 `tests/scaffold-allowlist-path.test.js`）；`npm --prefix D:\code\MC test` 退出码 0——**已证实**
- grep 核：`actions/move.js` 里 `exclusionAreasPlace.push` 只剩 L277 一处（排除集，`excluded.has ? 100 : 0`），白名单不再进代价回调（L286 起注释说明），`installScaffoldPlacementCellFilter(movements, { excluded, allowed })` L299 调用、L302 定义仍在——**已证实**，合对了

### 二、第 2 步：门楼续建（真机 08:17Z–08:31Z，本地 01:17–01:31）

**起法**：服务器 `start-detached.ps1` 起，java pid 16660，`Done (3.444s)`；RCON `time set day`（原 11323）、`forceload add 588 -26 611 5`（9 chunks）、`doDaylightCycle` 保持 true；改前方块查询 `607,73,-16` = air（`chiseled_tuff_bricks` Test failed）。bot `start-detached.ps1` 起，node pid 37060，五个环境变量齐；ONLINE-GATE：`[端口] 使用 .env 指定端口: 25565` 08:17:33.058Z、`[TickLoop] started` 08:17:34.302Z、`[Bot] 上线成功` 08:17:34.306Z、`[DEBUG_STATUS]` 由保姆脚本 `.tmp/cc-r18-fort-driver.js`（第 14 轮 v2 + 每 5 s 位置采样 + 预算到点自停）以 `accept_tester` 发 `debug_status` 触发——**已证实**。`start-detached.ps1` 用 `powershell -File` 嵌套调用时 `$PSScriptRoot` 为空会报错，须显式 `-WorkingDirectory`（本轮三次都这么起的）

**续建命中**（**已证实**）：
```text
[08:18:10.345Z] [BUILD_CONSTRUCTION_RUN_RESUME_PLACEMENT] runId=construction_run_d2f3cbb572ceb6b7 blueprint=fort_wall_gate origin=599,67,-19
[08:18:12.795Z] [BUILD_CONSTRUCTION_RUN_RESUME_EVIDENCE] runId=construction_run_d2f3cbb572ceb6b7 sequence=25 before=1033/1863 after=1047/1863 processRestarted=true lanPort=25565 worldId=building-A:新的世界
[08:18:12.850Z] [BUILD_CONSTRUCTION_RUN] mode=resume runId=construction_run_d2f3cbb572ceb6b7 verified=1047 pending=792 repair=0 stateRepair=24
```
注意 `stateRepair` 从存档的 38 变 24（verified 1033→1047）：后勤 10 的对账尺子在 resume 对账时把 14 步朝向类 state_repair 认成了 verified（红石线四向、活塞等），没放一块砖——**已证实**（对账数字）；哪 14 步 = 存档改前 38 条减改后名单（改后 27 条含本轮新增 3 条，见下）

**首撞 607,73,-16 全过程**（**已证实**，原样抄自 `logs/bot-current.log`，已另存 `.tmp/cc-r18-bot-log-2026-09-04T0817Z.log`）：
```text
[08:18:12.952Z] [BUILD_STEP] index=319 kind=place phase=frame pos=607,73,-16 block=chiseled_tuff_bricks
[08:18:13.092Z] [BUILD_VERTICAL_ACCESS_CANDIDATE] target=607,73,-16 id=vertical_0 stand=604,74,-17 endpoint=604,74,-17 preview=success execution=not_executed previewSlices=3 pathNodes=12 routeLength=19.66 scaffolds=601,70,-15|601,70,-14|601,71,-14|601,72,-14 formalConflicts=none toBreak=0 rejection=none
  （vertical_1..6 同为 preview=success，stand 分别 604,73,-16 / 604,73,-15 / 605,73,-16 / 606,73,-16 / 606,73,-15 / 606,73,-13）
[08:18:13.394Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=607,73,-16 total=7 previewed=7 legal=7 noPath=0 formalConflict=0 digRequired=0 previewError=0
[08:18:13.398Z] [BUILD_VERTICAL_ACCESS_PLAN] target=607,73,-16 stand=604,74,-17 pathNodes=12 scaffoldCount=4 scaffolds=601,70,-15|601,70,-14|601,71,-14|601,72,-14 alternatives=604,74,-17|606,73,-16|606,73,-15|605,73,-16
[08:18:13.519Z] [BUILD_STAND_PATH_PRESCREEN] target=607,73,-16 applied=true result=order_preserved probed=4 ranking=604,74,-17:timeout|606,73,-16:timeout|606,73,-15:timeout|605,73,-16:timeout
[08:18:13.522Z] [BUILD_STANCE_CANDIDATES] target=607,73,-16 total=4 attempts=604,74,-17|606,73,-16|606,73,-15|605,73,-16 blacklisted=none
[08:18:13.528Z] [BUILD_PLACE_REPOSITION] target=607,73,-16 stand=604,74,-17 reason=avoid_target_collision
[08:18:14.290Z] [BUILD_VERTICAL_ACCESS_SCAFFOLD_PLACED] target=607,73,-16 position=601,70,-15 block=dirt
[08:18:14.329Z] [状态] x=601.32 y=71.00 z=-15.40
[08:18:14.502Z] [BUILD_VERTICAL_ACCESS_SCAFFOLD_PLACED] target=607,73,-16 position=601,70,-14 block=dirt
[08:18:20.509Z] [BUILD_VERTICAL_ACCESS_SCAFFOLD_PLACED] target=607,73,-16 position=601,71,-14 block=dirt
[08:18:24.340Z] [状态] x=604.50 y=74.00 z=-16.40          ← 到了计划站位 604,74,-17
[08:18:26.993Z] [PATHFINDER_GOAL_CLEAR] reason=vertical_access_cleanup taskId=1
[08:18:28.893Z] [BUILD_STAIR_TEMP_REFERENCE_CLEAR] pos=601,71,-14 block=dirt   （601,70,-14 / 601,70,-15 同）
[08:18:31.367Z] [BUILD_VERTICAL_ACCESS_CLEANUP] target=607,73,-16 positions=601,70,-15|601,70,-14|601,71,-14
[08:18:33.520Z] [BUILD_STEP] index=320 kind=place phase=frame pos=607,73,-15 block=chiseled_tuff_bricks
```
保姆脚本位置采样（每 5 s）：08:18:08 `601.46,71,-15.37`（登出点）→ 08:18:24 `604.50,74.00,-16.40`。**事后方块查询** 08:18:5x：`execute if block 607 73 -16 minecraft:chiseled_tuff_bricks` → **Test passed**——**已证实**。整段 18.4 s，无 `REJECT`、无 `move_timeout`。与修缮 10 预测（从 601,71,-16 先向 +z 搭桥两块、上 y73 平台、到 604,74,-17 放砖）**吻合**：实际三块土 601,70,-15 → 601,70,-14 → 601,71,-14 是往 +z 的上行台阶，第四块 601,72,-14 没用上就到了

一个给修缮线的观察：`BUILD_STAND_PATH_PRESCREEN` 四个候选**全部 `timeout`**（与第 14 轮一字不差），但 `result=order_preserved` 没拦，执行侧一次走通。预筛的探针预算判 timeout 与执行结果矛盾——**已证实**（日志）；预筛用的 A* 预算比执行侧小、对搭桥路径必超时——**合理推断**。这轮它没坏事，但预筛结论在带搭桥的目标上没有参考价值

**继续盖**（**已证实**）：13 分钟内 `BUILD_STEP` 66 行 / 64 个不同目标；存档 verified 1047 → 1107（本轮实放 +60）；`BUILD_VERTICAL_ACCESS_SCAFFOLD_PLACED` 19 次（多处目标靠搭脚手架上去，都成了）；`607,73,-12` 事后查询 Test passed。三次续建重发都命中原 run（1074/764/25 → 1082/755/26 → 1107/729/27）

**停手点 604,77,-2 `lantern{hanging=true}`**（step `step_50037658b0a05f26`，依赖 `604,78,-2 stone_brick_slab` 已 verified；世界：`604,78,-2` 非空气、`604,77,-2` 空气、`604,76,-2` 空气）——**已证实**：
```text
[08:30:26.220Z] [BUILD_STEP] index=414 kind=place phase=frame pos=604,77,-2 block=lantern
[08:30:26.645Z] [BUILD_VERTICAL_ACCESS_CANDIDATE] target=604,77,-2 id=vertical_0 stand=603,78,-5 endpoint=603,77,-7 preview=partial execution=not_executed previewSlices=10 pathNodes=23 routeLength=34.24 scaffolds=601,69,-10|601,70,-10|601,71,-10|601,72,-10|603,73,-7|603,74,-7|603,75,-7|603,76,-7 formalConflicts=none toBreak=0 rejection=preview_partial
  （vertical_1..14 全部 preview=partial rejection=preview_partial，previewSlices 10–12，pathNodes 21–26，routeLength 30–34）
[08:30:32.735Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=604,77,-2 total=15 previewed=15 legal=0 noPath=15 formalConflict=0 digRequired=0 previewError=0
[08:30:32.739Z] [BUILD_VERTICAL_ACCESS_CANDIDATE_FALLBACK] target=604,77,-2 restricted=15 reason=restricted_candidates_exhausted
  （放宽后 vertical_0..18 共 19 个，仍全部 preview=partial）
[08:30:40.723Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=604,77,-2 total=19 previewed=19 legal=0 noPath=19 formalConflict=0 digRequired=0 previewError=0
[08:30:41.018Z] [task-manager] fail task=build_blueprint id=4 reason=no_reachable_build_stance_after_vertical_access_candidates
```
任务 #6（08:31:02）、#7（08:31:27）逐字重复，保姆脚本判同点三败 `GIVE_UP repeated_target=604,77,-2`，发 `停止任务` 退出。位置采样 08:30:27 → 08:31:27 全部 `597.50,67.00,-22.50`（她在工地西侧夹具带地面，三次都从这里起算；第 1 次前刚被脚本 `chest_rescue_tp` 到箱子边，第 2/3 次前脚本 `rescue_tp_site`）。三次全程 **15 s 内**结束、没有 `SCAFFOLD_PLACED`、没有 `PRESCREEN`、没有 `move_timeout`——**已证实**

判类：这不是第 14 轮那类（预演 success、执行 noPath 站着不动到超时），是**预演本身**对全部候选给出 `partial`（`actions/build.js` L5974–5996：`preview.status !== 'success'` 且终点没到目标、也不是可用的部分终点 → `preview_partial` → `noPath`）。目标在屋顶下方 y77，从 y67 地面上去每条候选都要 9–10 块脚手架、路径 21–26 节点、`previewSlices` 10–12——预演分片到上限就截断给 `partial`——**合理推断**（没读分片上限代码）。修缮 10 改的是执行侧白名单代价回调，不碰预演，所以治不到这里——**合理推断**。存档该步 `terminal_failed retry=3 lastError=no_reachable_build_stance_after_vertical_access_candidates`；下次 resume 是否会再撞它 **UNKNOWN**（`terminal_failed` 只在 `failureAndRepairRecords` 证据函数里见到，重试规则没查）

**朝向类（预期现象，只数数）**：本轮新增 3 步 state_repair（存档 24 → 27）——`607,77,-17 dark_oak_button` `facing:north!=south`（08:22:49）、`601,77,-5 dark_oak_button` `face:wall!=ceiling`（08:25:03）、`601,77,1 dark_oak_button` `face:wall!=ceiling`（08:29:47）。三次都触发任务失败 → 脚本重发续建 → 命中原 run 继续。存档现 27 条 state_repair 全名单：`603,68,-10/-9/-8/-7/-6 repeater`、`604,68,-10/-9/-8/-7/-6 sticky_piston`、`604,69,-10..-6 piston`×5、`603,74,-3 comparator`、`603,74,-2 hopper`、`604,74,-2 hopper`、`603,74,0 observer`、`604,74,0 dropper`、`604,75,-1 comparator`、`604,75,0 dropper`、`607,77,-5 / 601,77,-5 / 601,77,1 / 607,77,-17 / 601,77,-17 dark_oak_button`——**已证实**（存档）。给后勤线：本轮 `face:wall!=ceiling` 两例说明按钮贴天花板底面的 `face=ceiling` 也在失控面里

**修缮 9 预测的两块**（`603,69,-11` 红石线、`604,75,-2` 漏斗+比较器）：本轮**没被尝试**（`BUILD_STEP` 里无这两个坐标；`603,69,-11` 现已被对账认成 verified；`604,74,-2 hopper` / `604,75,-1 comparator` 仍是 state_repair 且本轮 run 没有重放 state_repair 步）；`[BUILD_PLACE_SNEAK]` 0 次、`place_failed:unstable_air` 0 次——**已证实**，潜行判据本轮无样本

**判定表其余行**：`lost connection` 0、`EVENT_LOOP_STALL` 1 次（08:18:33 `stallMs=2518`，紧接首撞放砖后的 `BUILD_STAND_PATH_PRESCREEN`，单次，无掉线）、`BUILD_PLACE_REPOSITION_REJECT` 2 次（`601,78,-18` / `601,78,-6` 各一个候选 `stand_move_timeout_blacklisted`，换候选后都盖上了）——**已证实**

**「夜里往老基地跑」**：不是在施工中。时序：08:31:30 脚本发 `停止任务`；此后 `WORKSITE_ANCHOR_HOLD priority=TOO_FAR_FROM_BASE / NIGHT_UNSAFE` 每 0.5 s 一对，`ageMs` 从 17102 一路涨到 179882（08:34:10）；`systems/worksite-anchor.js` L20 `graceMs: 180000`、L71 `ageMs > graceMs → null`；08:34:30 生存系统 `TASK_CREATED return_to_base reason=TOO_FAR_FROM_BASE`，她从 597.5,67,-21.5 西行，08:35:20 到 `511.4,77.4,-2.3` 卡住，#8 `move_timeout`、#9 `escape_exhausted`（含 `ESCAPE_RUNG_SUCCESS rung=pillar_up yGain=-5.24 flatGain=30.34` 这种数值可疑的成功）。判：**锚点 3 分钟宽限到期后的设计内行为**，修缮 9 的夜里改动在宽限内一直 HOLD 住了——**合理推断**（宽限内 HOLD 已证实，到期即跑已证实，"设计如此"是读注释得出）。副作用：收工时她在 511,77,-2，我用 RCON `tp LinXia 597 68 -22`（脚本同款救援）拉回再停 bot，服务器侧 `data get entity LinXia Pos` = `597.5,67.5,-21.5`——**已证实**（tp 后 3 s 内 `Stop-Process`，bot 自己日志最后一行 08:39:00 的 `[状态]` 仍写 511.5，是客户端没来得及同步；世界存的是服务器侧位置——**合理推断**）。给修缮线两条：(a) 511,77,-2 这段 `return_to_base` 两连败的脱困日志（L4253–L4340）；(b) 老基地 (11,106,8) 离工地 589 格，任何一次超过 3 分钟的停工都会触发跑家

**其他观察**（**已证实**）：`CHEST_PATH_UNREACHABLE` 10 行、脚本 `chest_rescue_tp` 5 次（她站在建筑上下不来去箱子，修缮 11 已列的取证项，本轮又 5 例，均在续建重发后 30 s 内）；`STORAGE_TASK_FAILED chest_item_not_found:dark_oak_button`（08:19:59）、`:stone_brick_slab`（08:28:21）各一次——材料箱没这两样但施工照常继续（她身上有）；`fail task=storage id=5 reason=destination full`（08:30:43，`CHEST_DEPOSIT_ATTEMPT item=dropper count=4` 往 596 箱子放不进）

**收工**（**已证实**）：`Stop-Process -Id 37060`（只此一只，父 35992 是我的包装）→ `list` 0 人 → `forceload remove`（query none）→ `doDaylightCycle` 仍 true → `save-all flush` → `mc-server.ps1 stop` 经 RCON → 01:39:09 `All dimensions are saved`、`Thread RCON Listener stopped`、status `not running` → 25565/25575 无监听。进程表剩 java 39536（`%APPDATA%\.minecraft\runtime` 老板自己的客户端运行时）与 node 43032（OpenAI Codex 运行时），非本线，未动。另：01:14:57 起有一个不属于本线的 `npm test`（pid 40900，父 42536）在跑，01:39 已自然结束，未动。收工复查时（01:41:54）另有一只 `node bot.js`（pid 43536）出现，`Get-NetTCPConnection` 显示它连的是 **25566**（修缮线端口），本线 `logs/bot-current.log` 01:39 后再无写入——是修缮线的 bot，未动、未报异常

### 三、第 3 步：判定表

| 现象 | 改前（第 14 轮） | 改后（本轮） | 证据 | 结论 |
|---|---|---|---|---|
| `607,73,-16` | 预演 7/7 成功、执行 4 站位全 `stand_move_timeout_blacklisted`、`#5/#6/#7 move_timeout`、位置不动 | 预演 7/7 成功、放 3 块土、位置 601,71 → 604.5,74、18 s 放砖、事后查询 Test passed | 上文日志段 + RCON | **修缮 10 在门楼真身上成立**——已证实 |
| 位置一动不动直到超时 | 有 | 无（本轮零 `move_timeout` 施工失败） | 采样 + fail 列表 | 未复现 |
| 有 SCAFFOLD_PLACED 但停桥头 | — | 无（19 次脚手架全部到位） | 日志 | 未出现 |
| `unstable_air` 无/有 SNEAK | — | 0 / 0 | grep | 无样本 |
| `state_mismatch:facing/face` | 38 步 | 对账 →24，本轮 +3 → 27 | 存档 | 预期现象，已数 |
| 夜里往老基地跑 | — | 停工 3 分钟宽限到期后跑，施工中未跑 | L4253 + worksite-anchor.js L20/L71 | 设计内——合理推断 |
| `lost connection` | — | 0 | grep | 无 |
| **新增：预演全 partial → `no_reachable_build_stance_after_vertical_access_candidates`** | — | `604,77,-2 lantern` 同点三败，15/19 候选全 `preview_partial`，15 s 内放弃 | 上文日志段 | **新病因，交修缮**——已证实 |

**三个数**：改前 verified 1033 / state_repair 38 / pending 792；改后 verified **1107** / state_repair **27** / pending **728** / terminal_failed **1**（合计 1863 不变，`updatedAt 2026-09-04T08:31:27.603Z`）。**下次首撞**：`604,77,-2 lantern` 若被重放即是它；否则 UNKNOWN（看重试规则）

### 四、第 4 步：文档

- 第 17 轮反馈整份挪到 `docs/协作/历史/第17轮-合修缮9后勤10与傀儡证伪.md`；`说明.md` 归档表补第 17 轮一行（「真机验收」一节未动）
- 随本反馈一笔提交：`docs/协作/建造/给CC.md`、`docs/协作/老板决策台账.md`（开发追加 #54–#56）、`docs/协作/后勤/给CC.md`（后勤 11 重发版）、`docs/协作/后勤/CC反馈.md`、`docs/协作/历史/修缮第10轮-白名单记错格根治与搭桥首通.md`（修缮 11 开场放的）
- 工作区里 `docs/协作/历史/` 下另有 20 余个早轮他线归档件（修缮第 1–7 轮、后勤第 1–7 轮、优先级线等）与 `docs/协作/修缮/`、`docs/协作/后勤/说明.md` 长期处于未跟踪状态（往轮都没提交，主仓也没有），本轮按任务书只提交上面点名的，其余未动，报开发定夺

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/给CC.md`、`docs/协作/建造/CC反馈.md`（第 17 轮）、`docs/协作/历史/第14轮-…md`、`第15轮-…md`（起法段落）、`docs/CODEX_BOT_STARTUP.md`、`scripts/mc-server.ps1`、`scripts/start-detached.ps1`、`scripts/mc-rcon.js`、`actions/move.js`（L262–340）、`actions/build.js`（L5940–6015、grep 日志标签）、`systems/building-system.js`（L344、L2512、L4815–4835）、`systems/worksite-anchor.js`（L11–L23、L71、L277–280）、`systems/survival-system.js`（grep）、`package.json`、`tests/actions.test.js`（diff）、`.tmp/cc-r14-fort-driver-v2.js`、`.tmp/cc-r15-smoke-probe.js`、`.tmp/cc-r16-recheck.js`、`data/memory/construction-runs.json`（只读两次）、`D:\code\MC-server\server.properties`（只读 RCON 端口/密码，密码未落任何文件）、`D:\code\MC-server\logs\latest.log`、`logs/bot-current.log`、`.tmp/cc-r18-driver-1.log`

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `ca07bcc6` | 合并 `feat/fix@ee30d137`（7 文件 +181/−9；`package.json` 一行手工并） |
| （随后一笔） | 本反馈、`docs/协作/建造/给CC.md`、`docs/协作/说明.md`（+1 行）、`docs/协作/老板决策台账.md`、`docs/协作/后勤/给CC.md`、`docs/协作/后勤/CC反馈.md`、`docs/协作/历史/第17轮-合修缮9后勤10与傀儡证伪.md`（新）、`docs/协作/历史/修缮第10轮-白名单记错格根治与搭桥首通.md`（新） |
| （主仓） | main 快进 `9454538a → ca07bcc6`，再快进到反馈笔 |
| （未入库） | `.tmp/cc-r18-fort-driver.js`、`.tmp/cc-r18-driver-1.log`（UTF-16）、`.tmp/cc-r18-bot-log-2026-09-04T0817Z.log`、`.tmp/cc-r18-server-start.log`、`.tmp/cc-r18-bot-stdout.log` |
| （世界） | 门楼 +60 块（存档 verified 1047→1107）；19 处脚手架土均已 `BUILD_VERTICAL_ACCESS_CLEANUP` 拆除（日志），未逐一 RCON 复查残留；她登出点 `597.5,67.5,-21.5`（我 tp 的）；昼夜规则与 forceload 恢复原状 |

### 3. 执行的命令

`git status/log/merge-base/diff/merge ee30d137/add package.json + 显式文件/commit`、`git -C D:\code\MC merge --ff-only …` + 只读 `log/status`、`npm test` ×2（开场 60 组、合并后 61 组）、`npm --prefix D:\code\MC test` ×1、单跑 3 组、`grep -n` 只读核 `actions/move.js`、`node -e` 只读解析存档 ×3、`scripts\start-detached.ps1` ×3（服务器、bot、保姆脚本，均显式 `-WorkingDirectory`）、`scripts\mc-server.ps1 status/stop`、`Stop-Process -Id 37060`（排除 `$PID`，按命令行核对后）、`Get-CimInstance Win32_Process`、`Get-NetTCPConnection`、RCON：`time query/set day`、`gamerule doDaylightCycle`（查询）、`forceload add/query/remove`、`execute if block` ×14、`data get block` ×8、`data get entity LinXia Pos/Health`、`list`、`tp LinXia 597 68 -22` ×1（收工救援）、`save-all flush`；保姆脚本自身：`tp LinXia`（`chest_rescue_tp` ×5、`rescue_tp_site` ×2）、`bot.chat` `debug_status` / `停止任务` ×3 / `build fort wall gate` ×7。
未执行：任何 JS 逻辑改动、`give`（材料短缺分支没触发）、`setblock`、代放、`npm install`、sync、`git add -A/.`、`stash`、`reset`、`clean`、`checkout --`、对 `MC-fix` / `MC-ops` 的访问、连 25566。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 @9454538a | `npm test` 60 组 | 退出码 0 |
| 合修缮 10 后 @ca07bcc6 | `npm test` 61 组 | 退出码 0 |
| 同上单跑 | `scaffold-allowlist-path`、`actions`、`execution-tasks` | 均退出码 0 |
| 主仓 @ca07bcc6 | `npm --prefix D:\code\MC test` | 退出码 0 |
| 真机 | 续建 13 分钟（08:18–08:31Z） | 首撞 PASS；+60 块；`604,77,-2` 同点三败 → 按 #48 停手（BLOCKED 于新病因） |

### 5. 当前最新判定

- 第 1 步 合修缮 10：**完成**（`ca07bcc6`，`package.json` 一行手工并，61 组全绿，main 快进 + 主仓全绿，grep 确认）
- 第 2 步 续建：**首撞 `607,73,-16` PASS**（事后方块查询 Test passed，修缮 10 在门楼真身上成立）；续盖 +60；**在 `604,77,-2 lantern` 上 BLOCKED**（预演全 partial 的新病因，同点三败按 #48 停手）
- 第 3 步 判定表：已逐行填；三个数已记
- 第 4 步 文档：完成
- 进程表：本线零服务器、零 bot、零脚本；25565/25575 不监听

### 6. 生成文件 / 内存是否变脏

`data/memory/construction-runs.json`（本轮真机改写，未跟踪文件，不提交）、`task-memory.json` / `world-memory.json` / `latest-*`（历史脏状态，本轮未跑 acceptance，未提交）、`logs/bot-2026-09-04-01-17-31.log`（本场原件）。`.tmp/` 下新增 5 个本轮文件，第 16 轮心跳文件原样保留。

### 7. 是否建议提交

已提交合并笔；本反馈 + 文档 + 他线文件随后一笔；主仓快进两次。

### 8. 剩余风险

- `604,77,-2 lantern{hanging}` 预演全 partial 是新病因（预演分片上限 / 高处目标长路径），修缮 10 不覆盖；同一屋顶下 `lantern` 图纸共 17 盏，同类目标可能不止一处——交修缮线
- 停工超过 3 分钟（锚点 `graceMs` 180 s）她必跑家且这次跑到 511,77,-2 两连败脱困；以后保姆脚本停手后应立刻收 bot 或及时 tp，否则她会被丢在半路
- `BUILD_STAND_PATH_PRESCREEN` 在带搭桥目标上全报 timeout 与执行结果矛盾，虽然本轮没拦，但它给出的排序对这类目标没有参考价值
- `package.json` `test` 行由我手工并集，请开发过目
- 修缮 9 的潜行判据本轮无样本（两块预测目标没被重放），仍是 UNKNOWN
