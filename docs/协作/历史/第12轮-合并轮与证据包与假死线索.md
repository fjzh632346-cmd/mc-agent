# 建造 第 12 轮反馈

- 线: 建造
- 轮次: 12
- 日期: 2026-09-02
- 状态: 已完成
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / 723cdf4d（合并笔 545b69a5 + 证据包笔 ef1bc1ff + 本反馈与文档笔 723cdf4d；本文件一处路径笔误的小修随后一笔，主仓快进到同一 HEAD）

---

## 一句话结论

合并轮全部完成：修缮线三笔修补已收进本线并推到主干、测试全绿；八项老毛病的证据包已归档给修缮线，其中"假死掉线"查出了具体线索——她不是死机，是埋头算路和翻找合成台时三四十秒不应答服务器的心跳，被当成掉线踢了下去。

## 白话摘要

第一件事，合并。把修缮线攒下的三笔修补（站脚清障、假时钟测试、凿顶自救）收进建造线，一处冲突都没有；本线的测试从四十九组增加到五十一组、全部通过；主干仓库顺利快进到同一版本，再跑一遍测试同样全绿。按您第三十七条拍板，只收到指定的那一笔为止，修缮线正在做的新活没有带进来。

第二件事，证据交接。把上一轮八项老毛病整理成四份证据文件加一份保姆脚本，放进三线共用的历史目录，修缮线拿得到。所有节选都是程序从原始日志里机械抽出来的，不是手抄；每条结论都标了"已证实、合理推断、待查"。

第三件事，也是本轮最有价值的发现。整理"假死掉线"时看出了门道：三次掉线前她的程序并没有整个卡死，每秒都在写日志；但它每十秒该报一次的"状态心跳"连续二十五到四十一秒没有报——说明她一头扎进两种忙活里不抬头（给站位反复算路径、翻遍地图找合成台），服务器发来的心跳没人应，十五秒后就把她判成掉线；忙活一结束，她立刻发现自己已被踢出。掉线以后程序不会自己重连，还继续打印旧位置、继续在本地空转，看着像活的。这条线索已经写清楚交给修缮线。顺带更正上一轮两处判断：所谓"开箱子二十秒没反应"和"任务排队不启动"，都是掉线之后的后遗症，不是独立的毛病。

第四件事，放置顺序死结也有了数：门楼图纸里一百九十九根有朝向要求的原木，按现在的施工顺序有六十八根轮到它时旁边还没有可依靠的方块——五十一根是依靠物排在它后面、六根在图纸里本来就悬空、三根旁边只有红石线，其余是连带受影响。上一轮手术补上的一百三十四块也逐块列了清单，将来终验对账用。

第五件事，可选项也做了：合并后的程序在真机上开了一次，四个上线信号全部出现，工地一格没动；说明文件的历史归档表补了第十一轮一行。

下一步：修缮线按证据包修假死重连（第七轮）与放置顺序（第八轮），修好后建造线再续建门楼。本轮没有需要您拍板的事。

## 需要老板拍板的问题

无

## 技术细节

### 〇、开场基线

- 开场 `git status --short`：已跟踪脏文件仅 `acceptance/reports/latest-*`（3，第 11 轮 case2 FAIL 记录）、`data/memory/task-memory.json`、`world-memory.json`、`docs/协作/建造/给CC.md`（开发下发的第 12 轮任务书）；未跟踪的 `.tmp/`、`dashboard/`、`docs/协作/后勤/`、`docs/协作/修缮/` 与其它线的历史归档件一律未动——**已证实**
- HEAD = `a56decb7`（第 11 轮文档笔），与任务书预期一致；`npm test` 49 组全绿、退出码 0——**已证实**

### 一、第 1 步：合并（完成）

- 命令：`git merge a044997c -m "Merge feat/fix at a044997c: …"`（显式指定提交号，未合 `feat/fix` 分支尖端；执行时 `feat/fix` 尖端恰好也是 `a044997c`，修缮线第 7 轮尚未提交）——**已证实**
- merge-base `870e9c00`；`git log HEAD..a044997c` 恰好三笔：`b67dba01`（清障不挖脚下）、`b976d5d2`（feedback-cooldown 假时钟）、`a044997c`（凿顶自救）——**已证实**
- 合并提交 **`545b69a5`**，ort 策略，**零冲突**：7 文件 +1039/−4——`actions/build.js` +150、`tasks/stuck-recovery.js` +110、`utils/feedback-cooldown.js` +9/−2、`tests/feedback-cooldown.test.js` +24/−1、新增 `tests/clear-footing-guard.test.js`（364 行）与 `tests/escape-ceiling-dig.test.js`（382 行）、`package.json` 测试主链。`package.json` 本线自 `870e9c00` 起未动过，git 自动取了修缮线的并集版本，无需手工处理——**已证实**
- 组数：合并前 49 组 → 合并后 **51 组**（`grep -o 'node tests/' package.json | wc -l`）；`npm test` 全绿退出码 0，`escape-ceiling-dig`、`clear-footing-guard` 两组通过，`feedback-cooldown` 首跑即过、1ms flake 未再现（本轮三次全量都没出现）——**已证实**
- 主仓推账：`git -C D:\code\MC merge --ff-only refactor/building-blueprint-ir-v1` → `Updating 870e9c00..545b69a5 Fast-forward`，14 文件 +1421/−174，**没有被未跟踪文件拦住**；`git -C D:\code\MC log --oneline -3` 与本线一致；`npm --prefix D:\code\MC test` 全绿退出码 0——**已证实**
- #26：版本闸门相关文件不在本次合并的改动集里，原样未动——**已证实**
- 主仓在本反馈提交后会再快进一次（同一元操作），让证据包与本反馈也能从 main 读到；届时 main HEAD = 本笔提交，`git -C D:\code\MC log -3` 可查——见收尾说明

### 二、第 2 步：证据包（完成，提交 `ef1bc1ff`）

目录 `docs/协作/历史/第11轮-证据包/`，5 个文件共 276 KB（<1 MB）：

| 文件 | 大小 | 内容 |
|---|---|---|
| `假死掉线.md` | 143 KB / 1217 行 | 三次假死总表、关键结论、每次前后 ±2 分钟 bot 日志节选（去 action-lock）、服务器 `latest.log` 对应行、`[状态]` 心跳表、假死后空转样例、相关代码原文、复现建议 |
| `放置顺序死结.md` | 69 KB / 648 行 | 机制代码指引、全部失败一览表、每次轴向失败的完整片段、REPAIR_TRIGGER 片段、存档序号分析（分类 + 顺推模拟 + 32 根塔柱底座表） |
| `其余六项.md` | 29 KB / 272 行 | unstable_air、朝向/状态错放、换手确认失败、跑回老基地、箱子路径不可达、队列悬死——各附关键片段与存档记录 |
| `手术代放清单.txt` | 15 KB / 181 行 | 134 块（133 原木 + 1 錾制石砖）逐条坐标/方块/来源清单/存档序号；另列 34 条被守护条件跳过的候选 |
| `cc-r11-fort-driver.js` | 6 KB | 第 11 轮保姆驱动脚本原件 |

生成方式：本轮会话脚本 `gen-evidence.js`（放在会话 scratchpad，未入库）按时间窗/行号从三段原始日志、服务器日志、施工存档机械抽取，单行 >300 字截断，行号 `L` 为原文件行号——**已证实**。时区说明写在每份文件头：bot 日志 UTC，服务器日志本机本地时间 UTC−7。

**假死掉线的核心发现**（详见文件 §2）：

- 三次断线均为服务器 `LinXia lost connection: Timed out`：`16:36:17Z` / `17:05:57Z` / `17:30:19Z`（第 11 轮写的 ~17:39 是发现时刻，实际 17:30:19）——**已证实**
- 断线前进程并未整体阻塞：忙碌段落相邻两行最大间隔 2.4 s / 2.5 s / 1.1 s；但 `bot.js:982` 的 10 s `setInterval` 心跳连续漏拍 **24.8 s / 41.5 s / 37.4 s**——定时器与 socket 读同属宏任务阶段，一起被饿死——**已证实**
- 三段忙碌内容：第 1 次 `BUILD_VERTICAL_ACCESS_CANDIDATE` 预览 48 条（受限 7 候选全 noPath → 回退 41 候选，13 s）；第 2、3 次 `utility-search` 49 / 39 条 + `auto-prep`/`STAGING_CRAFT_*`（站在塔顶、材料箱路径不可达时对 16 项缺料逐项暂存合成，每项 3 次 `bot.findBlocks({maxDistance:32/64})` 同步全图扫描，41 s / 30 s）。代码位置 `systems/UtilityBlockSearch.js:207-215`、`actions/build.js:6389`/`6049`/`6060-6076`——**已证实**
- 根因链（**合理推断**）：>15 s 不回到 I/O 阶段 → 服务器 `keep_alive` 留在缓冲区没被读 → `minecraft-protocol` 收到才应答（`node_modules/minecraft-protocol/src/client/keepalive.js:9-24`）→ 服务器 15 s 判超时关连接 → 忙碌一结束客户端立刻读到踢出包（第 1 次：预览结束 16:36:19.814，踢出包 16:36:19.972，相差 158 ms）。具体是同步扫描连续占用还是 await 链只走 microtask，**UNKNOWN**，建议修缮线 `--cpu-prof` 确认
- 无重连：`bot.js:1602-1608` `bot.on('end')` 只停定时器/TickLoop/TaskManager；进程假死后继续活 17.8 / 11.4 / 10.4 分钟，`[状态]` 继续打印缓存位置（107 / 68 / 63 行），第 2、3 段箱子 promise 链每 65 s 空转一轮；存档在断线后 10 分钟（17:40:09Z）还被 reconcile 写过一次（verified=998 / pending=828 / stateRepair=37，依据缓存区块，可信度存疑）——**已证实**（存档可信度为**合理推断**）

**放置顺序死结的核心发现**（详见文件 §5–§6）：

- 存档 `steps` 顺序即编译出的施工顺序（第一次施工 `index=314`↔steps[314]、`index=328`↔steps[328] 对得上）；续建后日志里的 `index=` 是当次重排序号——**已证实**/**合理推断**
- 199 根轴向原木：6 根蓝图里同轴两侧皆空气（横跨门洞的悬空梁）、3 根同轴邻居只有红石线、14 根同轴邻居全排在后面；按序号顺推模拟 **68/199** 放不下（后排 51、悬空 6、红石线 3、级联 8）——**已证实**（模拟规则为近似）
- 临时参照物对 axis=y 只允许「正下方」一个候选（`actions/build.js:3526`），柱子经过蓝图保留格即 `temporary_reference_reserved` 失败，且该错误不打日志（`build.js:855-863`）——机制**已证实**，套用到失败坐标为**合理推断**
- 同点反复：605,72,-4 ×3、602,72,-3 ×5、603,72,-3、603,72,-15、607,78,-12，每次续建都从同一步撞死——**已证实**

**手术清单重建**：两份候选清单共 167 条守护式 setblock 候选，减去三段日志里她自己 `BUILD_STATEFUL_PLACE` 放成的 34 条 = 133 根原木，加 604,75,-2 錾制石砖 1 块 = **134**，与第 11 轮反馈"133 + 1"完全吻合——总数**已证实**，逐条归属为**合理推断**（RCON 逐条回显未留档）

**对第 11 轮反馈的更正**：

1. 第三次假死实际 17:30:19Z（非 ~17:39）——**已证实**
2. "箱子交互卡死"中 `CHEST_OPEN_FAILED windowOpen 20s` 三段共 9 次，**全部在断线之后**（第 3 段 L903 起），是假死症状；真正的箱子问题是站在建筑上时 `CHEST_PATH_UNREACHABLE`（第 1 段 80 次）——**已证实**
3. "任务队列悬死"的 `manager_stop` 来自断线处理里的 `taskSystem.stop()`，与假死同源，不是独立队列 bug——**合理推断**
4. 老基地 (11,106,8) 距工地原点约 589 格（第 11 轮写 594，量法不同）——**已证实**

### 三、第 3 步：可选项（两项都做了）

**冒烟**（只上线不施工，工地一格未动）：

- 服务器：`scripts\mc-server.ps1 start`（`MC_SERVER_JAVA` 指 JDK 21）→ pid 38512、25565 监听、`Done`；收尾 `mc-server.ps1 stop` 走 RCON，`All dimensions are saved`、状态 `not running`——**已证实**
- bot：`Start-Process node bot.js`，环境 `MC_WORLD_ID=building-A:新的世界`、`MC_PORT=25565`、`MC_VERSION=1.21.8`。第 1 次（pid 39832，07:48:35Z）三信号 4 s 内齐：`[端口] 使用 .env 指定端口: 25565`（07:48:37.327Z）、`[TickLoop] started`（07:48:39.265Z）、`[Bot] 上线成功`（07:48:39.299Z）；第四信号 `DEBUG_STATUS` 需要玩家聊天 `debug_status` 触发（`bot.js:1164`），我的探针脚本放在会话 scratchpad 没带 `NODE_PATH` 报 MODULE_NOT_FOUND，27 s 后按 PID 杀掉重来。第 2 次（pid 31264，07:50:01Z）：三信号 07:50:02.428Z / 07:50:02.994Z / 07:50:03.004Z，探针以 accept_tester 发 `debug_status`（07:50:06.649Z）→ `[DEBUG_STATUS] {"botUsername":"LinXia","botPosition":{"x":512.45,"y":79,"z":-0.5},…}`（07:50:06.692Z）；13 s 后按 PID 杀掉——**四信号齐，已证实**
- 两段冒烟日志（`logs/bot-2026-09-02-00-48-35.log`、`-00-50-01.log`）grep dig/place/BUILD_STEP/setblock/ESCAPE_LADDER/pillar/ceiling/building-clear **均为 0**；她上线 1 s 内即被生存系统拉去 `return_to_base`（TOO_FAR_FROM_BASE，目标 11,106,8），脱困档位只用了 jump_and_step / nearby_exit（纯移动），两次都朝西走：567,70,-13 → 522,73,-2 → 512,79,-0.5。**副作用**：她的登出位置从工地旁 (598.6,67,-22.5) 变成了工地以西约 87 格的 (512,79,-0.5)，下次续建时保姆脚本开场的传送会把她带回工地——**已证实**
- 施工存档 `construction-runs.json` mtime 与 `updatedAt`（17:40:10Z）均未变，run 仍 `BLOCKED_MATERIAL_SHORTAGE` / `terminalState=null`；两只材料箱与别墅未被触碰（冒烟活动全在 x≤598、向西远离）——**已证实**
- 只杀了自己起的两个 bot PID（39832、31264，均确认退出）；机器上另有别人的 node 进程（会话中先后见到后勤线看板 `dashboard\server.js` 与一个不是本 worktree 起的 `bot.js`，都没有连到本线服务器——服务器日志里只有本线 LinXia 的两次登录与 accept_tester 探针），一律未动——**已证实**

**归档表**：`docs/协作/说明.md` 历史归档表补第 11 轮一行（含证据包目录指引），协议正文未动——**已证实**

### 四、收尾说明

- 第 11 轮反馈整份挪到 `docs/协作/历史/第11轮-门槛微调与门楼选址开工.md`（`cmp` 与原件一致）后重写本文件
- 本反馈 + 第 12 轮任务书（开发下发、此前未提交）+ 第 11 轮归档件 + `说明.md` 一笔提交；随后 `git -C D:\code\MC merge --ff-only refactor/building-blueprint-ir-v1` 再快进一次，main HEAD = 该笔（docs-only，主仓测试已在合并笔上跑过全绿，不重复跑）
- 修缮线第 7 轮新产出留待下次合并轮（#37）
- 反馈笔 `723cdf4d` 提交并快进主仓后，发现本文件第三节一处路径笔误（`dashboard\server.js` 的反斜杠被 shell 吃掉），另起一笔小修并再快进一次；不改写已推到主仓的提交

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/给CC.md`、`docs/协作/建造/CC反馈.md`（第 11 轮）、`docs/CODEX_BOT_STARTUP.md`、`package.json`（两侧）、`bot.js`（断线处理、心跳定时器、debug_status）、`actions/build.js`（轴向参照物、临时参照物候选与立柱、垂直通道预览）、`systems/UtilityBlockSearch.js`、`scripts/mc-server.ps1`、`node_modules/minecraft-protocol/src/client/keepalive.js`、三段第 11 轮 bot 日志、`D:\code\MC-server\logs\latest.log`（只读）、`data/memory/construction-runs.json`（只读）、`.tmp/cc-r11-*`（清单、驱动脚本与 5 份输出）、门楼缓存蓝图

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `545b69a5` | 合并 `feat/fix@a044997c`：`actions/build.js`、`tasks/stuck-recovery.js`、`utils/feedback-cooldown.js`、`tests/feedback-cooldown.test.js`、`package.json`、新增 `tests/clear-footing-guard.test.js`、`tests/escape-ceiling-dig.test.js` |
| `ef1bc1ff` | `docs/协作/历史/第11轮-证据包/` 5 文件 |
| （随后一笔） | 本反馈、`docs/协作/建造/给CC.md`、`docs/协作/历史/第11轮-门槛微调与门楼选址开工.md`、`docs/协作/说明.md`（+1 行） |
| （主仓） | `D:\code\MC` main 快进 `870e9c00 → 545b69a5`，反馈提交后再快进一次 |
| （未入库） | 会话 scratchpad 里的 `gen-evidence.js`、`analyze-freeze.js`、`smoke-probe.js`；`logs/bot-2026-09-02-*.log` 两份冒烟日志；`logs/bot-current.log` 已被冒烟覆盖（第 11 轮三段日志在带日期的文件里完好） |

### 3. 执行的命令

`git status/log/branch/merge-base/diff/show/merge/add/commit`（add 全部显式指定文件）、`git -C D:\code\MC merge --ff-only …` ×2 与 `git -C D:\code\MC log`、`npm test` ×2（本线）、`npm --prefix D:\code\MC test` ×1、`node` 分析/生成脚本若干（只读日志与存档）、`scripts\mc-server.ps1 start/status/stop`、`Start-Process node bot.js` ×2 + `Stop-Process` 指定 PID ×2、探针 `smoke-probe.js`（accept_tester 聊天 `debug_status`）。
未执行：`git add -A/.`、`git stash`、`git reset`、`git clean`、`git checkout --`、任何 sync、`npm install`、`taskkill /IM node.exe`、对 `D:\code\MC` 的 `cd` 或文件改动、对 25566 端口或其他 worktree 的任何访问、任何方块改动。

### 4. 测试结果

- `npm test`：开场 49 组全绿（exit 0）；合并后 51 组全绿（exit 0）——**已证实**
- `npm --prefix D:\code\MC test`：快进后 51 组全绿（exit 0）——**已证实**
- 冒烟：四信号齐（第 2 次），0 方块改动——**已证实**
- feedback-cooldown 1ms flake：三次全量均未再现

### 5. 当前最新判定

- 第 1 步合并：**完成**（`545b69a5`，零冲突，49→51 组，main 快进 + 主仓全绿）
- 第 2 步证据包：**完成**（`ef1bc1ff`，四文件 + 脚本，276 KB）
- 第 3 步可选：**完成**（冒烟四信号齐；归档表补行）
- 门楼工地 / run / 材料箱 / 别墅 / 孤本与隔离目录：**一格未动**
- 主仓状态：main = 本线 HEAD（反馈笔快进后）

### 6. 生成文件 / 内存是否变脏

`latest-*` 三份（第 11 轮 case2 FAIL 记录，本轮未碰、不提交）；`task-memory.json` / `world-memory.json` 冒烟会话正常滚动（不提交）；`construction-runs.json` 未变；`logs/bot-current.log` 现为冒烟第 2 段，新增两份带日期冒烟日志（不提交）。其它线留在本 worktree 的未跟踪文件（`dashboard/`、`docs/协作/后勤/`、`docs/协作/修缮/`、若干历史归档件）原样未动、未提交。世界净变化：仅 LinXia 玩家登出位置西移约 87 格；方块零改动。

### 7. 是否建议提交

已提交两笔（合并、证据包）；本反馈及文档随后一笔；主仓快进两次。生成报告与 memory 不提交。

### 8. 剩余风险

- 假死根因链是**合理推断**：宏任务饿死已由心跳漏拍证实，但"哪一层不让路"要修缮线用 CPU profile 或试验性让路（`setImmediate`）坐实；服务器 15 s 判定窗口是按原版逻辑推的
- 临时参照物"保留格拒绝"是按代码推的，失败原因没进日志；修缮线第一步建议先把 `temporaryReferenceError` 打出来再修
- 手术清单的逐条归属（133 vs 34）靠日志反推，若终验对账发现个别出入，以世界实测为准
- 冒烟把她的登出点移到了工地以西 87 格，续建前需传送（保姆脚本已有此步）
- main 现在领先 `feat/ops`（仍 `870e9c00`）三笔以上，后勤线下次合并时按常规处理即可
- 修缮线第 7 轮在 `feat/fix` 上继续，下次合并轮再收
