# 建造 第 24 轮反馈

- 线: 建造
- 轮次: 24
- 日期: 2026-09-06
- 状态: 已完成（第 1、2、5 步全做完；第 3 步真机按 #48 同砖三败自停于 `608,82,-15`，施工只跑了 3 分 46 秒；有 1 个问题等老板拍板）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / `5ea7f8d3`（合后勤 15）；本反馈与文档随后一笔
- 实际运行模型 / 强度 / ultracode: Fable 5.1（`claude-fable-5-1`，已证实）/ 思考强度本端读不到档位，UNKNOWN / **未开 ultracode**（真机轮，任务书标「不开」；全程未用子代理）；全程未切模型

---

## 一句话结论

上一轮卡住的那块城垛砖这次靠"救援规则 → 传送回屋顶"放上了，但紧接着的下一块砖又是同一种病（她站在城垛顶上算不出任何落脚点），救援把她送到地面后，从地面对这块砖同样算不出任何落脚点，三次失败停手——传送规则在第三次之前没轮到，是我的脚本链条缺了一环；真机只跑了不到四分钟，盖了一块。

## 白话摘要

先把后勤线交来的两批代码收进主线，没有冲突，六十四项测试全部通过，离线核对了新的施工单编号和两只漏斗的位置（挪到了通电源之前），主仓跟着更新。开工前按后勤线的要求查了那两只漏斗：两只现在都朝下，和后勤线的推断一致；又把首撞那块砖周围七乘四乘七的区域逐格拍了快照，交修缮线复现"一格宽墙顶"。

真机上她认出原工地，开场三个数与预估一致。上一轮三败的那块城垛砖，她从地面两次都算不出落脚点，第二次失败后脚本按老板批准的规则把她传到城垛上相邻的木柱顶，三十秒后砖就放上了。下一块砖在城垛东头，她站在城垛上两次都算不出落脚点，脚本这次按规矩把她救援到地面；可从地面对这块砖依然一个落脚点都算不出，四十秒失败——这是第三次，脚本按"同砖三败"停手，我立刻把她收了。

问题出在我的脚本链条：老板批准的流程是"救援到地面 → 目标比她高十格以上 → 传送回屋顶另一格"，我的脚本把"传送回屋顶"挂在"再等一次站位超时"上，而这块砖从地面根本算不出站位、直接失败，于是第三次失败先到、传送没轮到。锁占死没出现，补料白跑这轮四次都在屋顶上、每次三十秒。

下一步：问题 1 请老板拍板（这次三败要不要算数、下轮链条怎么补）；城垛顶零候选的完整现场（快照 + 三种起点的失败记录）交修缮线；漏斗朝向查证结果交后勤线。

## 需要老板拍板的问题

### 问题 1：`608,82,-15` 的三败要不要算数？下轮脚本链条怎么补？
- 背景：这块砖三次失败全是"算不出任何落脚点"：前两次她钉在城垛顶上，第三次是救援到地面之后。老板批准的流程要求救援之后紧接着传送回屋顶另一格再试，但我的脚本要等"再来一次站位超时"才传送，而这块砖从地面没有站位超时、直接失败，传送没轮到就三败了。上一轮老板已定"三败照算、只补脚本"。
- 选项 A：三败照算（与上轮口径一致）；下轮脚本改成"救援落地后立刻传送回屋顶相邻实心格"（两步连做），并且把"传送后的那次尝试"作为第三次。代价：这块砖留到下轮首撞；风险：无新增。
- 选项 B：把这次第三败判为脚本缺环造成的无效失败，允许下轮开场不计前三次、从传送后重新数。代价：口径松动；风险：以后每次脚本出错都可能被拿来当理由。
- 我的建议：A，因为规则不该跟着脚本的错误走；脚本改两步连做以后，这种"从地面也零候选"的砖就能在第三次之前站到屋顶上。

## 技术细节

### 一、第 1 步：合后勤 15 `feat/ops@878238d7`（完成，`5ea7f8d3`）

- 预核（**已证实**）：`merge-base HEAD 878238d7` = `bbc56ecf`；`HEAD..878238d7` 恰 2 笔 `878238d7`（看板）/ `1a889af1`（序列层）；`diff --stat bbc56ecf 878238d7` = `dashboard/board-data.json` ±18 / `package.json` +3−1 / `tests/clicked-face-reference-order.test.js` +349（新）/ `utils/site-planner.js` +163−2；主干侧自 `bbc56ecf` 起这四处零改动
- **一处意外**（**已证实**）：`git merge` 首次被拒——`dashboard/board-data.json` 工作区有后勤线直接写入的未提交改动（协议允许它这么做）。核对 `git diff --quiet 878238d7 -- dashboard/board-data.json` 为**空**：工作区内容与后勤 15 提交的版本逐字节一致（只差 CRLF）。处理：备份到 `.tmp/cc-r24-board-data.json.backup` → 用 `git show HEAD:… >` 把工作区文件写回 HEAD 内容（未用 `checkout --`/`stash`/`reset`）→ 合并 → 合后工作区文件 == 合并结果 == 后勤写入的内容（`diff` 忽略 CR 为空）。后勤线的改动一个字节没丢
- 合并（**已证实**）：`Merge made by the 'ort' strategy`，零冲突；4 文件 `<<<<<<<` 0 行；`actions/build.js` `git diff --stat dfc01698 HEAD -- actions/build.js` **空**
- 测试（**已证实**）：`package.json` test 脚本 **64** 个 `node tests/…`；`npm test` 退出码 0；单跑 `clicked-face-reference-order`（9 组含反证）/ `axis-reference-order` / `clickable-reference-order` / `construction-resume-plan-id` 均 `tests passed`
- grep（**已证实**）：`CLICKED_FACE_FACING_BLOCKS` `utils/site-planner.js:63`（`new Set(['hopper'])`）、`scheduleClickedFaceReferenceSteps` `:1003`（定义）/ `:450` `:452`（调用）、`orderDeferredClickedFaceSteps` `:1046` / `:455`
- 离线复核（**已证实**，scratchpad `compile-fort.js` 只读：`LegacyBlueprintAdapter → BlueprintValidator → ConstructionCompiler`，图纸 `data/community-builds/cache/blueprints/github-ifnullthenvoid-fort-wall-gate.json`，origin `599,67,-19`，与新单测同一套编译参数）：`planId = construction_plan_d1919e8b1032` ✔；两只漏斗 step id `step_c2d775981b9da42e`（`603,74,-2`）/ `step_7ec4b6a55565b426`（`604,74,-2`）与存档一致 ✔；在 1233 个 `place` 步里的序号 **#1208 / #1209（1 起数）= 0 起数 1207 / 1208** ✔，紧挨末 24 步供电源（末 24 步 24/24 为 `dark_oak_button|redstone_torch|redstone_wall_torch`，wire 0）✔；`orderPlan.summary`：`clickedFaceReferenceDeferred=2 clickedFaceReferenceRoots=1 clickedFaceTemporaryReferences=1`；`603,74,-2` 带 `clickedFaceReference={604,74,-2 west}` + `temporaryReference={604,74,-2 west}`，`604,74,-2` 带 `clickedFaceReference={603,74,-2 east}`、无临时参照 ✔。注：存档里 run 的 `planId` 字段现为 `construction_plan_82c288475728`（续建时写入），与编译值不同——续建匹配不依赖 planId（`construction-resume-plan-id` 组），本轮续建命中原 run 已证实，此字段差异 UNKNOWN 来源，交开发
- 主仓（**已证实**）：`git -C D:\code\MC merge --ff-only` `dfc01698 → 5ea7f8d3`；`npm --prefix D:\code\MC test` 退出码 0

### 二、第 2 步：改前快照（服务器 pid 37252，`Done (3.000s)`，`forceload add 588 -26 611 5` 9 chunks；未起 bot）

**漏斗两组 RCON（转后勤 16）**（**已证实**）：

| 命令 | 结果 |
|---|---|
| `execute if block 603 74 -2 minecraft:hopper[facing=east]` | Test **failed** |
| `execute if block 604 74 -2 minecraft:hopper[facing=west]` | Test **failed** |
| `execute if block 603 74 -2 minecraft:hopper[facing=down]` | Test **passed** |
| `execute if block 604 74 -2 minecraft:hopper[facing=down]` | Test **passed** |
| `… minecraft:hopper`（不带状态）两格 | 均 passed；`air` 均 failed |

后勤 15「两只都落成 `facing=down`」的推断**在真机成立**。存档：`603,74,-2` state_repair retry 0；`604,74,-2` state_repair retry 3（`protected_block:hopper`，09-01）。本轮未碰。

**首撞现场快照 `600..606 × 81..84 × -19..-13`（196 格，转修缮 16）**（**已证实**，scratchpad `snapshot-region.js`：逐格 `execute if block … air`，非空气格再按存档期望块 + 常见块逐个 `execute if block`）：非空气 **12 格**，全部与存档 verified 一致：

| 格 | 世界 | 存档 |
|---|---|---|
| `602,81,-18` / `606,81,-18` / `600,81,-16` | `dark_oak_log[axis=y]` | verified |
| `600,81,-14` | `iron_bars` | verified |
| `602,82,-18` / `606,82,-18` / `600,82,-16` | `dark_oak_log[axis=y]` | verified |
| `603,82,-18` / `604,82,-18` | `chiseled_deepslate` | verified（第 23 轮传送后盖的） |
| `600,82,-15` / `600,82,-14` / `600,82,-13` | `chiseled_deepslate` | verified（第 23 轮） |

即：y82 那一排是**一格宽**的城垛线：北边 `602..606,82,-18`（605 缺，本轮首撞）、西边 `600,82,-13..-16`，两段在 `600..602,82,-17..-18` 之间**不相连**（`601,82,-17` wall / `602,82,-17` trapdoor 都 pending）；城垛线两侧 y82 全空、y81 下一层只有四根柱子（`602/606,81,-18`、`600,81,-16`、`600,81,-14 iron_bars`），其余 y81 全空（`603..605,81,-18` stairs、`601,81,-17` wall、`600,81,-15/-13` wall 等 pending）。y83/y84 区域内 76 个图纸格全部 air（屋顶 `polished_blackstone_*` 一层还没开始）。**她站在这条线上时脚下是 1 格宽、两侧各 15 格深的空气**。完整 196 格清单在 `.tmp/`（未入库，scratchpad 输出已抄关键行）。

### 三、第 3 步：续建真机（02:45:28Z–02:49:12Z 施工 3 分 46 秒，bot 未重启，无缺料）

**ONLINE-GATE**（**已证实**）：bot pid 15484（父 29916，只连 25565）；`[端口] 使用 .env 指定端口: 25565` 02:44:02.014Z、`[TickLoop] started` 02:44:03.703Z、`[Bot] 上线成功` 02:44:03.708Z、`[DEBUG_STATUS] {"botUsername":"LinXia",…}` 02:45:13.837Z。日志另存 `.tmp/cc-r24-bot-log-2026-09-06T0244Z.log`。

**开场就跑家（给修缮 15，新样本）**（**已证实**）：她在登出点 `599.5,67,-18.5` 上线，**上线后 0.7 s** `enqueue task=return_to_base reason=survival_system TOO_FAR_FROM_BASE`（02:44:04.405Z）——此时**没有工地锚点**（第一条 `WORKSITE_ANCHOR_HOLD … ageMs=0` 直到 02:45:28 续建任务开工才出现），于是 75 s 内向西跑了 128 格（`[状态]` 02:44:13 `567,71,-13` → 02:44:53 `483,75,-0.5` → 02:45:13 `480,73`），期间回家任务 3 个（#1/#2/#3，各被 `ESCAPE_RUNG_*` 链缠住：`repath no_safe_player_point` → `jump_and_step` → `nearby_exit move_timeout` → `safe_dig no_safe_block_to_dig` → `pillar_up no_pillar_material`，共 17 行）。脚本 02:45:15 `停止任务` → tp 到 `597,68,-22` → 续建。**锁没有被占**（本轮 `lock_already_held` 任务失败 0 次）。结论（合理推断）：锚点不随进程重启恢复，上线瞬间就按"离基地 589 格"跑家；与第 22 轮"锁占死"是同一条链的上游。

**续建命中原 run + 开场三个数**（**已证实**）：
```
[02:45:31.114Z] [BUILD_CONSTRUCTION_RUN_RESUME_EVIDENCE] runId=construction_run_d2f3cbb572ceb6b7 sequence=59 before=1171/1864 after=1171/1864 processRestarted=true lanPort=25565 …
[02:45:31.160Z] [BUILD_CONSTRUCTION_RUN] mode=resume runId=construction_run_d2f3cbb572ceb6b7 verified=1171 pending=666 repair=0 stateRepair=27
```
与预估 1171 / 666 / 27 **完全一致**（`605,82,-18` retry 3 保持 pending，未复位）。

**保姆脚本 `.tmp/cc-r24-fort-driver.js`** = r23 + ①钉在原地救援（`FAIL_TRACK`：同目标连续失败计数 + 失败时位置；`consec≥2 && moved≤1 && heightDiff<10` → `RESCUE_PINNED` → `停止任务` → tp `597 68 -22` → `DRAIN` → 续建；放在 `pendingTp` 分支**之前**）②不重发旧失败（按任务 id 记 `handledFailIds`；`停止任务`+tp 之后 `drainAndMark` 把期间的失败行标为已处理，`STALE_FAIL_IGNORED`）③屋顶不去箱子救援（`CHEST_RESCUE_SKIPPED_ON_ROOF`）；#74 / #75 / 缺料 `give` / 5 s 采样 / `ROOF_FALL` 照旧。三处修**本轮都触发并按设计执行了**（下面逐条有行）；**但链条缺一环**（见"三败停手"）。

**首撞 `605,82,-18`——救援规则接住、砖放上**（**已证实**）：

| 尝试 | 她在 | 结果 | 行 |
|---|---|---|---|
| 1（任务 #4，02:45:28–02:46:14，46 s） | 地面 `597.5,67,-21.5`（脚本停车点，未动） | `no_reachable_build_stance_after_vertical_access_candidates` | 见下"原样段" |
| 2（#5，02:46:18–02:47:04，46 s） | 同上未动 | 同上 | `STAND_TIMEOUT count=2` → `FAIL_TRACK consec=2 moved=0 heightDiff=15` → **`[DRIVER_ROOF_TP] target=605,82,-18 to=606.5,83,-17.5 reason=stand_timeout_x2:no_reachable_build_stance… source=verified_roof(dark_oak_log) heightDiff=15 herBefore=597.5,67.0,-21.5`**（站 `606,82,-18` 木柱顶，目标东邻） |
| 3（#6，02:47:13 开工，02:47:45.8 `BUILD_STEP`） | 传送后她已沿城垛走到 `602.64,83,-17.5`（`606.5 → 602.6`，补料段 32 s 内走的） | **放上**：`BUILD_PLACE_REPOSITION target=605,82,-18 stand=603,83,-18` 02:47:45.862 → 02:47:46.437 释放锁 → 02:47:46.528 下一步 `BUILD_STEP 608,82,-15`（**0.6 s**）；02:48:36 对账 verified；RCON `chiseled_deepslate` Test passed | `TARGET_ADVANCED prev=605,82,-18 … AFTER_TP=true` |

**第一次失败原样段（从地面，给修缮 16）**：
```
[02:45:31.254Z] [BUILD_STEP] index=608 kind=place phase=frame pos=605,82,-18 block=chiseled_deepslate
[02:45:31.5–32.3] [BUILD_VERTICAL_ACCESS_CANDIDATE] target=605,82,-18 id=vertical_0/1/2 stand=603,83,-18|604,83,-18|606,83,-18 endpoint=603,67,-18|… preview=partial previewSlices=8|7|9 pathNodes=6|7|9 rejection=preview_partial
[02:45:32.311Z] [BUILD_VERTICAL_ACCESS_PREVIEW_EXTENDED] target=605,82,-18 truncated=3 extended=3 clearColumn=3 budgetMs=4000 order=vertical_0(clear)|vertical_1(clear)|vertical_2(clear)
[02:45:36.445Z] … id=vertical_0_extended stand=603,83,-18 endpoint=603,67,-18 preview=partial previewSlices=100 pathNodes=6 scaffolds=none rejection=preview_partial   （vertical_1/2_extended 同型，各 ~4.1 s，previewSlices=100）
[02:45:44.721Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=605,82,-18 total=3 previewed=3 extended=3 legal=0 noPath=6 formalConflict=0 digRequired=0 previewError=0
[02:45:44.724Z] [BUILD_VERTICAL_ACCESS_CANDIDATE_FALLBACK] target=605,82,-18 restricted=3 reason=restricted_candidates_exhausted
[02:45:45–57] 41 个 fallback 候选（stand 全在 y80，endpoint 全在 y67）preview=partial previewSlices=7–8 pathNodes=5–12 scaffolds=none
[02:45:57.832Z] [BUILD_VERTICAL_ACCESS_PREVIEW_EXTENDED] target=605,82,-18 truncated=41 extended=4 clearColumn=29 budgetMs=4000 order=vertical_0(clear)|vertical_1(clear)|vertical_2(clear)|vertical_3(clear)
[02:46:01–14] vertical_0..3_extended preview=partial previewSlices=100（四行 partial ×100 片，预算 4 s 耗尽）
[02:46:14.319Z] [BUILD_VERTICAL_ACCESS_CANDIDATES] target=605,82,-18 total=41 previewed=41 extended=4 legal=0 noPath=45 …
[02:46:14.590Z] [Task:build_blueprint#4] fail: no_reachable_build_stance_after_vertical_access_candidates
```
`[状态]` 全程 `x=597.50 y=67.00 z=-21.50`（未动）；`MOVE_PATH_STATUS` 0 行（没走到站位那一步）；**`scaffolds=none` 全部**——从地面到 y83 城垛顶 16 格高差，候选一律没有脚手架方案（第 22 轮同型：临时参照/高处站位不带脚手架方案），预演在 `previewSlices=100` 处截断。**这是修缮 12/13 的补算边界在门楼真身第二次出现**（第 22 轮 `604,81,-10` 一次）。

**三败停手 `608,82,-15`——链条缺一环**（**已证实**）：

| 尝试 | 她在 | 结果 | 脚本 |
|---|---|---|---|
| 1（#6 续，02:47:46.5–47.1，**0.6 s**） | 城垛顶 `602.64,83,-17.5`（站 `602,82,-18` 木柱顶） | `total=1`（`stand=608,83,-16 endpoint=606,83,-18 preview=noPath pathNodes=4`）→ fallback `total=41 noPath=41` → fail | `STAND_TIMEOUT count=1` `FAIL_TRACK consec=1 heightDiff=-1` |
| 2（#7，02:47:53–02:48:29，33 s，其中补料白跑 32 s） | 同上 `moved=0` | 同型 | **`RESCUE_PINNED target=608,82,-15 consec=2 moved=0 heightDiff=-1 her=602.6,83.0,-17.5`** → `停止任务` → `ACTION rescue_tp_site reason=pinned` → `POS_AFTER_RESCUE 597.5,67,-21.5` → `DRAIN tag=after_rescue lines=12 staleFailsMarked=0` → 续建 |
| 3（#8，02:48:34–02:49:12，38 s） | 地面 `597.5,67,-21.5` | 与 `605` 从地面完全同型：restricted 1 → `PREVIEW_EXTENDED truncated=1 clearColumn=1` → fallback 41 → `PREVIEW_EXTENDED truncated=41 extended=4 clearColumn=29` → 四行 partial ×100 → `legal=0 noPath=45` → fail | `CYCLE_5` → **`GIVE_UP repeated_target=608,82,-15 tps=0`** → `停止任务` → tp `599 68 -19` → 退出 |

**为什么传送没轮到**：我的 #74 触发条件是"站位超时事件计数 ≥2"，救援后我把计数预置为 1、等下一次站位事件（`REPOSITION_REJECT` 或任务失败）再传。但这块砖从地面**没有任何站位事件**（候选全 noPath，直接任务失败），第一个到达的事件就是第三次失败，而三败判定在传送之前执行。任务书写的链条是"救援 → 地面 → 高度差 ≥10 → 传送回屋顶另一格"（两步连做），我拆成了两段。**责任在脚本**。若两步连做，第三次尝试会从 `608,82,-16` 木柱顶（RCON `dark_oak_log` Test passed，目标北邻）开始，与 `605` 的第三次同型（后者 0.6 s 放上）。存档：`608,82,-15 terminal_failed retry.count=3`。

**修缮 14 记数**（**已证实**）：`SCAFFOLD_PLACED` 0 / `STAIR_TEMP_REFERENCE_CLEAR` 0 / `CLEAR_APPROACH` 0 / `CLEAR_UNREACHABLE` 0 / `RESIDUE` 0——整轮没有搭过一格脚手架（所有候选 `scaffolds=none`）。收工扫描 0 格、清残留 **0/0/0**。修缮 14 本轮**无新样本**。

**补料白跑记账（给修缮 16）**（**已证实**）：

| 任务 | 开工→首个 `BUILD_STEP` | `CHEST_PATH_UNREACHABLE` | 她在 |
|---|---|---|---|
| #4 / #5 / #8 | 2.8 / 2.3 / 2.3 s | 0 | 地面（箱子可达，正常补料） |
| #6 / #7 | **32.4 / 32.4 s** | 2 / 2（`596,67,-23` + `594,67,-23`，各 ~15 s：`CHEST_PATH_ESCAPE_ATTEMPT waypoint=… direct_move_timeout` → `ESCAPE_REACHED` → `MOVE_PATH_STATUS status=noPath goal=59x,67,-23 allowList=0 excl=0`） | 城垛顶 `602.64,83,-17.5` |

整轮 `CHEST_PATH_UNREACHABLE` 4 次全在屋顶；`noPath allowList=0 excl=0` 4 行全是箱子路；`CHEST_RESCUE_SKIPPED_ON_ROOF` 2 次（脚本按③没把她传下去）。每个屋顶上的续建任务固定多花 ~30 s（比第 23 轮的 ~60 s 短：本轮每只箱子 15 s 就放弃）。

**#74 / #75 记账**（**已证实**）：`DRIVER_ROOF_TP` 1 次（`605,82,-18` → `606.5,83,-17.5`，之后砖放上 ✔）；`rescue_tp_site` 1 次（pinned，`608,82,-15`）；`ROOF_TP_SKIPPED` 0；`ROOF_FALL` 1 条 = 救援 tp 本身（`602.64,83 → 597.5,67`），无真实掉落，血量整轮 20；`lock_already_held` 0 / `DRIVER_BOT_RESTART` 0（修缮 15 的锁占死本轮仍无样本，只有上面的"开场跑家"链）；`STALE_FAIL_IGNORED` 0（②没被触发到，但 `DRAIN` 已按设计在救援后执行一次）。

**三线记数**（**已证实**）：后勤——`LOOK_SYNCED` 0（没放朝向块）/ `state_mismatch` 0 / 供电源步 0 / state_repair 27→27；修缮——`PREVIEW_EXTENDED` 6（全带 `clearColumn`，`clearColumn=3|29|1|29|3|29`）/ `preview=partial previewSlices=100` **19 行** / `stand_move_timeout_blacklisted` 0 / `MOVE_PATH_STATUS` 4（全 noPath 箱子路）/ `return_to_base` 3（全在开场、续建前）/ 掉屋顶 0；建造——`SCAFFOLD_PLACED` 0 / `EVENT_LOOP_STALL` 0 / `destination full` 0 / `BLOCKED_MATERIAL_SHORTAGE` 0 / `give` 0 / `chest_item_not_found` 0。`clear_obstruction` 5 行全是 `BUILD_PIPELINE` stages 列表（非事件）。

**三个数改前 / 改后**（**已证实**，存档 `reconciliation.at=02:48:36Z` + 收工存档）：verified 1171 → **1172**（+1：`605,82,-18` chiseled_deepslate，**传送后 1 步、救援后 0 步**）；pending 666 → 664；state_repair 27 → 27；terminal_failed 0 → **1**（`608,82,-15` retry 3，lastError `no_reachable_build_stance_after_vertical_access_candidates`）。run `ACTIVE`。**下次首撞**：`608,82,-15 chiseled_deepslate`（城垛东头，北邻 `608,82,-16` 木柱 verified）；两盏 y82 灯笼未到。

**收工**（**已证实**）：02:49:14 脚本 `GIVE_UP` → `停止任务` → tp `599 68 -19`（`FINAL_POS 599.5,67,-18.5`）→ 02:49:3x `Stop-Process` 本线 bot（pid 15484，`RemotePort=25565` + `bot.js`，排除自身；25566 无连接、修缮线无进程在线）→ `list` 0 人 → 残留 0 格无需清 → `forceload remove all`（复查 0）→ `doDaylightCycle` true（未改）→ `save-all flush` → `mc-server.ps1 stop` 经 RCON → `All dimensions are saved` / `RCON Listener stopped` → 25565/25575 无监听、`server.pid` 已删、本线 node/java 进程零（剩余 java 两只是 gradle 守护进程、node 一只是 Codex 自带，均非本项目）。停工到收 bot 约 20 s。

### 四、第 4 步：判定表

| 现象 | 本轮 | 去向 |
|---|---|---|
| 城垛顶首撞被救援接住、砖放上 | **`605,82,-18` 是**（#74 传送 → 0.6 s 放上）；**`608,82,-15` 否**（救援落地后传送没轮到 → 三败） | 脚本①②③都触发且按设计执行；**链条缺"救援后立刻传送"一环**——老板问题 1 |
| 救援后仍三败 | `608,82,-15`：第三次从地面零候选 | 不是"另一种病"，是同一种病 + 脚本缺环；抄段见上 |
| RESIDUE 名单外仍 dirt | 无样本（0 脚手架） | — |
| `lock_already_held` → 重启 | 0 / 0 | 无样本；开场跑家链给修缮 15 |
| 火把最末放后活塞再顶出 | 没盖到 | — |
| 补料白跑 ≥60 s/任务 | 屋顶上 32 s/任务（×2），地面 2–3 s | 记账交修缮 16 |
| 漏斗 `state_mismatch:facing` | 没轮到（#1208/#1209）；RCON 两只 `facing=down` | 交后勤 16 |
| 开场三个数偏差 >3 | 0 | — |
| （表外）城垛顶零候选 | 城垛顶 2 次 + **地面 3 次**（`605` ×2、`608` ×1）全零候选；地面侧四行 partial ×100 | 现场快照 + 三种起点原样段交修缮 16 |
| （表外）上线即跑家 128 格 | 锚点不随重启恢复 | 交修缮 15 |

### 五、第 5 步：文档

- 第 23 轮反馈整份挪到 `docs/协作/历史/第23轮-合修缮14与隔空拆成立与城垛顶三败.md`；`说明.md` 归档表补第 23 轮一行（开发同时改了 `说明.md` 的 ultracode 三档段 #79，一并提交）
- 台账 #78–#81 开发已追加，随本反馈一笔提交；他线文件显式提交：`docs/协作/后勤/给CC.md`（后勤 16 任务书）、`docs/协作/历史/后勤第15轮-漏斗互指编排层解法.md`（新，他线）、`docs/协作/建造/给CC.md`（本轮任务书）；`docs/协作/历史/修缮第15轮-*.md` 尚未出现，未提交；`docs/协作/后勤/CC反馈.md` 第 15 轮已随第 23 轮反馈提交（`dfc01698`），文件现已被后勤线覆写成**第 16 轮**进行中内容，按协议未动、未提交
- 主仓 main 提交后再快进一次

## 交接

**给修缮 16（城垛顶零候选）**：① 196 格快照表（上）：y82 城垛线 1 格宽、两侧 15 格空、y81 只有四根柱子；② 三种起点的失败原样段——城垛顶 `602.64,83,-17.5`（`total=1 noPath` 0.6 s 失败，`stand=608,83,-16 endpoint=606,83,-18 pathNodes=4 preview=noPath`）、地面 `597.5,67,-21.5`（restricted 3 + fallback 41，`scaffolds=none`，`PREVIEW_EXTENDED clearColumn=3/29`，四行 partial ×100，46 s 失败）；③ 同一站点靠"够得着就放"能放（`605` 从 `602.6,83` 放 `605,82,-18` 距离 ≈2.9，第 23 轮 `604,82,-18` ≈5.0）；④ 补料账（屋顶 32 s/任务、箱子路 `noPath allowList=0 excl=0`）。

**给修缮 15**：上线 0.7 s 即 `return_to_base TOO_FAR_FROM_BASE`（无锚点）→ 75 s 跑 128 格 → 3 个回家任务 17 行脱困链；本轮 `停止任务` 打断后锁**没有**被占（与第 22 轮不同：那次是 build 失败后被 `player_build_command` 打断、脱困链在 `nearby_exit` 途中）。

**给后勤 16**：`603,74,-2` / `604,74,-2` 真机均 `hopper[facing=down]`（`[facing=east]` / `[facing=west]` 均 failed）；离线 planId `d1919e8b1032`、漏斗 #1208/#1209（1 起数）、声明字段齐。

**世界现状**：屋顶 `604,79,-10..-6` / `604,81,-10..-6` 十只活塞朝下未伸出；`604,79,-11` air；`603,80,-5` air；城垛 `600,82,-13..-15`、`603..605,82,-18` chiseled_deepslate（`605` 本轮新）；`608,82,-15` air（terminal_failed）；残留土 0；两只漏斗朝下未动；她登出点 `599.5,67,-18.5`；昼夜规则与 forceload 恢复原状。

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`docs/协作/说明.md`（含 #79 段）、`AGENTS.md`（`git log` 确认自 `e7410995` 未变，沿用上轮通读）、`docs/协作/建造/给CC.md`（第 24 轮）、`docs/协作/建造/CC反馈.md`（第 23 轮）、`docs/协作/后勤/CC反馈.md`（第 15 轮：结构 + 交接段）、`docs/协作/老板决策台账.md`（diff）、`tests/clicked-face-reference-order.test.js`（L17–100、L286–340）、`tests/construction-resume-plan-id.test.js`（L1–60）、`utils/site-planner.js`（grep 行）、`package.json`（test 脚本）、`data/memory/construction-runs.json`（只读 ×3）、`.tmp/cc-r23-fort-driver.js`、`logs/bot-current.log`、`.tmp/cc-r24-driver-1.log`、`D:\code\MC-server\server.properties`（端口/密码键，密码只进进程环境）、`D:\code\MC-server\logs\latest.log`（尾部）

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `5ea7f8d3` | 合并 `feat/ops@878238d7`（零冲突，4 文件 +525/−8，含后勤 15 两笔） |
| （随后一笔） | 本反馈、`docs/协作/说明.md`（+1 行归档 + 开发的 #79 段）、`docs/协作/历史/第23轮-合修缮14与隔空拆成立与城垛顶三败.md`（新）、`docs/协作/历史/后勤第15轮-漏斗互指编排层解法.md`（新，他线）、`docs/协作/老板决策台账.md`、`docs/协作/后勤/给CC.md`、`docs/协作/建造/给CC.md` |
| （主仓） | `D:\code\MC` main 快进 `dfc01698 → 5ea7f8d3`（本反馈提交后再快进一次） |
| （工作区，非提交） | `dashboard/board-data.json`：合并前写回 HEAD 内容再由合并带到后勤 15 版本，内容与后勤线直接写入的一致（备份 `.tmp/cc-r24-board-data.json.backup`） |
| （未入库） | `.tmp/cc-r24-fort-driver.js`、`.tmp/cc-r24-driver-1.log`（utf8）/ `.ps.log`（UTF-16）、`.tmp/cc-r24-bot-stdout-1.log`、`.tmp/cc-r24-bot-log-2026-09-06T0244Z.log`、`.tmp/cc-r24-server-start.log`、`.tmp/cc-r24-manual-actions.log`；scratchpad：`compile-fort.js`、`snapshot-region.js`、`r24-parapet-snapshot.txt` |
| （世界） | 门楼 +1 步 verified（`605,82,-18`）；其余未动（未拆任何格、未碰漏斗、无残留） |

未改任何项目 JS 逻辑（合并带进来的除外；保姆脚本在 `.tmp/`）；未手改 `construction-runs.json`；未代放砖。

### 3. 执行的命令

`git status/log/merge-base/diff/merge 878238d7`（首次被工作区 `board-data.json` 拒、核对一致后写回 HEAD 内容再合并，零冲突）、`git show HEAD:… >`、`git -C D:\code\MC merge --ff-only`、`npm test` ×2（开场 @dfc01698 63 组、合后 @5ea7f8d3 64 组）、`npm --prefix D:\code\MC test` ×1、单跑 4 组、`node --check` 脚本、`node` 只读：离线编译门楼 ×2、解析存档 ×3、日志统计 ×2、区域快照 ×1；`cp` 归档；`scripts\start-detached.ps1` ×3（服务器 1、bot 1、保姆脚本 1）、`scripts\mc-server.ps1 status/stop`、`Stop-Process` ×1（bot 15484，按端口 25565 + `bot.js` 核对、排除自身）、`Get-CimInstance` / `Get-NetTCPConnection`；RCON（scratchpad `rcm.js`，密码运行时读）：`list` ×4、`time query` ×1、`forceload add/query/remove all`、`gamerule doDaylightCycle`（只查）、`execute if block` ×≈330（漏斗 8、区域快照 196 + 非空气 12 格逐块名 ~110、收工核对 5）、`data get entity LinXia Pos` ×1、`save-all flush`；保姆脚本自身：`tp LinXia` ×4（`initial_park` 1、`DRIVER_ROOF_TP` 1、`rescue_tp_site` 1、`stop_tp_worksite` 1）、`give` 0、`bot.chat` `debug_status` ×1 / `停止任务` ×3 / `build fort wall gate` ×5。
未执行：`npm install/ci/update`、`git add -A/.`、`git reset/clean/checkout --/restore/stash`、进 `MC-fix`/`MC-ops`、连 25566、改项目 JS、手改存档、代放砖、`setblock`（本轮一次都没用）、拆图纸格、碰漏斗、改 gamerule/time、手动 tp。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 @dfc01698 | `npm test` 63 组 | 退出码 0 |
| 合后勤 15 后 @5ea7f8d3 | `npm test` 64 组 + 单跑 4 组 | 均退出码 0 |
| 离线 | 门楼 planId `d1919e8b1032`、漏斗 #1208/#1209 + step id 一致、末 24 步 24/24 供电源 | 通过 |
| 主仓 @5ea7f8d3 | `npm --prefix D:\code\MC test` 64 组 | 退出码 0 |
| 真机 | 三个数偏差 0；`605,82,-18` 救援链 PASS（传送后 0.6 s 放上）；`608,82,-15` 三败（脚本缺环）；修缮 14 无样本；漏斗 RCON 两只朝下 |

### 5. 当前最新判定

- 第 1 步 合后勤 15：**完成**（`5ea7f8d3`，零冲突，grep 齐，64 组全绿，planId/漏斗序号/step id 复核通过，主仓快进 + 全绿）
- 第 2 步 快照：**完成**（漏斗 4 条 RCON + 196 格区域快照）
- 第 3 步：续建命中、三个数一致；首撞 `605,82,-18` 救援链**成功**（传送 1 次、放上）；`608,82,-15` **三败停手**（救援 1 次、传送未轮到，脚本缺环）；修缮 14 记数 0 样本；补料账、传送/救援/重启账、三线记数**完成**；清残留 0/0/0
- 第 4 步 判定表：**完成**
- 第 5 步 文档：**完成**（本笔；修缮 15 归档件未出现）

### 6. 生成文件 / 内存是否变脏

`acceptance/reports/latest-*`、`data/memory/task-memory.json`、`world-memory.json`：`npm test` 历史脏状态，未提交；`data/memory/construction-runs.json`：本轮真机更新（verified 1172 / pending 664 / state_repair 27 / terminal_failed 1），未提交、未手改；`dashboard/board-data.json`：现与 HEAD 一致（见上）；`logs/`：本轮一段日志（另存 `.tmp/`）；`.tmp/*`、`_backups/`、`probe/`、`acceptance/reports/building-case1-*`、`data/community-builds/cache/*` 他线或往轮产物，未动。

### 7. 是否建议提交

已提交合并一笔；本反馈 + 文档 + 他线文件随后一笔；主仓再快进一次。`data/memory/*`、`logs/*`、`.tmp/*`、`dashboard/*` 不提交。

### 8. 剩余风险

- `608,82,-15` terminal_failed retry 3，下轮首撞；从地面和城垛顶都零候选，**只有传送到 `608,82,-16` 木柱顶这一条路**——脚本要改成"救援落地后立刻传送回屋顶"两步连做（问题 1 选 A 即做），否则重演
- 城垛线上每一块都是同一种病：北段还剩 `601,82,-17`、`602,82,-17`、`606,82,-17`、`601,82,-16` 等，东段 `608,82,-13..-15`，屋顶 y83 一层 76 格全在上面——修缮 16 不修的话每块都要靠传送
- 上线即跑家：每次重启 bot 她都会先往老基地跑，靠脚本停车拉回；若哪次脚本晚起几分钟，她会跑出 589 格外（第 18 轮同型）
- 修缮 14 本轮 0 样本，"治好了"仍是第 23 轮 8 格的小样本结论
- #75 重启兜底仍未经真机验证（锁占死两轮都没再出现）
- 存档 `planId` 字段 `82c288475728` 与编译值 `d1919e8b1032` 不一致，来源 UNKNOWN（续建匹配不依赖它，但值得开发看一眼是不是续建时用了别的编译参数）
