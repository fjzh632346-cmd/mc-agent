# 建造 第 15 轮反馈

- 线: 建造
- 轮次: 15
- 日期: 2026-09-03
- 状态: 已完成（长活起法的"活过会话退出"一项按任务书标 UNKNOWN，待下轮开场核）
- 分支 / HEAD: refactor/building-blueprint-ir-v1 / 1f097855（合并笔；本反馈、启动脚本与文档随后一笔，主仓快进到同一 HEAD）
- 实际运行模型 / 强度: Claude Fable 5.1（`claude-fable-5-1`），思考强度为会话默认档，本端读不到具体档位；任务书建议的 Opus 5 + high 未生效于本会话

---

## 一句话结论

后勤的档案瘦身收进主干并在真档案上验过（一百零三兆缩到十一兆、门楼记录原样、再读不再搬）；找到了一种不随会话死的启动方式并用它跑通了冒烟；工地五处残留土块按图纸全清；门楼一块砖没动。

## 白话摘要

第一件事，合并。后勤第八轮那笔"施工档案瘦身"收进来，零冲突，测试五十八组增到五十九组全部通过，主干仓库快进后再跑一遍同样全绿。

第二件事，长活起法。查清了上一轮全灭的机制：这个工具会把自己和它起的所有子进程装进同一个"作业对象"，工具一退出整个作业里的进程一起被杀，所以之前无论怎么起都逃不掉。试了三种起法：常规两种都在作业里，只有"通过系统管理接口拉起"这一种在作业外——用它起的服务器和她本人这次都在作业外。已经写成一个小脚本并配了说明。留了一个带标记的傀儡进程和一个每五秒写一次的心跳文件，下轮开场先看它活没活着，那才是最终证据。计划任务那种起法被本工具的安全审查拦下，没有试。

第三件事，验搬家。趁服务器和她都不在线，直接对真档案触发了一次搬家：四十九条已完工或已放弃的记录搬到旁边的文件夹，主档从一百零三兆缩到十一兆，搬家前整份备份留在原处；门楼那条活跃记录一千八百六十三步原样留在主档，续建匹配仍命中；再读一次一个字节都没变、不再重复搬。之后用新起法开服务器和她做冒烟：四个上线信号齐，日志里没有再出现搬家记录、没有报错，她就站在工地内空闲着——按任务书没有下施工令。

第四件事，清残留。五处土块对照图纸全部是空位，逐块确认是土再清成空气，区块常驻成对加撤。

下一步：下轮开场先看傀儡进程；门楼续建等修缮线修站位（第四十八条）。本轮没有需要拍板的事。

## 需要老板拍板的问题

无

## 技术细节

### 〇、开场基线

- 开场 HEAD `dcc1107e`，`npm test` 58 组全绿（80 行通过）——**已证实**
- 第 14 轮反馈整份挪到 `docs/协作/历史/第14轮-三线合并与门楼续建卡站位.md`（`cmp` 一致）——**已证实**
- `81e93393` = `feat/ops` 尖端，merge-base `367bbdbe`，`HEAD..81e93393` 恰一笔——**已证实**
- 开场时机器上有别线的 java 服务器 + `node bot.js`（都在 Job 里，端口未查），未动；到第 3 步真做迁移时它们已经退出（进程表零 java / 零 bot.js），迁移无并发——**已证实**

### 一、第 1 步：合并（完成）

- `git merge 81e93393` → 合并提交 **`1f097855`**，ort，**零冲突**：8 文件 +594/−36（`systems/construction-run-store.js` +170、`scripts/verify-construction-resume-evidence.js` +14、`tests/construction-run-archive.test.js` 新 362 行、`package.json` +4/−2、`.gitignore` +5、`dashboard/` 3 处）——**已证实**
- `npm test`：**59 组**全绿，81 行通过；单跑 `construction-run-archive`（11 组）、`construction-resume-plan-id`（3 组）PASS——**已证实**
- 主仓 `merge --ff-only` → `dcc1107e..1f097855`；`npm --prefix D:\code\MC test` 全绿 81 行——**已证实**

### 二、第 2 步：长活起法（机制已证实；跨会话存活 UNKNOWN 待下轮）

**机制**（P/Invoke `IsProcessInJob(hProcess, NULL)`，PowerShell `Add-Type`）：

- 本工具 shell 的祖先链：`powershell.exe IN_JOB ← claude.exe IN_JOB ← claude.exe IN_JOB ← explorer.exe NOT_IN_JOB`——Claude Code 把自己的进程树放在一个 Windows Job Object 里，所有后代继承；工具退出时作业被关（第 14 轮全灭）——**已证实**（Job 的 `KILL_ON_JOB_CLOSE` 标志无法直接读出，是由第 14 轮行为反推的**合理推断**）
- 三种起法各起一个 `node -e` 傀儡：

| 起法 | PID | Job | 父进程 | 备注 |
|---|---|---|---|---|
| `Start-Process` | 3448 | **IN_JOB** | powershell（工具 shell） | 第 14 轮用的就是它 |
| `cmd /c start "" /B` | 25124 | **IN_JOB** | cmd | 同上 |
| WMI `Win32_Process.Create`（直接起 node） | 36012 | **NOT_IN_JOB** | `WmiPrvSE.exe` | 起来时探到在作业外，但**几分钟内自行退出**；再试一只同样直起的（pid 5708）6 s 内消失、心跳文件没写出来。无事件日志、无崩溃记录，原因 **UNKNOWN**（**合理推断**：WMI 直起的进程没有可用的 stdio 句柄） |
| WMI + `powershell -NoProfile -EncodedCommand` 包装（`scripts/start-detached.ps1`） | 包装 5620 → node **25740** | **NOT_IN_JOB** | WmiPrvSE → powershell | **存活**：心跳文件 `.tmp/cc-r15-puppet-wmi2.heartbeat` 14:03:18Z 起每 5 s 刷新，写反馈时 14:07:14Z 仍在刷 |
| `schtasks /Create … /Run` | — | — | — | **未试**：命令被本工具的 auto-mode 安全分类器拒绝（创建计划任务属持久配置），按提示不绕 |

- **选定起法**：WMI + PowerShell 包装，落成 `scripts/start-detached.ps1`（`-Command`、`-Env <本会话变量名列表>`、`-WorkingDirectory`、`-LogFile`；变量值从当前会话复制进包装脚本，`-EncodedCommand` 走 UTF-16 所以 `MC_WORLD_ID='building-A:新的世界'` 原样到达）；说明写进 `docs/CODEX_BOT_STARTUP.md`「Start LinXia Bot」下新小节
- 用它起真机（第 3 步冒烟）：服务器包装 31848 → `java.exe` **1432 NOT_IN_JOB**；bot 包装 11976（父 WmiPrvSE）→ `node bot.js` **31996 NOT_IN_JOB**——**已证实**
- **跨会话验证（UNKNOWN，待下轮开场第一件事）**：留下的傀儡 = 包装 PID **5620** + 工作进程 PID **25740**，命令行含标记 `mc-r15-puppet-wmi2`，心跳文件 `D:\code\MC-blueprint-ir-v1\.tmp\cc-r15-puppet-wmi2.heartbeat`（每 5 s 写 ISO 时间）。判据：下轮开场 `Get-CimInstance Win32_Process -Filter "Name='node.exe'"` 里 25740 仍在且心跳时间接近当下 → 起法成立；两只对照傀儡 3448 / 25124（在 Job 里）预期已死。核完后由下轮 `Stop-Process -Id 25740` 收掉
- 副作用：本轮起的服务器/bot 在作业外，**不会随会话自动清理**——本轮收尾已手动 `Stop-Process 31996` 与 RCON 优雅停服；以后用此起法必须记得自己收

### 三、第 3 步：验自动搬家（真档案，离线触发；完成）

**3.1 离线触发**（脚本 `.tmp/cc-r15-migrate.js`，按后勤用例 1 的方式 `isWritable: () => true` 绕过连接闸门；触发前先 `cp` 一份到 `.tmp/construction-runs.pre-migrate-backup-r15.json`，md5 与原件相同）：

- 迁移日志整行：`[CONSTRUCTION_RUN_ARCHIVE_MIGRATED] runs=49 fields=steps,frozenBlueprintIR backup=construction-runs.pre-archive.json archiveDir=construction-runs-archive ms=502`（首次 `load()` 合计 1160 ms）——**已证实**

| 项 | 结果 |
|---|---|
| 备份 `data/memory/construction-runs.pre-archive.json` | 102,737,035 B，md5 `dec3da08…`，**与迁移前主档逐字节相同** |
| 旁挂 `construction-runs-archive/` | **49 份**，50,261,119 B（47.9 MB） |
| 主档 | 102,737,035 → **11,474,440 B**（11.5 MB，−88.8%），`schemaVersion` 1 → 2，50 条 run |
| 主档形态 | 43 ABANDONED 瘦记录 + 6 COMPLETED 瘦记录 + **1 ACTIVE 全量**（门楼） |
| 门楼 `d2f3cbb572ceb6b7` | `ACTIVE` / terminal null / `steps` **1863**（verified 1033 / state_repair 38 / pending 792）/ `frozenBlueprintIR` 1233 块，**原样在主档** |
| 对账 | `constructionRunCompatibility` `{ok:true}`，`findActiveCompatible` → 命中门楼 |
| 补水 | 抽查终态 run `c57fec1bd503f07e`：主档无 `steps`，`getRun()` 回来 1330 步 + 冻结图纸 |
| 幂等 | 第二次 `load()` 66 ms、MIGRATED 0 行、主档 md5 `ecaa0ae4…` 不变、备份与旁挂不变 |

- 磁盘：备份 102.7 + 旁挂 50.3 + 主档 11.5 = **164.5 MB**（后勤估 157）；另有我在 `.tmp/` 的兜底副本 102.7 MB。**备份未删**（开发确认续建成功前不裁）；`.tmp/` 那份可随时删
- `.gitignore` 生效：`git status` 里旁挂目录与备份都不出现——**已证实**（`construction-runs.json` 本身一直是未跟踪文件）

**3.2 真机冒烟**（服务器与 bot 都用 `start-detached.ps1` 起，不下施工令）：

- 服务器 java 1432（作业外）；bot 31996（作业外），五个环境变量齐；ONLINE-GATE：`[端口] 使用 .env 指定端口: 25565` 13:58:24.191Z、`[TickLoop] started` 13:58:26.898Z、`[Bot] 上线成功` 13:58:26.911Z、`[DEBUG_STATUS]` 14:03:28.567Z（探针 `.tmp/cc-r15-smoke-probe.js` 以 accept_tester 发 `debug_status`，随后发 `停止任务`，她答"当前没有正在执行的任务"）——**已证实**
- `CONSTRUCTION_RUN_ARCHIVE_MIGRATED` **0 行**（已迁过）；`[ERROR]`/exception **0**；`EVENT_LOOP_STALL` **0**；`[连接] 自动重连=开 上限=20 次`——**已证实**
- 她的位置 `(601.46, 71, -15.37)`，与第 14 轮最后一条状态一致（工地内）；**8 分钟在线期间没有生成任何 `return_to_base`**，一直"空闲"停在原地——与第 12/14 轮"上线 1 s 就被拉回老基地"不同，原因 **UNKNOWN**（**合理推断**：修缮 8 的工地锚点或其 180 s 窗口之外的某条件把 TOO_FAR_FROM_BASE 压住了；本轮不追）
- 冒烟期间主档 md5 未变（未下施工令，bot 没读写档案）；`logs/bot-current.log` 84 行
- 收尾：`Stop-Process -Id 31996`（作业外必须手动收）；`mc-server.ps1 stop` → 07:06:06 本地 `All dimensions are saved`、`not running`——**已证实**

### 四、第 4 步：清工地残留（完成，5/5 清）

对照缓存图纸（原点 599,67,-19；`origin` 字段 0,0,0，1233 块索引）：

| 坐标 | 图纸局部坐标 | 图纸 | 清前核 | 动作 | 清后核 |
|---|---|---|---|---|---|
| 603,67,-17 | 4,0,2 | **空** | dirt ✔ | `setblock … air` | air ✔ |
| 603,68,-17 | 4,1,2 | **空** | dirt ✔ | 同上 | air ✔ |
| 598,67,-23 | −1,0,−4 | 空 | dirt ✔ | 同上 | air ✔ |
| 597,67,-22 | −2,0,−3 | 空 | dirt ✔ | 同上 | air ✔ |
| 597,68,-22 | −2,1,−3 | 空 | dirt ✔ | 同上 | air ✔ |

`forceload add 588 -26 611 5` / `forceload remove 588 -26 611 5` 成对（query 结果 none）；`doDaylightCycle` 仍为 true；别的一格未动——**已证实**

### 五、第 5 步：文档（完成）

- `docs/协作/说明.md` 归档表补第 14 轮一行（`:234`）
- `docs/协作/老板决策台账.md`（#48–#50 与开发确认段）随本反馈一笔提交
- 主仓历史目录里已有 `修缮第8轮-工地锚点与就近卸货.md`、`后勤第8轮-施工档案瘦身与看板收口.md`，一并提交；`后勤第7轮-…` 与修缮第 1–7 轮等其它线的归档件仍是未跟踪状态，本轮未越权提交

## 按 AGENTS.md 第 11 节

### 1. 读过的文件

`AGENTS.md`、`docs/协作/说明.md`、`docs/协作/建造/给CC.md`、`docs/协作/建造/CC反馈.md`（第 14 轮）、`docs/协作/后勤/CC反馈.md`（第 8 轮 2.6/2.7 及冲突面）、`docs/协作/老板决策台账.md`、`tests/construction-run-archive.test.js`（构造方式）、`systems/construction-run-store.js`（迁移 API 表面）、`docs/CODEX_BOT_STARTUP.md`、`data/community-builds/cache/blueprints/github-ifnullthenvoid-fort-wall-gate.json`（只读，残留坐标核图）、`data/memory/construction-runs.json`（迁移前后）、`D:\code\MC-server\server.properties`（只读取 RCON 端口/密码，密码未落盘）、`D:\code\MC-server\logs\latest.log`（只读）、`logs/bot-current.log`

### 2. 改动的文件

| 提交 | 内容 |
|---|---|
| `1f097855` | 合并 `feat/ops@81e93393`（8 文件） |
| （随后一笔） | 新增 `scripts/start-detached.ps1`；`docs/CODEX_BOT_STARTUP.md`（+1 小节）；本反馈；`docs/协作/建造/给CC.md`；`docs/协作/老板决策台账.md`；`docs/协作/说明.md`（+1 行）；`docs/协作/历史/第14轮-三线合并与门楼续建卡站位.md`（新）；`docs/协作/历史/修缮第8轮-工地锚点与就近卸货.md`、`docs/协作/历史/后勤第8轮-施工档案瘦身与看板收口.md`（他线归档件，按任务书一并入库） |
| （主仓） | main 快进 `dcc1107e → 1f097855`，反馈笔后再快进一次 |
| （未入库） | `data/memory/construction-runs.json`（迁移后 11.5 MB）、`construction-runs.pre-archive.json`、`construction-runs-archive/`（49 份）；`.tmp/`：`construction-runs.pre-migrate-backup-r15.json`、`cc-r15-migrate.js`、`cc-r15-smoke-probe.js`、`cc-r15-server-start.log`、`cc-r15-bot-stdout.log`、傀儡日志与心跳文件 |
| （世界） | 5 处 dirt → air；其余零改动 |
| （进程） | 留活：傀儡 5620/25740（`mc-r15-puppet-wmi2`）；对照 3448、25124 预期随会话死 |

### 3. 执行的命令

`git status/log/merge-base/diff/merge <显式提交号>/add <显式文件>/commit`、`git -C D:\code\MC merge --ff-only …` + 只读 `log`、`npm test` ×2（58 / 59 组）、`npm --prefix D:\code\MC test` ×1、单跑 2 组、`node .tmp/cc-r15-migrate.js`（真档案迁移，一次）、`scripts\start-detached.ps1` ×3（服务器、bot、傀儡）、`Invoke-CimMethod Win32_Process Create` ×2（直起傀儡）、`Start-Process` / `cmd /c start` 各 1（对照傀儡）、`Add-Type` P/Invoke `IsProcessInJob`、`Get-CimInstance Win32_Process`、`Get-WinEvent`（只读，无结果）、`Stop-Process -Id 31996`、`scripts\mc-server.ps1 stop/status`、RCON：`forceload add/remove/query`、`execute if block` ×10、`setblock … air` ×5、`gamerule doDaylightCycle`（查询）、`list`。
未执行：`schtasks`（被分类器拒绝）、`git add -A/.`、`stash`、`reset`、`clean`、`checkout --`、任何 sync、`npm install`、`taskkill /IM node.exe`、`build …` 施工令、`give`、对 607,73,-16 / 别墅 / 材料箱的任何操作、对 25566 或其他 worktree 的访问、删除任何备份。

### 4. 测试结果

| 时点 | 命令 | 结果 |
|---|---|---|
| 开场 | `npm test` 58 组 | 退出码 0，80 行 |
| 合并后 | `npm test` 59 组 | 退出码 0，81 行 |
| 主仓 @1f097855 | `npm --prefix D:\code\MC test` | 退出码 0，81 行 |
| 单跑 | `construction-run-archive`（11）、`construction-resume-plan-id`（3） | PASS |
| 迁移 | 真档案 load ×2 | MIGRATED 1 行 / 0 行；md5 幂等；门楼原样；对账命中 |
| 冒烟 | 四信号 + 0 MIGRATED + 0 error + 0 stall | PASS |

### 5. 当前最新判定

- 第 1 步 合并：**完成**（`1f097855`，58→59 组，main 快进 + 主仓全绿）
- 第 2 步 长活起法：机制**已证实**（Job Object）；起法**选定并落地**（`scripts/start-detached.ps1`，WMI + 包装，作业外）；**跨会话存活 UNKNOWN**，傀儡 25740 留活待下轮核；`schtasks` 未试（被拒）
- 第 3 步 验搬家：**完成**（离线触发五项全核 + 冒烟无重复迁移）
- 第 4 步 残留：**完成**（5/5 清，图纸全空）
- 第 5 步 文档：**完成**
- 门楼：一格未动；run `ACTIVE` verified 1033，下次续建仍先撞 607,73,-16（等修缮 10 / #48）
- 服务器：已优雅停；bot：已停；LinXia 登出点 (601.46,71,-15.37) 工地内

### 6. 生成文件 / 内存是否变脏

`data/memory/construction-runs.json` 被迁移改写（任务本意，不提交；备份两份在位）；`task-memory.json` / `world-memory.json` 随冒烟滚动（不提交）；`latest-*` 仍是第 11 轮遗留；`logs/bot-current.log` 为本轮冒烟（84 行）；`.tmp/` 新增见上表。其它线的未跟踪文件未动。

### 7. 是否建议提交

已提交合并笔；本反馈 + 启动脚本 + 文档 + 两份他线归档件随后一笔；主仓再快进一次（本笔无 JS 改动，不重跑主仓测试）。生成文件、memory、logs、`.tmp` 不提交。

### 8. 剩余风险

- 起法的最终证据在下轮开场（傀儡 25740 + 心跳）；WMI 直起 `node -e` 会自灭的原因未查明，靠包装绕过，若包装 powershell 有别的退出路径（例如系统注销）同样会带走工作进程——**合理推断**
- 作业外进程不再随会话清理：每次真机后必须手动 `Stop-Process` bot、RCON 停服，否则会留下常驻进程（本轮已收干净，傀儡除外）
- `schtasks` 路线未验证；若下轮傀儡死了，备选只剩"老板在独立终端起"
- 迁移后档案首次真机续建的检查点耗时（后勤估 2.1 s → 0.28 s）本轮没机会测（未施工）
- 冒烟时她没被拉回老基地的原因未查（对续建是好事，但行为变了要留意）
- 磁盘多占 164.5 MB + `.tmp` 兜底 102.7 MB；续建成功后可按开发确认裁备份