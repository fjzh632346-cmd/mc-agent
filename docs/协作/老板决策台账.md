# 老板决策台账（三线共用，只追加不改写）

由开发维护。任务书每轮覆盖重写，决策的正式出处容易丢，从 2026-09-02 起统一记在这里；各线引用决策编号时以本文件为准，看板补录也以此为出处。旧账 #1–#23 散在各轮任务书第 0 节（见 `docs/协作/历史/`），暂不回填。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 24 | 2026-09-01 | 孤本图纸入库存档 + 两份 .tmp 备份退役；全量 sync 禁令继续有效 | 建造第 11 轮任务书第 0 节 | ✅ 建造 10 执行 `1a52c434` |
| 25 | 2026-09-01 | 老板游戏名 `action` 加入 ops | 同上 | ✅ 生效 |
| 26 | 2026-09-01 | 旧草名图纸过版本闸门：维持哨兵报警、人工确认改名后放行，**不改闸门代码** | 同上 | ✅ 后勤 5 执行 |
| 27 | 2026-09-01 | 垫方块爬坑结案（1.21.8 下 7/9），从修缮候选池划掉 | 同上 | 结案 |
| 28 | 2026-09-01 | 别墅室内摆件（9 展示架/4 村民/1 猫）不补，记档为已知差异 | 同上 | 结案 |
| 29 | 2026-09-01 | 别墅屋外 43 块土堆一律不动 | 同上 | 结案 |
| 30 | 2026-09-01 | 住宅资格门槛各松一格：`minWidth` 18→17、`usableInteriorVolume` 16→15 | 同上 | ✅ 建造 11 执行 `d1920d41` |
| 31 | 2026-09-01 | 下一栋先盖门楼（fort-wall-gate），两条现代小屋候选排门楼之后 | 同上 | 执行中（门楼 33%） |
| 32 | 2026-09-01 | 拒挖先这样：无处可站时拒绝清障=接受，极个别缺块真出现再排修 | 修缮第 6 轮任务书第 0 节 | 挂候选池 |
| 33 | 2026-09-01 | 东缘沟修：逃生梯加「凿顶」档 | 同上 | ✅ 修缮 6 执行 `a044997c` |
| 34 | 2026-09-01 | 合并轮时点：等建造 11 交卷审毕后再开 | 建造第 12 轮任务书第 0 节 | ✅ 建造 12 执行 `545b69a5` |
| 35 | 2026-09-01 深夜 | **重跑稳定性验收·打折版**：小屋、门楼两档做「同一蓝图连续 3 次干净通过」——出新根因→修掉、计数清零；只出已记档低危边角→不清零照算；连 3 次零新根因→该档标记稳定。从小屋轮起写进任务书验收；门楼档的三连在门楼首盖完工后再排；重跑可离线复核+真机穿插省时。**别墅不重盖**（维持「长跑用例」定位）；**泛化生成不做**，等执行层稳定后再议（届时首选参数化零件库 generator、输出同格式蓝图 IR、拿小屋练手；「从样本学风格」暂缓） | 老板与 chat Claude 对话 + 开发评估 + 老板拍板；本台账为正式出处 | 待落实（小屋轮任务书） |
| 36 | 2026-09-02 | **门楼先修后建**：修缮先修掉「假死掉线」与「放置顺序死结」两个根因，建造线再续建门楼；门楼不赶工期 | 建造第 12 轮任务书第 0 节 | 执行中 |
| 37 | 2026-09-02 | **合并轮与修缮线并行**：建造 12 合并 `feat/fix` 只到 `a044997c`；修缮 7 同时开工，新产出下次合并收 | 同上 | ✅ 合并部分完成 |
| 38 | 2026-09-02 | **不加新线；后勤线兼「离线修缮」**：内存开不起第三台服务器（后勤 6 实测：双线运行剩 1.37 GB，第三线需 1.2–1.5 GB）；后勤线修不需进游戏就能验证的执行层 bug（规划/顺序/状态类），标准同修缮线；真机验证由开发转给带游戏的线 | 后勤第 6 轮任务书第 0 节 | ✅ 生效（后勤 6 首修放置顺序） |
| 39 | 2026-09-02 | 建造 13 = 收后勤 6 成果 + 续建准备（不进门楼工地）；后勤 7 = 离线修「悬空放置」（unstable_air） | 本台账 | 执行中 |
| 40 | 2026-09-02 | 自动重连 20 次都失败后**进程退出**（维持修缮 7 默认；挂着不退就是假死本身） | 本台账（修缮 7 反馈提问） | ✅ 生效 |
| 41 | 2026-09-02 | 修缮 8 = 「施工中被拉回老基地」根治（INVENTORY_FULL / TOO_FAR_FROM_BASE 触发 return_to_base 中断施工） | 本台账 | 执行中 |
| 42 | 2026-09-02 | 门楼续建节奏：建造 14 第一步先合修缮 7 `c1274c5a`（及后勤 7 若已交卷），全绿后**同一轮**续建——本次豁免「合并单独开轮」，理由：只 1–2 笔、冲突面已知 | 本台账 | ✅ 已下发（范围由 #43 扩大） |
| 43 | 2026-09-02 | **建造 14 合并范围扩大**：修缮 7+8（`feat/fix@b08066fb`）与后勤 7（`feat/ops@5dfb0ff1`）一起收，全绿后同一轮续建门楼。理由：修缮 8 修的正是门楼工地会咬人的「施工中被拉回老基地」（老基地离工地 589 格，第 12 轮冒烟 1 秒即被拉走） | 本台账（建造 13 / 后勤 7 / 修缮 8 交卷后） | 执行中 |
| 44 | 2026-09-02 | 夜里 `NIGHT_UNSAFE` 那条回家（天黑 + 离基地 > 24 格）**纳入**修缮 8 的「工地就是家」口径，排修缮 9 首件；建造 14 续建期间用 `doDaylightCycle false` 先挡住 | 本台账（修缮 8 反馈提问 1） | 执行中 |
| 45 | 2026-09-02 | 修缮 9 = **放置前潜行名单补全**（漏斗、中继器、比较器、发射器等交互块；红石线/铁轨等扁块潜行能否救回由真机定），目标门楼末尾两块 `603,69,-11` / `604,75,-2` 真正盖上 | 本台账（后勤 7 第四节第 1 条建议） | 执行中 |
| 46 | 2026-09-02 | 后勤 8 = **施工档案 `construction-runs.json` 瘦身**（102.7 MB，43 个终态 run 各带整份图纸；终态 run 大块移出主档、旧格式自动迁移带备份、对账不变量单测） | 本台账（修缮 7 记入、后勤 7 建议升优先级） | 执行中 |
| 47 | 2026-09-02 | **任务书标注建议模型与思考强度**：开发每轮下发任务时按复杂度写「建议模型 / 思考强度」，原则「该花就花、能省就省」；分档见 `docs\协作\说明.md`「模型与思考强度」节；CC 开场把实际运行的模型/强度写进反馈头部 | 老板要求（chat） | ✅ 生效（建造 14 / 修缮 9 / 后勤 8 起） |
| 48 | 2026-09-03 | 门楼卡点 `607,73,-16`（八个站位全部走位超时、同点三次）：**交修缮线修站位，建造线不代放** | 建造 14 反馈提问 1 | 执行中 |
| 49 | 2026-09-03 | 修缮 10 = 「找不到落脚点」根治（`stand_move_timeout` / `no_reachable_build_stance_after_vertical_access_candidates`，顺带堵「停止任务后脱困链继续跑」）；短暂卡顿（`EVENT_LOOP_STALL` 最长 10.7 s）排修缮 11 | 本台账（建造 14 反馈 2.4 / 2.6） | 待修缮 9 交卷后下发 |
| 50 | 2026-09-03 | 门楼等修缮期间，建造 15 = 收后勤 8 `81e93393` + 在真档案上验自动搬家 + 清工地 5 处残留土块 + 找到「不随会话死」的长活起法；**不盖砖** | 本台账 | ✅ 建造 15 执行 `1f097855`（起法跨会话存活由建造 16 核） |
| 51 | 2026-09-03 | **终验忠实度尺子与续建对账尺子同步放宽**：红石线四向连接态、楼梯 `shape`、`powered/power/extended/triggered/enabled/locked/attached`、红石类 `lit` 不再算差异；**转发器 `delay`、比较器 `mode`、`inverted`、`open` 继续比**（真能补的本事留着显形）。口径与已放过的酿造台瓶子/讲台书/栅栏连接一致；她将来学会摆这些时再收紧 | 后勤 9 反馈提问 1（选项 A） | ✅ 后勤 10 执行 `f1f4472a`（共享名单 `utils/derived-placement-state-keys.js`；门楼终验口径 143→5；待下次合并入主干） |
| 52 | 2026-09-03 | **建造 16 = 开场核傀儡进程（长活起法最终证据）+ 收后勤 9 `feat/ops@11e1c03e` + 合并后离线复核门楼续建匹配（planId `15d31d360064`→`b262d82f1416`）；不进游戏、不盖砖**。理由：后勤 9 没碰修缮 9 在改的文件，早收一笔下次大合并少一笔；起法结论早拿到 | 本台账（建造 15 / 后勤 9 交卷后） | ✅ 建造 16 执行 `99ae1fde`（傀儡试验因宿主未退出而未发生，r16 傀儡留活待核） |
| 53 | 2026-09-03 | **后勤 11（40 处朝向失控）与修缮 10（找不到落脚点）并行开工，按函数划地盘**：两线都改 `actions/build.js`——后勤只碰 `placementLookDirection` / `orientBotForPlacement` / `lookTowardDirection` / `expectedPlacementStateKeys` / `statefulPlacementProfile` + 新增语义表与 delay/mode 调档；修缮只碰站位 / 路径 / 垂直通道 / 走位超时 / 黑名单 / 脱困；`shouldSneakForPlacementReference` / `findReferenceBlocksForPlacement` 两线冻结。谁越界谁手工并。后勤 11 须从含修缮 9 的主干起步（建造 17 先合） | 本台账（修缮 9 交卷后，开发提问） | 执行中（建造 17 已合 `af8393b6`；后勤 11 首发因起得早停手，重发） |
| 54 | 2026-09-03 夜 | **长活进程「脱离会话独立存活」起法放弃**：建造 17 证伪（r16 三只傀儡在全机关机前 47 分钟已消失，时点贴着 `claude.exe` 退出）。**轮内照旧 `scripts\start-detached.ps1`**（活到宿主退出，够一轮，CC 收工自己收进程）；要跨会话留着的服务器（老板自己旁观）由老板自己开窗口起；不再花轮验证 `schtasks` | 建造 17 反馈提问 1（选项 A） | ✅ 生效（`说明.md` 真机验收一节补记） |
| 55 | 2026-09-03 夜 | **门楼马上续建，后勤并行**：建造 18 = 合修缮 10 `feat/fix@ee30d137` + 立刻续建门楼真机验 `607,73,-16`；后勤 11 原样重发（塞进修缮 10 真机朝向表）同时跑；本轮朝向类方块仍 `state_mismatch:facing` 属预期只数数；建造 19 收后勤 11 后补。理由：修缮 10 是不是真治好门楼当天就有答案，不等后勤一轮 | 本台账（三线交齐后，开发提问） | 执行中 |
| 56 | 2026-09-03 夜 | **修缮 11 = 排除集回调同样记错格（修缮 10 候选池 5，同根同文件趁热根治）+ 执行侧「算不出路」补一行限频日志（候选池 3）**，顺带只取证 `CHEST_PATH_UNREACHABLE`「站在建筑上下不来」（判「不同源」采纳）；`EVENT_LOOP_STALL` 顺延 | 本台账（三线交齐后，开发提问） | 执行中 |
| 57 | 2026-09-04 | 修缮 11 排除集回调修复**接受离线证据（离线改前 noPath/改后 success + 反证 + 「回调根本没在保护」对照 + 整栋小屋零垫脚石进图纸格），不补真机靶子**（需 ≥11 格悬挑建筑，占半轮） | 修缮 11 反馈提问 1（选项 A） | ✅ 生效 |
| 58 | 2026-09-04 | **后勤 12 自己补那 6 行**：`placeBlockAgainstReference()` 通用尾巴临时划给后勤一轮，照床那处 `_genericPlace(..., {forceLook:'ignore'})` 写法，让 32 处「转了白转」生效；配单测 | 后勤 11 反馈提问 1（选项 A） | 执行中 |
| 59 | 2026-09-04 | **修缮 12 = 灯笼「预演全部截断」根治**（`604,77,-2 lantern{hanging}` 19 候选全 `preview_partial`，图纸同类 17 盏）**+ 顺带箱子自救链带下行落脚点**（修缮 11 候选修法 3），单独一笔主活做完才做 | 建造 18 反馈剩余风险 1 + 修缮 11 候选池 2（开发提问） | 执行中 |
| 60 | 2026-09-04 | **建造 19 = 小合并轮**：现在合后勤 11 `feat/ops@79d8155e` 三笔 + 修缮 11 `feat/fix@a123b121` 两笔，不进游戏；**建造 20** 再合后勤 12 + 修缮 12 并续建门楼。理由：下次大合并小一半；两线新产出各自地盘不重叠 | 本台账（三线交齐后，开发提问） | 执行中 |
| 61 | 2026-09-04 | 修缮 12 的「箱子在楼下、没台阶时按血量算可承受落差（留 6 HP、上限 8 格）直接跳」**保留**；血不够拒绝并写明原因 | 修缮 12 反馈提问 1（选项 A） | ✅ 生效（`cc6b16ee`） |
| 62 | 2026-09-04 | **修缮 13 = 候选站位不查脚下**（`controlledVerticalAccessStandCandidates` 生成候选全是悬空格、每条路以脚手架柱收尾）：先量化再改排序/分区，悬空候选不删只排后。理由：门楼还有两盏高 5 格的吊灯笼，补算 4 s 可能不够，让路便宜是第二层保险 | 本台账（建造 20 并行期间，开发提问） | ✅ 交卷（`edfbb642`；量化推翻「有地板优先」，改成补算名额「净空柱优先」，两盏 y82 灯笼离线由未命中变命中） |
| 63 | 2026-09-04 | **后勤 13 = 小屋轮前置**：本地十份图纸靠 `ai/intent-parser.js` 写死关键词表才点得到 → 名单从 `blueprints/` 派生、中文名随图纸走、歧义返回候选；顺带普查十份图纸各过哪一档复杂度预算；预算表不动 | 本台账（#35 小屋轮前提，开发提问） | ✅ 交卷（`85b08696`/`53d577d8`，8→11 份可点；四份卡档 → #66） |
| 64 | 2026-09-05 | 带红石电路的图纸，**红石通电源（火把/红石线/拉杆/按钮等）排到整栋最后放**（或放完立刻断电）。理由：建造 20 她按图纸顺序放火把，电路一通活塞顶出、活塞头占图纸格，同砖三败停手；门楼剩 20 多个红石元件，不改每轮都撞 | 建造 20 反馈提问 1（选项 A） | ✅ 后勤 14 执行 `b8828606`（供电源最末 phase；红石线留原位；在建 run 自动采用新顺序）；真机由建造 22 验 |
| 65 | 2026-09-05 | **允许建造线用服务器命令清脚手架残留土**（31 格 + 3 根逃生柱，都不在图纸格上；只删土不加砖，逐块记账）。理由：残留已干扰她规划路线（两次把旧残留柱当新脚手架去放）；「隔空拆」修好前残留会继续累积 | 建造 20 反馈提问 2（选项 A） | ✅ 建造 21 执行（50/50/0，图纸格零触碰）；此后各轮收工同法清本轮残留 |
| 66 | 2026-09-05 | 四份卡在档位之间的图纸（`small_house` 26 块 2 层 / `two_story_wood_house` 97 / `simple_farmhouse` 89 / `garden_manor` 89——层数超 L1、块数不够 L2）**加进「按图纸原样盖、不按档位裁」白名单**（别墅/城堡花园已在里面）；预算表数字不动 | 后勤 13 反馈提问 1（选项 A） | ✅ 后勤 14 执行 `21693396`（14/14 份可点） |
| 67 | 2026-09-05 | 分工：**建造线等修缮 13 交卷再合**（建造 21 = 合后勤 14 + 修缮 13 + 清残留 + 续建）；**后勤 14 先开**（活塞视线服务器没收到 + 红石排序 + 四份放行）；**修缮 14** = 隔空拆脚手架 + 失败即跑家 + 柱子搭一半超时，修缮 13 交卷后写 | 本台账（建造 20 / 后勤 13 审毕，开发提问） | ✅ 修缮 14 执行 `9d871333`/`621b2d91`（隔空拆根治；跑家取证到锚点过期停手） |
| 68 | 2026-09-05 | **建造 21 = 现在开小轮不盖砖**：合修缮 13 `edfbb642` + #65 清残留 + #69 屋顶处置 + 文档（Sonnet high）；**建造 22** 再合后勤 14 + 续建（Fable）。理由：清残留和合并不依赖后勤 14，建造线别空等；把大轮拆小每轮便宜 | 本台账（修缮 13 审毕，开发提问） | ✅ 建造 21 执行 `af3b5b98`（合并 + 清残留 + 拔火把；拆活塞按停手条件未做） |
| 69 | 2026-09-05 | 门楼屋顶红石现场：**拔火把 `603,80,-5` 断电 → 活塞缩回 → 拆两个错朝向活塞 `604,79,-10` / `-7` 与残留活塞头**（`setblock air`，只删不加，逐块记账）；`604,79,-9`（朝向正确）留着。续建对账从世界重推，这几格变回待放，她用后勤 14 修好的视线重放。**不算代放砖** | 建造 20 反馈剩余风险 2（开发提问） | 部分执行：火把拔了；现场核实 `-10` 已是 down、`-7` 已是 air，无需再拆 → #71 |
| 70 | 2026-09-05 | **建造 22 = 现在开**：合后勤 14 `feat/ops@7c3af0a2` 五笔 + 续建门楼真机，验「视线同发」「供电源最后放」；修缮 14 不等，本轮脚手架残留照旧、收工按 #65 同法清；建造 23 再收修缮 14。理由：两个修复早点上真机 | 本台账（建造 21 / 后勤 14 审毕，开发提问） | ✅ 建造 22 执行 `02394b1e`（视线同发 2/2、供电源 0/28 成立；+13 步；三败停于 `604,81,-10`） |
| 71 | 2026-09-05 | 屋顶三个断电仍伸出的活塞（`604,79,-9` / `-10` / `-6`，方向都对）**留着不动**，建造 22 开场翻第 20 轮日志顺带取证「-10 是重放放对的、-7/-8 是被她拆掉的」，不占真机 | 建造 21 反馈提问 1（选项 A） | ✅ 谜底：装反的 `-7` 通电伸出把整排往北推一格（`-10` 朝北那只现在 `-11`）；三只伸出活塞在邻格更新时自动缩回 |
| 72 | 2026-09-05 | **后勤线歇着**，等建造 22 真机对视线同发 / 供电源顺序的定论再定后勤 15（候选：漏斗互指死结、小屋轮验收工具） | 本台账（开发提问） | ✅ 真机定论后 → #76 后勤 15 |
| 73 | 2026-09-05 | **拆掉 `604,79,-11` 那只朝北活塞**（图纸范围外、被推出去的事故残留、紧邻红石线）：建造 23 开场 RCON `setblock air`，只此一格，记账 | 建造 22 反馈提问 1（选项 A） | ✅ 建造 23 执行（只动一格，快照前后核对） |
| 74 | 2026-09-05 | **允许保姆脚本把她传送回屋顶**：目标比她高 ≥10 格且同一目标连续两次站位超时 → tp 到该目标的 `PLAN stand` 格（或屋顶已 verified 实心格上方）；只传人、不放砖不拆砖；每次记账，反馈里「传送后盖的砖」单列。理由：屋顶还有一百多块高处砖，按每轮十几块要十轮以上；「爬回去」本身归修缮另修 | 建造 22 反馈提问 2（选项 A） | ✅ 建造 23 执行：2/2 传送后砖各 4 s 放上；继续沿用 |
| 75 | 2026-09-05 | **修缮 15 = 锁占死主攻**（被 `player_build_command` 打断的回家任务，脱困链继续跑、重新抓移动锁不放；之后每次开工 `lock_already_held`，只能重启 bot）**+ 顺带给锚点不刷新加限频日志取证**；「柱子搭一半超时」顺延修缮 16。修好前建造线保姆脚本以「连续 2 次 `lock_already_held` → 重启 bot」兜底 | 本台账（建造 22 / 修缮 14 审毕，开发提问） | 执行中 |
| 76 | 2026-09-05 | **后勤 15 = 漏斗互指死结**（门楼 `603,74,-2` / `604,74,-2` 互为参照谁也放不了，第 11 轮记账至今，终验已知 2 处）：序列层给出可放顺序（一只借临时参照），不硬编码门楼 | 本台账（视线/顺序真机定论后，开发提问） | ✅ 后勤 15 交卷 `1a889af1`（编排层 2→0；执行层三口子 → #78） |
| 77 | 2026-09-05 | **分档表加两样**：①「极难+」档 = Fable 5.1 `max`（证据齐仍想不通的根因才用；缺日志的 UNKNOWN 先加日志）；②**ultracode 作为第三个旋钮**（CC 会话里多子代理并行）——普查/清账、全量日志复盘、多根因分头取证、大合并撞车开；真机轮、最小补丁、归档不开。任务书头部改为「建议模型 / 思考强度 / 是否 ultracode」，CC 反馈头部照报。理由：老板问「为什么 max 和 ultracode 一次没用过」——开发定档保守、且漏了 CC 侧 ultracode 这个旋钮 | 老板要求（chat） | ✅ 生效（`说明.md` 分档表；修缮 15 / 后勤 15 任务书补标） |
| 78 | 2026-09-05 | **漏斗执行层三个口子由后勤 16 全包，临时划地盘一轮**（照 #58 先例）：①`building-system` `placeOptions` 只加透传；②`findReferenceBlocksForPlacement`（#53 冻结）**解冻仅限加一个可选入参**、默认路径 diff 零并单测钉住；③临时参照**放置侧**加「步骤自带声明」第三种启动条件 + 指定位置；清除侧（修缮 14）不碰。理由：后勤最懂声明格式，一轮接通；拆两线要拖两轮以上 | 后勤 15 交接（开发提问，三选一） | 执行中 |
| 79 | 2026-09-05 | 任务书 ultracode 标法分三档：**可开 = CC 自判，不开须在反馈头部写一句理由；必开 = 必须开；不开**。后勤 15 用探针替代子代理普查、理由成立，接受 | 后勤 15 反馈「与任务书不同」1（开发提问） | ✅ 生效（`说明.md`） |
| 80 | 2026-09-05 | **建造 24 = 现在开**：合后勤 15 两笔 + 保姆脚本补三处（钉在原地救援、不重发旧失败、屋顶不去箱子）+ 续建屋顶；修缮 15 交卷后建造 25 再收。建造 23 提问 1 开发定 A：三败照算、#48 不改、只补脚本 | 本台账（建造 23 / 后勤 15 审毕，开发提问） | 执行中 |
| 81 | 2026-09-05 | **修缮 16 = 城垛顶零候选主攻**（她站一格宽墙顶时寻路对 41 个站位全 noPath，但够得着就能原地放；屋顶每轮必撞）+ 顺带取证「从屋顶算箱子路不带脚手架方案」；「柱子搭一半超时」留修缮 17。修缮 15 交卷后下发 | 本台账（开发提问） | 待下发 |

## 代码地盘（开发划定，避免三线撞车，2026-09-02）

- 修缮线：连接层、逃生梯、寻路、箱子交互——一切要跟世界打交道才能验的
- 后勤线（离线修缮）：序列规划、放置状态映射、任务队列——离线回放+单测能钉死的
- 建造线：施工链路与验收；续建/终验时顺手取证，不修根因

## 开发直接确认、未惊动老板的（2026-09-02，三线第 13/7/8 轮审毕）

- 修缮 8 反馈提问 2：「一直盖到完工」**不追**，`no_reachable_build_stance_after_vertical_access_candidates` 入修缮候选池
- 后勤 7 第四节第 2 条（计划参照修复同步排除不可点方块）**暂缓**：等修缮 9 把漏斗等变成可点后再看这条补救链是否已通
- 看板入库后主仓 `dashboard\` 改为只由合并带过去，后勤不再手改主仓那份（后勤 8 落实）

## 开发直接确认（2026-09-03，建造 14 / 后勤 8 审毕）

- 后勤 8 加的 `.gitignore` 两行（归档目录 + 迁移备份）接受；迁移备份 `construction-runs.pre-archive.json` 续建成功前不删
- 工地残留脚手架/脱困土块 5 处：图纸该处有方块→不动，为空→RCON 清掉，逐块记账（建造 15 第 4 步）
- 后勤 9 = 带朝向方块的状态错放（按钮/转发器/比较器）诊断 + 规划层可修部分；从候选池「转发器 delay/facing 与天花按钮朝向」提上来，理由：建造 14 唯一一次非站位类任务失败就是它，存档 38 步 state_repair 大半同族
- 修缮 8 第 8 轮反馈的归档件由修缮 9 开场放进主仓历史目录（建造 14 未见到属正常）

## 开发直接确认（2026-09-03 晚，建造 15 / 后勤 9 审毕）

- 建造 15 / 后勤 9 审毕通过；长活起法「活过会话退出」一项由建造 16 开场核傀儡 25740 + 心跳定论，核完收掉
- 后勤 9 故意留 5 处转发器 `delay=2` 继续比：接受（两把尺子都留）
- 后勤 10 = 小轮（#51 尺子对齐 + 看板补录 + 归档），Sonnet 5 / medium
- **后勤 11 = 后勤 9 4.3 的 (b) 类「朝向失控」40 处**（`placementLookDirection` 名单 17 处水平 + `lookTowardDirection` 加俯仰 23 处上下 + `expectedPlacementStateKeys` 名单 + 转发器 `delay` 放完右键调）：归后勤线离线做 + 单测；「按钮 facing 背向还是面向玩家」由建造线续建时真机核。**等修缮 9 交卷后下发**（同文件撞车）
- 建造 15 冒烟 8 分钟「未被拉回老基地」原因不追，记观察项（行为变了，续建时留意）
- 作业外进程收尾责任：每轮真机后 CC 自己 `Stop-Process` bot + RCON 优雅停服，写进建造线常驻要点
- 下次合并（修缮 9+10）后离线复核续建匹配继续作为固定动作：**每次改序后都核一次**

## 开发直接确认（2026-09-03 夜，建造 16 / 后勤 10 审毕）

- 建造 16 / 后勤 10 审毕通过
- 长活起法跨会话存活**仍 UNKNOWN**：第 15→16 轮之间 Claude Code 宿主（`claude.exe`，06:41 本地起）从未退出，试验没发生。r16 傀儡留活：WMI 包装 34476 → node 32716（`mc-r16-puppet-wmi`，心跳 `.tmp/cc-r16-puppet-wmi.heartbeat`）+ 对照 `Start-Process` node 35248（`mc-r16-puppet-control`）。**判据**：下轮建造开场先看 `claude.exe` 创建时间是否晚于 09-03 07:38（本地）——晚于才算宿主退出过；然后 32716 活 + 心跳新鲜 且 35248 死 → 起法已证实；32716 也死 → 证伪，改「老板独立终端起」。老板需在下轮建造前把 Claude Code 全部退出一次
- 续建匹配靶字段读码钉死：`constructionRunCompatibility()` 只比 `blueprintId` / `blueprintHash` / `placementContext` / `world`，**planId 不参与**；步骤按 `createStableStepId()` 对账、哈希不含序号。后勤 6/7/9 三次改序 planId 三变、命中不变。以后改序仍复核，但可降为「合并轮里一行核对」
- 后勤 10 对 `systems/building-system.js` 的改动超出任务书「只改注释」字面（删本地定义改 `require` 共享 util、`_test` 多导出一行）：**接受**——逻辑未变，35 组一致性单测 + 探针 + 金值 + 全绿四重验证；任务书红线是开发写窄了（没料到循环依赖逼出第三种写法）
- `waterlogged` 两把尺子不一致（对账排除、忠实度比对，第 9 轮前旧账）：**暂不放宽、维持现状**——含水与否是可见的实打实差异，不属「放置动作产生不了」；对账排除它是防施工中水流动引起反复砸块；门楼工地为干地，当前不扣分。后勤 10 已钉测试防回退。老板若要一并放宽再改
- 建造 16 尽力项：存档步骤不含观察到的实际状态，「38 步 state_repair 新口径下几步免比」离线算不了；按方块种类估约 26 步转 verified、12 步（转发器 delay / 比较器 mode / 漏斗 facing / 按钮）看真机——以真机 reconcile 日志为准
- MC-ops 工作区 `data/community-builds/cache/` 下 5 个未跟踪文件（`mcbuild-a-small-modern-house-16786` / `mcbuild-wood-modern-house-15774` 两份缓存 + `index.json`）：后勤 1 素材抓取遗留，**原样留着、不提交不删**，小屋轮再定
- **两线歇，等修缮 9 交卷**（老板拍板 2026-09-03 夜）：不下发建造 17 / 后勤 11；修缮 9 交卷后一次写修缮 10（站位）+ 后勤 11（(b) 类 40 处）任务书

## 开发直接确认（2026-09-03 深夜，修缮 9 审毕，三线交齐）

- 修缮 9 审毕通过：悬臂靶子 17 种改前/改后逐一对照，11 项进名单、6 项故意不进；`redstone_wire` 潜行救得回（B 类 UNKNOWN 结案）；补救链 12→0；夜里回家纳入工地锚点（#44 落实）；后勤四.3 稳定分区顺手做了
- **修缮 10 = #49 原题**（找不到落脚点 + 顺带脱困链打断即停），建议 Opus 5 xhigh（Fable 亦可）；**顺手加第 6 步**：真机核一张朝向语义表（面向/背向玩家 + up/down）给后勤 11，≤ 30 分钟
- **建造 17 = 核 r16 傀儡（老板先把 Claude Code 全退一次）+ 合修缮 9 `feat/fix@95898337` + 合后勤 10 `feat/ops@30cc2e64`**，不进游戏不盖砖；先于后勤 11 开，Sonnet 5 medium。另复核修缮 9 提醒：`findReferenceBlocksForPlacement` 返回顺序变了，主干有无依赖首候选方向的调用点
- **后勤 11 = (b) 类 40 处**：水平 17（`placementLookDirection` 名单）+ 上下 23（`lookTowardDirection` 加俯仰）+ 放置当场闸门名单 + 有余力 delay/mode 放完右键调；语义表做成数据、每项标来源/UNKNOWN，修缮 10 真机表回来后合并轮只改表；开场 main 不含 `95898337` + `30cc2e64` 则停手等建造 17
- 修缮 9 报的「主机整卡死两次」：老板说明**是笔记本没电了**，与代码、内存无关，结案；开发先前「三会话同开挤爆内存」的推测**作废**，会话并开不受此限
- 候选池新增：本地 `blueprints/*.json` 靠 `intent-parser.js` 写死关键词表才点得到（小屋轮前要解）；`npm test` 偶发退出码 1（未复现）；`bell` / `cake` / `respawn_anchor` / `chiseled_bookshelf` 右键有交互却没被吃掉的原因 UNKNOWN；夜里回家真机未单独复现
- 修缮 9 反馈归档件由修缮 10 开场放主仓历史；建造 17 见到就一并提交

## 开发直接确认（2026-09-03 夜，建造 17 / 修缮 10 / 后勤 11 首发审毕）

- 建造 17 审毕通过：两笔合并零冲突、60 组全绿、main 快进 `af8393b6`；第三条预核字面不过的 git 拓扑解释（`b08066fb` 是 `dff6c4ad` 的合并父提交，`A..B` 按可达性算进区间）成立，接受
- 修缮 10 审毕通过（Fable 5.1 跑）：根因 = `actions/move.js` 白名单 `exclusionAreasPlace` 代价回调记错格（上游 `getMoveForward` 把搭桥步的 exclusionPlace 记在要走进的格），执行侧 A* 对所有搭桥步 `noPath`；离线逐字复现（`nodes=7 cost=13.2` 对上第 14 轮日志）+ 坑中孤柱靶子改前/改后两次 + 183 块木屋零站位失败；**项目首次真机走通搭桥**。脱困链打断即停 `ee30d137` 单测 + 反证。任务书两条 UNKNOWN 结案：y71 头顶被封不是原因；预演/执行同一套 A* 只是配置不同
- 后勤 11 首发停手**正确**（起得比建造 17 早，主干未含修缮 9），零改动；重发任务书塞进修缮 10 真机朝向表；单测改放 `tests/oriented-placement-state.test.js` 避开修缮 10 改过的 `tests/actions.test.js`
- 修缮 10 朝向表纠正了后勤 9 两条合理推断：**侦测器 `facing` 背向玩家、漏斗 `facing` 只看所点面**（转身无用，可能要参照面）——后勤 11 表以真机为准
- 起法证伪的因果是合理推断（无 Windows 审计记录），接受，不再追
- 修缮世界残留报备：坑 `589..599/93..99/296..310` + 孤柱 `594,93..98,303`、朝向探针台 `554..568,99,294..306`、小屋平台 `640..670,99,290..320` + 木屋——留着复用
- 候选池新增：`controlledVerticalAccessStandCandidates` 不查脚下（44 候选大半悬空）；执行侧无路无日志（修缮 11 收）；排除集回调同款错（修缮 11 收）；第 8 轮小屋那次 `no_reachable…` 是否即排除集回调 UNKNOWN（修缮 11 顺手判）

## 开发直接确认（2026-09-04，建造 18 / 修缮 11 / 后勤 11 审毕，三线交齐）

- 建造 18 审毕通过（Fable）：`607,73,-16` 18 s 盖上、事后方块查询 passed → **修缮 10 在门楼真身成立**；+60 块；`package.json` `test` 行手工并集（`door-traversal → scaffold-allowlist-path`）过目接受；存档 verified 1107 / state_repair 27 / pending 728 / terminal_failed 1（`604,77,-2`）；后勤 10 尺子在 resume 对账时把 14 步朝向类 state_repair 认成 verified（没放砖）——属 #51 口径，接受
- 停工 3 分钟锚点宽限（`worksite-anchor.js graceMs=180000`）到期跑家 → **设计内，不改宽限**；建造线常驻要点加一条：保姆脚本停手后立刻收 bot（或先 tp 回工地），不给她跑家机会
- 修缮 11 审毕通过（Opus）：排除集回调删除 `0c9682c2` + 无路日志 `a123b121`；`CHEST_PATH_UNREACHABLE` 机制钉死（箱子路 `canDig:false` 无脚手架、`maxDropDown=4`、自救落脚点同高度）；「夹具改施工档已失效、只能改世界」记入主池；第 8 轮小屋那次是否同病 UNKNOWN 留池
- 后勤 11 审毕通过（Opus）：语义表落数据、俯仰、闸门、delay/mode（(c) 5→0）；**32/40 「转了白转」如实报、看板 f13 记 doing 不记 done——正确**；`confirmPlacedBlock` 加 2 行越界报备接受；导出 7 个单测符号接受；漏斗互指死结（`603,74,-2`/`604,74,-2`）**先记账不修**（老板问题 2 选 B，开发定）；`white_bed part=head` 半张床旧账记池（后勤 12 有余力看）
- 建造 18 报的他线未跟踪归档件 20 余个 + `docs/协作/修缮/` + `docs/协作/后勤/说明.md`：**建造 19 一并显式入库**（文档、只增不删）
- 建造 18 观察到的非本线进程（老板客户端 java、Codex node、修缮 bot 连 25566）：正常，不追
- 候选池新增：`BUILD_STAND_PATH_PRESCREEN` 对搭桥目标全 timeout（25 ms 预算）；跑家半路两连败脱困 `ESCAPE_RUNG_SUCCESS yGain=-5.24` 数值可疑；`build_origin_too_far` 提示语；`STORAGE destination full`；`chest_item_not_found` 材料箱缺 button/slab 但施工照常

## 开发直接确认（2026-09-04，建造 19 / 修缮 12 / 后勤 12 审毕，三线交齐）

- 建造 19 审毕通过（Sonnet）：`fecef100` + `464f26b4`，`package.json` 并集顺序正确，62 组，main `464f26b4`；21 份归档件 + `docs/协作/修缮/` + `后勤/说明.md` 入库；「未逐条清点后勤 22 组断言」不追（后勤 12 反证已覆盖）
- 修缮 12 审毕通过（Fable）：根因 = 我们自己的预演片数预算（`min(16, max(6, 2+ceil(距离/3)))` × 40 ms）按平地估，真机吞吐 1/3–1/4；改法只在第一遍全失败且有截断候选时补算 ≤4 个 × 4 s + 片间让出——**取舍对，矮目标零回归**；`terminal_failed` 续建不用复位（`reconciledStepStatus` 从世界重推）；第 8 轮那条 UNKNOWN 结案为零候选分支；摔落伤害账未验（peaceful）留修缮 13 顺手；「同一 origin 二次施工被保护边距拦脚手架清理」入池
- 后勤 12 审毕通过（Opus）：六行 `f3ad00d9`（七族走 `_genericPlace(forceLook:'ignore')`，漏斗/按钮不走，潜行照包，失败串不新增，反证红）；离线四类朝向计数全零（剩漏斗互指 2）；半张床查实为探针误报、生产码不改——**正确**；未接主干属 #60 预期，建造 20 收
- 建造 20 = 合 `feat/fix@cc6b16ee` + `feat/ops@35fa4650`（导出块预期撞车、并集）+ 续建门楼首撞灯笼（不复位）；Fable 5.1 high；停工即收；三线记数表写进任务书
- 门楼图纸 `hanging=true` 灯笼共 3 盏（`604,77,-2` / `604,82,-1` / `604,82,-15`），其余 14 盏站地上
- 候选池新增：保护边距 ±1 内脚手架拆不掉；摔落伤害账；`loopMaxMs=1518` 爬柱来源；RCON `fill` 单次 32768 上限（夹具经验）；预建结构再下令必被拒 `EXISTING_STRUCTURE_DETECTED_NEEDS_RECONCILIATION`（夹具经验）；床 `part=head` 先执行时随机朝向（理论风险，未开卡）

## 开发直接确认（2026-09-05，建造 20 / 后勤 13 审毕；修缮 13 未交）

- 建造 20 审毕通过（Fable）：合修缮 12 `7839ad83` + 合后勤 12 `ddae9294` 零冲突、62 组、main 快进；灯笼 `604,77,-2` 第一遍预演 legal=12 一次挂上（未用补算）；补算真机 4/4 首候选 success（`608,78,1` / `607,78,2` / `603,79,-10` +1）；跳下楼开箱 1/1 → **修缮 12 在门楼真身成立**；+40 步（verified 1147 / state_repair 27 / pending 690 / terminal_failed 0）；run 状态 `BLOCKED_MATERIAL_SHORTAGE`（缺的是箱子里也没有的族）
- 建造 20 **两处违规如实报、接受但记账**：①停工即收没做到（等待循环把 SHUTDOWN 计进基线，闲置 13 分钟，收工后她跑到 `460.5,72,0.5`）；②收工 `Stop-Process` 按「命令行含 bot.js」过滤，**误杀修缮线连 25566 的 bot（pid 18632）**——修缮服务器未动。协议加硬规矩：关进程前核端口/工作目录，只关本线（说明.md 已加）。修缮 13 交卷时核它是否被打断
- 建造 20 新病因归属：活塞视线服务器没收到（原地放错/走完位放对，RCON Rotation 仍水平；mineflayer `look(force)` 不发包）→ **后勤 14 第 1 步**；红石通电顶活塞 → #64 后勤 14 第 2 步；隔空拆脚手架（`clearTemporaryReferenceBlock` 直接 dig、`confirmTemporaryReferenceClear` 只看客户端世界，31/87 残留）→ **修缮 14**；失败 0.4 s 即 `return_to_base TOO_FAR_FROM_BASE`（`worksite.atWorksite` 为假，原因未查）→ 修缮 14；y=79 目标从地面起柱子搭到 y≈74 停、`PROGRESS_RETRY 12 s` 超时（`stand_move_timeout_blacklisted` 33 次）→ 修缮 14 / 与修缮 13 排序相关
- `604,79,-10` 错朝向活塞被对账记 verified：怀疑尺子共享名单把 piston `facing` 放宽了（#51 放宽的是通电/伸缩/连接/拐角，**朝向不在内**）→ 后勤 14 第 1 步顺带核
- 后勤 13 审毕通过（Opus）：`85b08696` 名字从图纸派生 + 自然档 + `53d577d8` 看板；**纠正任务书前提**（解析器三条路，路 A 已覆盖十来个名字）；真病灶 = 不认识的名字默默盖成 `small_house` + 默认 L2 下限 120 块挡掉小图纸；8→11 份可点、66 句只变 5 句、38 条老说法钉住、金值三份不变、61 组；一档不过的图纸故意不认领（避免「盖错」变「盖不了」）——正确
- 小屋轮图纸定案（后勤 13 交接）：**用 `simple_wood_cabin`（183 块、L2）**，说法「建个小木屋 / 盖个木屋 / build a cabin / build a wood cabin」；**不用 `small_house`**（解析得到但一档不过，实际盖的是 183 块木屋，名字实物不一致）；更大靶子 `simple_two_story_cabin`（465 块、L3）；本地十四份无 lantern/cake/花盆，「被禁方块低档点不到」只对社区图纸成立；「建个城墙/瞭望塔」解析对但选择器无候选（社区缓存不在候选池）——另一单，门楼续建走 resume 不受影响
- 建造 20 报的 `CHEST_PATH_UNREACHABLE` 10 次未进下行分支（屋顶 y=78 到箱子 y=67 落差 11 > 上限 8，且没打 `_REFUSED`）→ 修缮候选池；`EVENT_LOOP_STALL` 11.4 s 在缺料补料的合成台搜索链 → 候选池；`604,82,-1` / `604,82,-15` 两盏高灯笼未轮到
- 候选池新增：`chest_item_not_found:dark_oak_button` 身上有却报缺（施工照常）；`build a small house` 解析名从 `simple_wood_cabin` 变 `small_house`（盖的东西一样，对账按名字的要留意）；后勤第 10 轮 `waterlogged` 两把尺子不一致仍未处理

## 开发直接确认（2026-09-05，修缮 13 审毕，三线交齐）

- 修缮 13 审毕通过（Opus）：`edfbb642` 只动 `actions/build.js` + `tests/actions.test.js`；量化：门楼三目标生成候选脚下实心 0/20、0/32、0/33，第一遍逐个预演与顺序无关 → **#62 预设的「有地板优先」收益为 0，任务书降级方案「脚下落差升序」方向相反**（R2 全未命中）；真正承重的是补算四个名额的挑法——现状「截断终点最近」在两盏 y82 灯笼上扎堆漏掉唯一算得完的候选；改成「脚下净空柱优先、组内仍按终点最近」后 gains=2 / regressions=0；真机 `clearColumn=12/13/14`、4/4 `(clear)`；小屋零补算；H=7 逐字一致；**掉血 6 格 = 3 HP 已证实**（#61 代价有数）
- 生成候选那处分区**留着**（超过预演上限 48 那天承重 + 两处口径一致），不算越界
- 修缮 13 五.1「`Start-Process` bot 无预警消失」= 建造 20 误杀 pid 18632（时间 02:29Z、现象对得上）——**不是坑**，修缮 14 任务书划掉
- 修缮 13 W=28 改后一次 `move_timeout`（选了离墙两格的独立柱）归执行侧，与建造 20 「柱子搭到 y≈74 停 + `PROGRESS_RETRY 12 s`」合成**修缮 15 候选**（补算只有一个方案 → `verticalAccessTerminalPillarRisk` 失效）
- `minimumPlacementStandY = target−2`（`systems/building-system.js:2674`，把生成候选 y 窗口压成一层）→ **归修缮线**，另立一轮
- 修缮 14 = 隔空拆脚手架主攻（诚实层必做：假成功归零 + 新日志 + 残留账；走位层最小或先算账）+ 顺带取证失败即跑家（`atWorksite` 为假原因）；Opus xhigh
- 建造 21 收工照旧 RCON 停服、只动 25565；第 4 步他线归档件「若有」才提交
- 候选池新增：净空柱判据对无碰撞体方块偏保守（可按 `shapes` 空数组放宽）；`maxDepth=16` 常数；板砖遮篷送起跳平台（夹具经验）；一座夹具塔只够跑一趟；真机吞吐跑间方差大——「第一遍有没有方案」判据要多跑

## 开发直接确认（2026-09-05，建造 21 / 后勤 14 审毕）

- 后勤 14 审毕通过（Opus）：**开发假设「force look 不发包」被证伪**（`lastSentYaw/Pitch` 累加器与 `lastSent` 上一个包是两个东西；活塞那条路 `forceLook:false` 根本没走 force）；真根因 = `pathfinder.stop()` 只挂牌子、走位超时后旧路径每刻 `bot.look(yaw,0)` 掰回水平（`stopMovePathfinder` 非脚手架场景不调 `setGoal(null)`）——与三条真机现象逐条吻合；改法 = KEEP_YAW 段 `_genericPlace` 前同一同步块写 `look` 包（`b058f9bb`），离线反证红/绿；两把尺子都还比朝向 + 断言钉住；`604,79,-10` 记 verified 原因待现场对读
- 后勤 14 两处偏离任务书**都接受**：①`package.json` 测试清单纯追加冲突就地并集并报备——**以后同类机械冲突可就地解决、显著报备**；②红石线不挪（自身不供电；门楼 y=72 四根横梁靠它当同轴参照，挪走 `axis-reference-order` 4 处 violation）；别墅金值变（有拉杆/按钮/压力板 7 步）是任务书前提错，纠正接受
- 后勤 14「在建 run 无需迁移」三条读码证据（step id 哈希不含顺序 / `mergeRuntimeAndStoredSteps` 按新顺序遍历 / run 匹配键不含 planId）接受；门楼 planId `62e98b560881`、别墅 `26d666778fb5` 为新金值
- #66 副作用记档：「建个小屋 / build a small house」从此真盖 26 块 `small_house`（以前实物是 183 块木屋）；`build two story wood house` 不带大小词盖 97 块原样、不再套 L3
- 建造 21 审毕通过（Sonnet）：`af3b5b98` 零冲突、62 组、主仓快进；50/50/0 清残留、图纸格零触碰；拔火把后电路零电；按停手条件停在拆活塞前——**先拍快照、对不上就停，正确**
- 建造 21「原因不明」两条开发解释（合理推断，建造 22 核）：`604,79,-10` 是 02:06 之后某次重放放对的（对账 verified 没记错）；`604,79,-7` + `-8` 一起变空 = 她 clear `-8` piston_head 时真拆到一次（拆头 = 整只活塞没）。断电不缩回 UNKNOWN，不专门查
- 建造 21 世界现状恰好是续建想要的：错的没了、对的留着、火把待放且排最末 → 建造 22 直接续建；三个伸出活塞按 #51 对账不碰
- 建造 22 保姆脚本三处改：回声正则 + 缺料不计三败 + 停手立刻 SHUTDOWN；`give` 名单补齐 8 项
- 候选池新增：后勤 14 顺序卡点保守估计门楼 1→2 处（`603,69,-11`，不算世界方块、可能假警报）；`look` 包协议版本写不出去的退路（`sent=false`）；寻路走位超时后继续掰视线本身未修（`actions/move.js`，修缮地盘，影响其它需视角的动作）；断电活塞不缩回（UNKNOWN）；红石线先铺若连到已有电源会意外通电（理论）

## 开发直接确认（2026-09-05，建造 22 / 修缮 14 审毕，三线交齐）

- 建造 22 审毕通过（Fable）：`02394b1e` 合后勤 14 七笔（含后勤 13 两笔，按可达性属正常）零冲突、63 组；离线门楼 planId `62e98b560881`、末 24 步供电源；开场三个数与建造 21 预估零偏差；**视线同发 2/2 成立**（原地站着放也对，`state_mismatch:facing` 13→0）；**#64 成立**（28 步供电源 0，火把仍 air）；`604,79,-10` 对读一致；三只伸出活塞邻格更新即缩回（#71 「断电不缩回」= 没收到过邻格更新）；停工 6 s 内收、只杀本线、残留 36/24/0——两条协议都做到
- 建造 22 **证伪开发两条猜测**（`-10` 重放放对 / `-7` 被拆到），真因 = 通电推一排；`-7` 本体与 `-8` 头消失仍 UNKNOWN，不追
- 建造 22 新病因归属：锁占死 → 修缮 15 主攻；掉屋顶回不去（37 分钟 13 块；临时参照站位不带脚手架方案；`604,81,-10` 补算四行 partial ×100 片）→ #74 传送兜底 + 修缮 16 候选；掉落原因 UNKNOWN 记池；跑家 0.3 s ×2 → 修缮 15 顺带（锚点日志）
- 修缮 14 审毕通过（Opus）：根因用真机探针双向对照钉死（近 3.2 格：ack + block_change + 掉落；远 14 格：只 ack，客户端到区块重载前永远假空气）+ 184 条 CLEAR 按掉落物判据画出「近真远假」线；`9d871333` 诚实层（`temporaryReferenceClearReach` 5.1 + `canDigBlock` 两者都要；`CLEAR_UNREACHABLE` / `RESIDUE` 日志；内存残留账；被骗过的格子不认空气；留残留不判失败——取舍对）+ `621b2d91` 走位层（`moveTo` range 3 / 8 s / 不挖不垫）+ `SCAFFOLD_PLACED` 只记 ≤6 格；真机 6/6 假成功 → 0/6；小屋 184 步、靶塔 21 格 0 残留；三次反证红
- 修缮 14 纠正建造 20 残留名单：`600,67..74,-8` 是脱困柱重新垫的；它报的矛盾（`601,69..73,-10` / `608,71,-8` 有掉落物却扫成 dirt）**建造 21 已答**：改前逐格 `execute if block dirt` 全通过后才删，扫描没错，掉落物归因有误——建造 23 正式写一段转回
- 修缮 14 跑家取证接受：锚点 15 分 24 秒没刷新（`graceMs` 3 分钟）、失败瞬间离锚点 2.22 格 / 半径 64；为何不刷新 UNKNOWN 停手正确 → 修缮 15 加限频日志
- 修缮 14 剩余风险记档：走位层让她清理时主动走动（最坏每格多 8 s，可 `walkIntoReachBeforeClear:false`）；残留账只在内存（跨会话补拆要动 schema → 拍板）；`5.1` 抄 mineflayer 偏宽松（1.21 交互距离 ≈4.5）；`SCAFFOLD_PLACED` 6 格余量；350 ms 判据来路 UNKNOWN
- 建造 23 = 合修缮 14 两笔 + #73 + 续建（#74 传送 + #75 重启兜底）+ 验修缮 14「dirt 只许在 RESIDUE 名单内」；Fable high。修缮 15 Opus xhigh；后勤 15 Opus high
- 候选池新增：掉屋顶原因；`EVENT_LOOP_STALL` 在图纸加载/编译处（建造 22 12 次 2–5 s）；`CHEST_PATH_UNREACHABLE` 她在地面时箱子路 noPath（非落差）；隔空拆 4.6–5.5 格三条例外清掉了（边界值）；漏斗 state_repair 那 2 只修好序列后怎么重放（后勤 15 第 1 步 4.）

## 开发直接确认（2026-09-05，建造 23 / 后勤 15 审毕）

- 建造 23 审毕通过（Fable）：`d64c7777` 合修缮 14 零冲突、63 组；#73 只动一格；**修缮 14 在门楼真身成立**（5 格 `out_of_reach` 老实认账 + RESIDUE 准确 + 名单外 0 残留 + `unverified_client_air` 0；样本 8 格）；#74 传送 2/2；首撞 `604,81,-10` 不传送 46 s 放上（起步点不同，legal 6，修缮 13 边界未触发）；活塞 6/6 `sent=true`、mismatch 0；三个数零偏差；停工 20 s 内收、只杀本线；+12 步 → verified 1171 / pending 666 / state_repair 27
- 建造 23 新现象归属：**城垛顶零候选**（`605,82,-18`，她站 `600,82,-15` 顶上，restricted 3 → 41 候选全 noPath，`MOVE_PATH_STATUS noPath allowList=0 excl=1233`，但同站点够得着就能放；三败 33 s 一次、原地未动）→ **修缮 16 主攻**；**每次开工先去箱子补料、从屋顶算不到箱子**（10 次 UNREACHABLE、每任务 ~60 s；脚本的箱子救援把她传下屋顶两次）→ 修缮 16 顺带；保姆脚本两处漏洞（`pendingTp` 分支漏掉钉在原地救援；`停止任务` 后把旧失败当新终态重发续建、三次之间无介入窗口）→ 建造 24 脚本修；锁占死本轮无样本（`return_to_base` 0，锚点 184 次 HOLD 都在刷新）
- 建造 23 答修缮 14 矛盾：那 6 格第 21 轮逐格 `execute if block dirt` 通过后才删，扫描没错；掉落物坐标只是物品实体位置，多半是「拆过又垫回」——合理推断，转修缮
- 后勤 15 审毕通过（Opus）：20 份图纸普查只有门楼一对互指；序列层第三趟（`clickedFaceReference` + 成环头一只 `temporaryReference`，一块砖破任意长度的环）；门楼 2→0、planId `d1919e8b1032`、step id 不变、别墅/小屋不变；9 组单测 + 反证；**执行层三个口子未接、声明无消费者——它把话说全了**；世界里两只漏斗推断 `facing=down`（建造 24 RCON 验）；state_repair 会自动返修但口子接上前重放照错；不实现 `unresolvable_reference_cycle`（死代码）接受；`site-planner.js:28-32` 过期注释记档（后勤 16 顺手）
- 后勤 15 未开 ultracode 用探针替代——理由成立（判据取生产代码），接受 → #79
- 顺序卡点探针门楼第 14 轮记 2、第 15 轮记 1，差异 UNKNOWN 记池
- 候选池新增：城垛顶 A* 起点判定（1 格宽 + 两侧落差）；补料只在真缺料时去 / 屋顶算箱子路带脚手架方案；`SCAFFOLD_PLACED` 6 格余量；修缮 14 小样本（8 格）待建造 24 加样本

## 老板决策（2026-09-06，方向大转向：转「基础可玩版」）

老板拍板把项目目标从「追求泛化建造能力」改为「实现 agent 大部分功能 + 优化功能之间的协调性，做出一个基础可玩版本」，泛化建造能力以后有机会再升级。理由：这几轮建造线卡的全是「在窄墙顶/悬挑结构上找落脚点」这个泛化难题，把力气转到能快速见成果的地方。三条线随之重新对准基础可玩版。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 82 | 2026-09-06 | **门楼（fort_wall_gate）就地封存**、不再续建——run `construction_run_d2f3cbb572ceb6b7` 与门楼世界原样留着、一块不拆、存档不改（符合 AGENTS 红线，就地封存 = 停止续建、不删不动）。城垛顶零候选/屋顶层/供电源等卡点全部不追；原修缮 16「城垛顶零候选」随之取消 | 老板拍板（chat，方向大转向）+ 开发规划 | 生效 |
| 83 | 2026-09-06 | **民居目标 = 一栋小木屋（现成 `simple_wood_cabin` ~183 块）+ 一栋 600-850 块的中等房屋**。老板看过小木屋 183 与双层小屋 465、觉得都太简单只算基础级，中等要落 600-850 块，需另找/导入图纸；同一张连盖三次干净才算稳（#35 口径）。小木屋由建造 25 先验、中等图纸由后勤 17 离线选/导入 | 老板拍板（chat，问答） | 执行中 |
| 84 | 2026-09-06 | **诡秘之主模组联动先不急**（模组本身也没完成）——只要求开发时在架构上留个口子，不单独排线、不深做。后勤/建造接名字解析这层时若见天然接缝只记一笔给开发，不实现 | 老板拍板（chat，问答） | 挂起（只留口子） |
| 85 | 2026-09-06 | **建造线转「民居线」**：建造 25 = 门楼封存 + 合修缮 15（2 笔）+ 合后勤 16（4 笔）进主干 + 起真机把小木屋连盖三次验稳（#35）+ 顺带验修缮 15「`lock_already_held` 0 条」以定 #75 能否撤重启兜底 | 开发规划（三线重对准） | 已下发 |
| 86 | 2026-09-06 | **修缮线转「协调线」**：修缮 16 = 设计一套「连玩一局」验收流程（覆盖多能力域、刻意制造功能交叉）+ 真机首跑暴露「功能之间打架」的问题 + 按严重度排序，最多顺手修最狠一个。地盘 = 仲裁/锁/任务队列；不碰 `actions/build.js` 候选/预演/放置。目标是把协调性拦路虎摸清排好 | 开发规划 | 已下发 |
| 87 | 2026-09-06 | **后勤线**：后勤 17 = 离线选/导入一张 600-850 块的中等民居图纸（社区库 `--only` 增量优先，全量禁令仍有效；改造凑区间须先拍板）+ 验编译/住宅资格/planId + 挂名字解析能被聊天点到 + 单测；顺手记联动口子位置（#84），不实现 | 开发规划 | 已下发 |

**基础可玩版时间估算（2026-09-06，开发）**：约 10-14 轮、按现节奏约 2-3.5 周。分解：民居两栋盖稳约 4-6 轮（含 600-850 中等图纸新未知）、协调性连玩暴露并修配合 bug 约 5-8 轮（最大不确定项）、联动只留口子约 1 轮内顺带。区间偏大主因是协调性——连玩暴露的配合 bug 少则明显更快。

**三线新路线**：建造 25（封存门楼+合两线+连盖小木屋）∥ 后勤 17（选中等图纸）∥ 修缮 16（连玩验收首跑）→ 中等图纸到货后建造盖三次验稳 → 协调性逐条修（修缮线主导，建造/后勤配合）→ 各功能连玩顺畅 + 两栋民居盖稳 = 基础可玩版。联动口子随手留、不排线。

---

## 2026-09-06 晚：建造 25 / 后勤 17 交卷后（#88–#93）

建造 25（Opus 5）：合修缮 15 + 合后勤 16 零冲突全绿、主干快进到 `2033a38c`；小木屋在三块平地连盖三次全部 `complete reason=ok`，六类日志三次全 0（#35 小木屋档标「稳定」）；修缮 15 真机判据成立（失败令→建造令连发 `lock_already_held` 0 条）；门楼一块未动。新空档：背包被门楼旧料塞满 + 荒地 32 格内没箱子 → 建造被生存系统按停。后勤 17（Sonnet 5）：18 份候选全量过，无一落 600-850 民居档；新试接社区源 `hunterx-simple-house` 为假素材被校验器拦下；停手等拍板。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 88 | 2026-09-06 | **中等民居图纸走「自家设计」路线**：不再去外面找社区素材，由开发（Cowork Claude）自己设计，「可以不大，但要看着比较别致有创意」；可上网借鉴博主设计（需登录的网站叫老板来登）。后勤 17 的 A（找新社区源）/ B（双层小屋加盖凑区间）两条都不选 | 老板拍板（chat，问答，自填） | 生效 |
| 89 | 2026-09-06 | **背包旧料处置 = A 过渡 + C 排给协调线**：眼下每轮开工前由建造线 CC 用传送把她送到最近箱子边、让她按自己流程存一次（只用传送，不加新指令）；同时把「开工前若空位不够、先就地放一只临时箱子中转、完工收走」排进协调线（修缮 16 交卷后写入修缮 17），因为荒地建房以后是常态、这是建造×生存×存储三个功能的配合空档 | 老板拍板（chat，问答） | 生效（C 待排） |
| 90 | 2026-09-06 | **造「注定失败的建造令」当靶子时**：先用编造的、不存在的图纸名；若连任务都不生成、退回「材料清零后下小木屋令」。红线新增一句：**造失败靶子时不得使用任何与在建/封存施工单同名或同族的图纸**（建造 25 用「建个城堡花园」擦到了封存门楼的续建查找，结果无害但口径要定） | 老板拍板（chat，问答） | 生效 |
| 91 | 2026-09-06 | **小木屋三连盖判「稳定」、不重盖**：第 1 栋是续上 8 分钟前同 origin 那 9 步清障（0 块放置）而非纯 `mode=new`，老板认可其实质等于从零；#35 小木屋档正式标「稳定」 | 老板拍板（chat，问答） | 生效 |
| 92 | 2026-09-06 | **中等民居图纸定为「塔楼小筑」`tower_cottage`**（开发设计稿 v1，716 块）：两层都铎风主屋（云杉木骨架 + 白陶瓦 + 石砖墙基、深色橡木楼梯瓦人字顶、山墙木柱）+ 东北角三层切角石砖塔楼带锥顶火把 + 正门门廊（石砖铺地、云杉栅栏、两根带火把门柱）+ 西山墙圆石烟囱；室内：一层工作区/储物、二层卧室两床、塔楼储物/书房/客房；23 扇玻璃板窗。刻意避开红石/漏斗/悬挂灯笼/活塞/悬挑，楼梯全直、火把全落实心块。老板看三张渲染图后拍板「满意」。图纸文件 `docs/协作/后勤/附件/tower_cottage.json`（生成脚本同目录）。**由后勤 18 接入 + 离线验证 + 挂说法** | 老板拍板（chat，看图） | 已下发 |
| 93 | 2026-09-06 | **#75 重启兜底可撤**（建造 25 判据成立，样本 1 次）：建造 26 起保姆脚本不再自带「锁占死自动重启 bot」；若真机再见 `lock_already_held` 立即记档、回到修缮 15 追 | 开发规划（依 #75 口径） | 待建造 26 执行 |

**下一步排程**：后勤 18（接塔楼小筑）现在开 ∥ 修缮 16 继续跑 → 后勤 18 交卷后建造 26 = 合后勤 18 + 真机连盖塔楼小筑三次（#35 口径）+ 撤 #75 兜底 + 背包旧料 A 过渡 → 修缮 16 交卷后修缮 17 = 协调性清单首修 + #89 的 C。建造线在后勤 18 交卷前歇着、不下发。


## 2026-09-06 夜：自动化巡检对话中拍板（#94）

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 94 | 2026-09-06 | **塔楼小筑三层箱子拍板 = 选 A**：直接删掉塔楼三层那只挡宜居硬闸的箱子（全屋箱子 6→5，三层作纯卧室），不采用后勤 18 建议的 B（挪二层），也不采用自动化巡检时另请 Fable 顾问提出、但未经验证的 C（床转向腾位、箱子挪到 10,9,0）。由后勤 19 落地改图纸并复验 | 老板拍板（自动化巡检对话，chat 直接回复把箱子删了） | 待后勤 19 执行 |

**下一步排程（更新）**：后勤 19（落实 #94，删箱子 + 复验宜居硬闸 + 材料清单更新）现在开 ∥ 修缮 16 继续跑 → 后勤 19 交卷后建造 26 = 合后勤 18+19 + 真机连盖塔楼小筑三次（#35 口径）+ 撤 #75 兜底 + 背包旧料 A 过渡 → 修缮 16 交卷后修缮 17 = 协调性清单首修 + #89 的 C。建造线继续歇着、不下发，等后勤 19 交卷。

---

## 2026-09-12：建造 26 / 后勤 20 / 修缮 16 交卷后（#98–#103）

建造 26（Fable）：合后勤 18+19 快进全绿；63 万列测绘证明世界无 20×16 零高差草地，选地口径放宽为「落脚 15×12 零高差、外圈 ≤1」（开发认可）；#89-A 清背包 20→6 格；**开工 5/5 失败 `verified_real_community_samples_unavailable`**——根因 = `previewBlueprint` 对 L4 且非社区导入的图纸强制走社区样板路，库里 4 份样本风格全不匹配；一块砖未盖，收工干净。后勤 20（Opus）：#89-C 临时箱子中转离线落地，四场景 + 反证全过、老路径逐字不变、88 组全绿；开工前预检押后；发现 `placeBlock`/`craftItem` 会一把放光调用方的锁（绕过未修）。修缮 16（Opus）：连玩一局抓 16 条协调性问题（3 条挡玩），最狠一条「挂起栈与队列两把尺子互抢」根治（`feat/fix@0f1a4fb8`，真机 38→1）；看板疑点 1 属实同根已修、疑点 2 虚惊但暴露「夜里自保为空」；纠正解封判据为「终态 owner 的 `lock_already_held` 0 + `reclaim` 0」。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 98 | 2026-09-12 | **本地手工图纸不管档位都走「预算路」**（建造 26 卡点选 A）：`blueprints/*.json` 里非社区导入的图纸，开工前不再要求社区样板参考；不采用 B（补一份都铎石塔样板）、C（标回 L3）。由后勤 21 离线改路由 + 单测 + 补「开工级」离线探针（21 份图纸「说法→路径」表） | 老板拍板（chat，问答） | 已下发（后勤 21） |
| 99 | 2026-09-12 | **临时箱子放下不收走**，留在工地当物资箱（后勤 20 替老板定的取舍，老板认） | 老板拍板（chat，问答） | 生效 |
| 100 | 2026-09-12 | **修缮 17 主攻「她死了整套系统不知道」**（清单第 2 条：接 `death`/`respawn`，当前任务与挂起栈按 `bot_died` 终止、释放锁、复位脱困/锚点/生存冷却、复活后对玩家说一句；不做「回去捡尸体」），**顺手把失败提示两句写死的话改成对照表**（第 9 条，≥8 种失败各一句人话、兜底带原始 key） | 老板拍板（chat，问答，选 A+B 捎上） | 已下发（修缮 17） |
| 101 | 2026-09-12 | **连玩序列下一轮就补齐剩下 8 种**（捡东西/探索/穿甲/备战/睡觉/打单怪/挖指定方块/回到玩家身边），扩成完整一局：老序列当 16 条的回归，新 8 种首跑暴露；第 7 条（插队等长 update）再量一次秒数供拍板 | 老板拍板（chat，问答，未采纳 CC 的「不扩」建议） | 已下发（修缮 17） |
| 102 | 2026-09-12 | **「跟着我」被工作命令顶掉后不自动接回，但干完要说一句**「XX 干完了，要我继续跟着你吗」；塞进对照表那轮顺手做，不改挂起/恢复逻辑 | 老板拍板（chat，问答） | 已下发（修缮 17） |
| 103 | 2026-09-12 | **建造 27 = 合三线（修缮 16 + 后勤 20 + 后勤 21）+ 真机连盖塔楼小筑三次（用建造 26 的 A/B/C 三块地与脚本）+ 三连盖完成后有余力再造「背包塞满旧料 + 荒地开工」验后勤 20**；解封判据改用修缮 16 的新口径；建造线歇着等后勤 21 交卷。另：修缮 16「同档位改先来后到」的行为变化接受，不加第三套规则；清单第 3 条判据侧（工地有玩家→等待/换原点）排后勤 22 或修缮 18；第 4 条「不在工地」那一支等后勤 20 合入后再排 | 开发规划 | 建造 27 待下发 |

**下一步排程**：后勤 21（路由 #98）∥ 修缮 17（死亡 + 对照表 + 完整一局）→ 后勤 21 交卷 → 建造 27 → 建造 27 交卷定 #35 塔楼小筑档 + #95 脚手架结论 → 修缮 17 交卷 → 修缮 18 按新清单（候选：玩家挡工地判据侧、背包满不在工地支、安全模式闸死代码、烧炼回箱补料、采矿挖脚下）。

---

## 2026-09-13：后勤 21 交卷后（#104–#105）

后勤 21（Opus）：#98 路由补丁三处最小改动，`建个塔楼小筑` 离线出施工单 715 块；`previewBudgetedSimpleBlueprint` 无第二道拦截（已证实）；补开工级离线探针 `scripts/offline-build-start-probe.js`，15 句说法 × 2 种样本库口径「说法→路径」表，改前改后仅塔楼小筑 2 行变化；89 条全绿、21 份 planId 不变。判据比 #98 字面窄一格，写成拍板问题 1。真机未验。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 104 | 2026-09-13 | **#98 范围收窄 = 选后勤 21 的 B**：只有图纸自己声明了 `metadata.complexityTier` 的本地定稿图纸直接走预算路（今天只有 `tower_cottage`）；`small_house` 老模板照旧走设计师改造 + 样板路，「盖个房子」行为不变、不改现成验收。代价：以后新图纸入库必须写档位，忘了会静默走样本路（写进入库检查） | 老板拍板（chat，问答） | 生效（后勤 21 判据不用改） |
| 105 | 2026-09-13 | **开工级离线探针进后勤线协议**：每次新图纸入库/改图纸、每次 `feat/ops` 合主干前必跑 `offline-build-start-probe.js`，表贴进反馈；写进 `docs\协作\后勤\说明.md` 硬规矩 | 老板拍板（chat，问答） | 已写进协议 |

**#103 状态更新**：建造 27 正式下发（2026-09-13）——合修缮 16（`feat/fix@0f1a4fb8`）+ 后勤 20/21（`feat/ops@a6242a5b`，四笔）→ 合并后跑探针核塔楼行 → 真机连盖塔楼小筑三次（A/B/C）→ 有余力验临时箱子；不等修缮 17。Fable 5.1 high，不开 ultracode。

**下一步排程**：建造 27（真机）∥ 修缮 17（死亡 + 对照表 + 完整一局）→ 建造 27 交卷定 #35 塔楼小筑档 + #95 脚手架结论 + 预算路真机全链路 → 修缮 17 交卷 → 后勤 22 / 修缮 18 按候选排。

---

## 2026-09-14：修缮 17 交卷后（#106–#109）

修缮 17（Fable 5.1）：死亡/复活链根治（`core/death-handler.js` 新、`abortAllForDeath`、`resetAfterDeath`；单测 7 组 + 反证；真机 3 次 kill + 1 次僵尸真死全通，死后再说「建个小木屋」续建到完工）；失败提示对照表 30+ 条 + 兜底带原始 key（一局抓出 8 个漏网键，7 个已补）；#102 跟随一句真机三次出现；完整一局：八种到七种（「找路探索」触发了 18 没验成）、16 条回归修好 1/2/9/14、仍在 6/7/8/11/12/13/16、未触发 3/4/5/10/15；第 7 条 6.66 s；新暴露 17–24，其中 17（挖不回来冻死任务系统）、18（全场搜索卡事件循环 10–20 s 被踢）为挡玩。`feat/fix` HEAD `dcc8b215`（8 笔待合）。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 106 | 2026-09-14 | **她死时队列里未开始的活一起清掉**（修缮 17 问题 1 选 B），同标 `bot_died`；代价：死前排的话要重说 | 老板拍板（chat，问答） | 已下发（修缮 18 第 2 步） |
| 107 | 2026-09-14 | **「守着我」顶掉跟随后不问那一句**（问题 2 选 B，维持任务书字面）；「守着我改成自动接回」记候选池 | 老板拍板（chat，问答，未采纳 CC 的 A） | 生效（名单不动） |
| 108 | 2026-09-14 | **修缮 18 先修清单 18（搜索卡死被踢），17（每步超时上限）排修缮 19**（问题 3 选 A 再 B） | 老板拍板（chat，问答） | 已下发（修缮 18） |
| 109 | 2026-09-14 | **建造 27 改合 `feat/fix@dcc8b215`**（含修缮 17 五笔，修缮线 8 笔三轮一次收）；建造 27 尚未开工，任务书已修订 | 老板拍板（chat，问答） | 已写进建造 27 任务书 |

另：对照表老板过目无改口；新条目 19（`续建` 丢档位）→ 建造线候选；20（`铁矿` 只认 raw_iron）→ 后勤 22 候选；21（两个任务类聊天不可达）开发暂判「有替身、不补入口」；22（复活远处夜里硬走 1500 格）修缮候选。

**下一步排程**：建造 27（合 fix@dcc8b215 + ops@a6242a5b → 真机塔楼三连盖）∥ 修缮 18（清单 18 + #106）→ 建造 27 交卷定 #35/#95/预算路全链路 + 排后勤 22 → 修缮 18 交卷 → 修缮 19 = 清单 17（每步超时上限，第 7 条同归）。

---

## 2026-09-17：修缮 18 交卷后（#110–#113）

修缮 18（Opus）：上轮「候选混远点 + 逐个算路」推断被证伪；真根因钉到两层——`UtilityBlockSearch` 对记忆里每座房子（50 座）各做一遍以她为中心的同参数全量扫描（52 遍/次）× mineflayer `findBlocks` 对 1.18+ 无调色板的单值区段逐格 `blockAt`（她脚下 2/3 区段如此）。两笔补丁（一次搜索内同参数只扫一遍 `57f1da82`；跳过「整段单一方块且非目标」区段 `6209a1c5`），五组逐格核对结果一致；真机 0 卡顿 0 踢线，搜索 ~10 s → 21–49 ms，探索首跑到（`safe_explore_point_not_found`）。#106 清队列 `8925c376`；24 话术 `2ef88e3b`；23 前提不成立（空话出自复活后 `TOO_FAR_FROM_BASE`，并入 22）。`feat/fix` HEAD `2ef88e3b`（12 笔待合）。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 110 | 2026-09-17 | **「你现在在干嘛」维持当场找一次工作台和床**（修缮 18 问题 1 选 A）；现在两次合计 ~50 ms，不改成读缓存 | 老板拍板（chat，问答） | 生效（不改） |
| 111 | 2026-09-17 | **其余 12 处直接调库 `findBlocks`/`findBlock` 的慢扫法，修缮 19 之后单开一小轮统一换**（问题 2 选 A）→ 修缮 20 | 老板拍板（chat，问答） | 待下发（修缮 20） |
| 112 | 2026-09-17 | **建造 27 改合 `feat/fix@2ef88e3b`**（含修缮 18 四笔，修缮线 12 笔一次收）；建造 27 截至 09-17 未开工，任务书再修订 | 老板拍板（chat，问答） | 已写进建造 27 任务书 |
| 113 | 2026-09-17 | **修缮 19 = 清单 17**（一步卡死拖死全局）：`tick()` 执行步看门狗（超时按 `update_timeout` 失败、放锁封 owner、放行队列；迟到返回丢弃；`ctx.heartbeat()` 给长步续命）+ `bot.dig` 超时；N 先按各任务类型正常步分布量再定；不解决第 7 条 | 老板拍板（chat，问答，按 #108 顺序） | 已下发（修缮 19） |

另：22+23 合并（复活离家太远先问玩家 + 配话术）待拍板；探索无安全点、木镐做不成两个 UNKNOWN 记候选（修缮 19 有余力只取证）。

**下一步排程**：建造 27（合 fix@2ef88e3b + ops@a6242a5b → 真机塔楼三连盖，**老板尚未起**）∥ 修缮 19（清单 17 看门狗）→ 建造 27 交卷定 #35/#95/预算路全链路 + 排后勤 22 → 修缮 19 交卷 → 修缮 20 = #111 十二处统一换 → 22+23 拍板。

---

## 2026-09-17：建造 27 交卷后（#114–#117）

建造 27（Fable）：合修缮 15–17（`288d6a9a`）+ 后勤 20/21（`d11d4c20`），只撞 `package.json` test 链两次按并集解，87→90 组全绿，主干 `D:\code\MC` 快进；探针 30 行逐行同后勤 21；三块地逐格核过。真机三栋（A `460,64,-236` / B `966,72,-278` / C `917,67,-406`）都一句话开工、都不搭脚手架过了高处（B 五个高处点全在），**0/3 完工**：A 707/715、B 714/715 卡「床按 yaw 放不按图纸 facing」（`BUILD_BED_PLACE_KEEP_YAW`，A 床头压火把格→档案卡死）；C 586/715 卡楼梯井 (4,4,5) 多一块木板 `STRUCTURAL_CLEAR_REQUIRES_RESTORE_PLAN`（来源 UNKNOWN）。高处代价约 90 s/块（运行期立柱 474 次）。新根因：同图纸活动档案不看距离（`building-system.js:1225`）；旧版脱困锁死区（`bot.js:1183` 血 ≤6 进、≥16 出、吃饭只在饱食 ≤15，血 13.5/饱食 17 卡死只能重启）。修缮 16/17 判据在建造场景成立（终态 owner 0 / reclaim 0）。

| # | 日期 | 决策 | 原出处 | 状态 |
|---|---|---|---|---|
| 114 | 2026-09-17 | **同图纸活动档案只认她 32 格以内的**（建造 27 问题 1 选 A）：`findActiveCompatible` 加距离条件（复用续建的 32 格常量）；不加中文「另起一栋」触发词（B）、不维持靠英文 rebuild（C）。交后勤 22 离线改 + 单测 | 老板拍板（chat，问答） | 已下发（后勤 22） |
| 115 | 2026-09-17 | **塔楼小筑脚手架先不开**（问题 2 选 A，#95 续）：`skipScaffolding: true` 原样；床与楼梯井修好后再用同一栋比开/关耗时 | 老板拍板（chat，问答） | 生效 |
| 116 | 2026-09-17 | **建造 28 = 床按图纸朝向放（根治，她自己放的床不受保护块豁免）+ 楼梯井放置日志取证 + 真机三连盖（首栋续 C，楼梯井木板由 CC `setblock air` 清一次记档；另两栋选新地，首令英文 rebuild 绕档案）**；#35 稳定档三连盖全完工才定 | 老板拍板（chat，问答） | 已下发（建造 28） |
| 117 | 2026-09-17 | **修缮 20 = 旧版脱困锁死区**（血 7–15 / 饱食 16–17 时不回血、不吃、不退出逃生，`legacy_escape` 占死 movement 锁）插到前面；#111 十二处慢扫法顺延修缮 21 | 老板拍板（chat，问答） | 待下发（修缮 19 交卷后） |

**下一步排程**：建造 28（床 + 日志 + 三连盖）∥ 修缮 19（看门狗，在跑）∥ 后勤 22（档案距离 + 词表 + planId 清单入库）→ 修缮 19 交卷 → 修缮 20（脱困死区）→ 建造 28 交卷定 #35 → 修缮 21（12 处）→ 22+23 拍板。
